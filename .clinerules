所有回复、思考过程、代码注释、执行计划全部使用简体中文输出。

### 代码规范
1. Python 代码严格遵循 PEP 8 规范，前端代码对齐项目现有风格，变量、函数命名见名知意，核心逻辑必须补充清晰注释。
2. 修改代码前先通读上下文，遵循项目原有架构分层与设计模式，不随意改动公共依赖和底层接口，保证代码向后兼容。
3. 新增功能优先复用现有工具函数、公共组件，避免重复造轮子；抽取公共逻辑时不破坏原有调用方式。

### 执行原则
1. 接到任务先输出清晰的执行计划，明确涉及的文件、改动范围和预期影响，确认后再开始执行。
2. 每一步改动保持最小化原则，一次只做一件事；多文件任务分步执行，每完成一个节点同步进度。
3. 遇到不确定的逻辑主动询问，不擅自猜测业务规则；发现潜在风险第一时间说明。

### 安全约束
1. 涉及数据库表结构变更、权限体系修改、核心配置改动时，必须先单独输出方案与风险说明，经确认后再执行。
2. 终端命令执行前说明命令作用，涉及删除、覆盖、环境变更的高危操作必须做二次风险提示。
3. 严格在当前项目目录内操作，不访问、修改项目外的系统文件和配置。

### 输出要求
1. 文件修改附带清晰的变更说明，明确标注新增、修改、删除的核心内容。
2. 任务完成后输出简短的验收说明，列出改动的文件、核心变更点和验证建议。
3. 报错排查优先给出根因分析，再提供修复方案，必要时给出多套可选方案。

### Ragflow 项目专属规则
1. 优先通过原生 Plugin 插件机制扩展功能，能不改核心源码就不改，降低后续官方版本升级的兼容成本。
2. 不随意修改 deepdoc 文档解析引擎、rag 检索引擎、graph 工作流执行器的底层核心逻辑，优先在外层做封装与扩展。
3. 新增接口遵循现有 api 模块的分层规范（router → service → dao），保持异常处理、参数校验、日志打印风格统一。
4. 涉及数据模型变更时，同步提供数据库迁移脚本，兼容存量数据，不直接修改生产表结构。