# RAGFlow 知识库二次开发技术方案设计文档

| 项目 | 内容 |
|---|---|
| 文档编号 | RAGFLOW-EXT-HLD-001 |
| 文档版本 | V1.0 |
| 编制依据 | 《20260819-RAGFlow知识库二开工作计划》 |
| 编写人 | 李燕文 |
| 编制日期 | 2026-09-07 ~ 2026-09-10 |
| 评审状态 | 待评审 |
| 密级 | 内部 |

**修订记录**

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| V1.0 | 2026-09-10 | 李燕文 | 初稿，覆盖 18 项二开需求的完整技术方案 |

---

## 目录

1. 文档说明　2. 需求综述与总体策略　3. 总体架构设计　4. 功能需求技术方案　5. 扩展数据库设计汇总
6. 接口规范　7. 官方版本升级兼容策略　8. 非功能设计　9. 风险与应对　10. 附录

---

# 1. 文档说明

## 1.1 编制背景与依据

本方案依据《20260819-RAGFlow知识库二开工作计划》编制，是任务 1.1「需求确认与技术方案设计」的核心交付物。工作计划确认了 **18 项二开需求**、五个里程碑（M1~M5，2026-09 至 2027-12），并明确了二开总策略：

> **以 API 扩展为主，避免修改源码。**

本文档将该策略工程化为可执行、可评审、可验收的技术设计，覆盖权限模型、多层级知识空间架构、审批流引擎三项核心技术设计，以及全部 18 项需求的逐项实现方案。

## 1.2 代码基线与环境基线

**代码基线**（以部署仓库实际版本为准，高于工作计划要求的 v0.24.0+）：

| 项 | 基线值 | 说明 |
|---|---|---|
| RAGFlow 版本 | **0.26.4** | `pyproject.toml`；`main` 分支 commit `c0582b8e1`，本地二开分支 `my-dev` |
| Python 版本 | 3.13 | `requires-python = ">=3.13,<3.14"` |
| 后端框架 | Quart（异步 Flask 系）+ Peewee ORM | Python 侧为二开主战场；Go 侧（gin + eino）为对等实现、活跃重构区，**本方案不依赖 Go 侧** |
| 前端 | React 18 + TypeScript + Vite + antd 5 | `web/` |
| 业务库 | MySQL 8.0.40（已运行） | Peewee 连接池，兼容 PostgreSQL/OceanBase |
| 向量/全文库 | Elasticsearch 8.11.3（已运行） | 经 `DocStoreConnection` 抽象，兼容 Infinity/OpenSearch 等替换 |
| 对象存储 | MinIO（已运行） | 支持 SSE-S3 服务端加密 |
| 缓存/队列 | Redis（valkey 8，已运行） | 任务队列、会话、分布式锁 |
| 网关 | nginx（ragflow 主容器内置，80 端口） | `docker/nginx/`，`API_PROXY_SCHEME` 切换 Python/Go 后端，默认 Python |

**扩展服务基线**（本文档新增，详见第 3 章）：

| 项 | 选型 | 说明 |
|---|---|---|
| ragflow-ext 后端 | Python 3.13 + FastAPI + SQLAlchemy 2.x + Alembic | 独立进程、独立目录，不侵入 RAGFlow 仓库 |
| ragflow-ext 前端 | 独立 React 18 + Vite + antd 5 应用（ext-web） | 详见 3.3 |
| 扩展库 | MySQL 8，独立 schema `ragflow_ext` | 与 RAGFlow 业务库物理隔离，可同实例 |

> **说明**：工作计划任务 1.2 中提到 Milvus，当前基线实际部署为 Elasticsearch。本方案所有涉及向量库的能力均经由 RAGFlow 的 `DocStoreConnection` 抽象层（`rag/utils/*_conn.py`）与 `/api/v1` 接口访问，**不直接依赖具体向量库选型**，后续若切换 Milvus 不影响扩展层设计。

## 1.3 二开策略总纲（四层扩展模型）

"避免修改源码"工程化为 **L0~L3 四层扩展模型**，每个需求归入其可达的**最低侵入层**：

| 层级 | 名称 | 侵入度 | 定义 | 典型场景 |
|---|---|---|---|---|
| **L0** | 纯配置层 | 零改动 | 仅使用 RAGFlow 既有配置项、既有 API、既有管理界面完成 | HTTPS/TLS 配置、MinIO SSE、答案调优参数配置 |
| **L1** | 外部扩展服务层 | **零仓库改动** | 新建独立 **ragflow-ext** 服务（独立进程/目录/数据库），所有对 RAGFlow 的读写经其官方 HTTP API（`/api/v1`，API Token 认证）完成 | 权限中心、审批流、空间层级、搜索历史、热词、外链、模板、组织架构同步、审计日志 |
| **L2** | 仓库安全区增量层 | 只增文件、**不改任何现有文件** | 在 RAGFlow 仓库**新增独立文件**：后端在 `api/apps/restful_apis/` 新增 `ext_*.py`（路由自动注册机制，Blueprint 自动生效）；前端在 `web/src/pages/` 新增页面/组件文件 | 部门过滤检索代理、防截图水印注入所需的靠近主服务的接口 |
| **L3** | 最小补丁层 | 修改既有源码 | **严格例外流程**：仅当缺陷根因在 RAGFlow 核心且无 L0~L2 绕过方案时启用；须评审批准、最小 diff、配套回归测试、记录到升级重放清单 | 标签筛选 BUG（若根因在核心检索逻辑） |

**分层判定规则**（需求评审时按序判定）：

1. 原生能力是否直接满足？→ 满足则归 **L0**；
2. 能否通过"调用原生 API + 扩展服务自有数据"实现？→ 能则归 **L1**；
3. 是否必须运行在 RAGFlow 进程内/复用其请求上下文（如检索链路内联过滤）？→ 是则归 **L2**；
4. 以上均不可行 → **L3**，进入例外评审流程（见 9.2）。

**架构总原则**（已评审确认）：**L1 为主 + L2 兜底**。扩展业务能力优先放 ragflow-ext 独立服务；仅检索增强等必须贴近主服务的场景在仓库内**新增文件**实现，**不修改任何现有文件**。

## 1.4 术语定义

| 术语 | 定义 |
|---|---|
| RAGFlow 主服务 | 官方 RAGFlow 0.26.4（Python 后端 + task_executor + deepdoc），不做源码修改（L3 除外） |
| ragflow-ext / 扩展服务 | 本方案新建的独立二开扩展服务，承载权限、审批、空间层级等扩展能力 |
| ext-web / 扩展前端 | 扩展服务配套的前端应用（管理后台、审批中心等） |
| 服务账号（Service Account） | 在 RAGFlow 内注册的专用机器账号及其 API Token，扩展服务以其身份调用 `/api/v1` |
| 知识空间（Space） | 企业知识的多层级容器，逻辑上映射到一个或多个 RAGFlow Dataset（知识库） |
| Dataset / KB | RAGFlow 原生概念「知识库」，本方案中是知识空间的物理载体 |
| 扩展库 | MySQL 中独立 schema `ragflow_ext`，存放全部二开扩展表 |
| 主库 | RAGFlow 原生 MySQL schema，扩展服务只读访问（L1 契约内），不写入 |
| L0~L3 | 见 1.3 四层扩展模型 |

## 1.5 阅读对象与文档关系

- **阅读对象**：二开开发人员、架构评审组、测试负责人、运维负责人、业务负责人。
- **文档体系**：本文档为纲领性《二开技术方案设计文档》；下游文档为《数据库设计文档》《接口规范文档》《组织架构对接方案》（任务 1.3/1.4 交付物），本文档第 5、6 章为其输入基线。

---

# 2. 需求综述与总体策略

## 2.1 需求清单（18 项）

以下清单与《工作计划》任务编号一一对应，工作量与计划周期保持一致：

| # | 需求 | 计划任务 | 工期 | 计划周期 | 里程碑 |
|---|---|---|---|---|---|
| R01 | 精细化权限管理体系（基础版） | 2.1 | 15 人天 | 2026-09-29 ~ 10-23 | M2 |
| R02 | 精细化权限管理体系（完善版） | 4.1 | 15 人天 | 2027-10-12 ~ 11-01 | M4 |
| R03 | 知识空间多层级管理 | 2.2 | 30 人天 | 2026-10-26 ~ 11-27 | M2 |
| R04 | 部门维度筛选与组织架构对接 | 2.3（前置 1.4） | 15 人天 | 2026-11-30 ~ 12-17 | M2 |
| R05 | 标签筛选功能修复 | 2.4 | 5 人天 | 2026-12-18 ~ 12-24 | M2 |
| R06 | 文档解析模型智能匹配 | 2.5 | 25 人天 | 2026-12-25 ~ 2027-01-27 | M2 |
| R07 | 多格式解析优化—核心格式（PDF/Excel） | 2.6 | 30 人天 | 2027-01-28 ~ 03-11 | M2 |
| R08 | 知识审批流程（基础版） | 2.7 | 25 人天 | 2027-03-12 ~ 04-14 | M2 |
| R09 | 文档版本管理 | 2.8 | 15 人天 | 2027-04-15 ~ 04-30 | M2 |
| R10 | 答案准确率企业场景调优 | 2.9 | 12 人天 | 2027-05-06 ~ 05-25 | M2 |
| R11 | 数据加密与传输安全配置 | 2.10 | 5 人天 | 2027-05-26 ~ 06-01 | M2 |
| R12 | 搜索历史记录 | 3.1 | 5 人天 | 2027-06-02 ~ 06-08 | M3 |
| R13 | 热词推荐 | 3.2 | 10 人天 | 2027-06-10 ~ 06-23 | M3 |
| R14 | 多格式解析优化—完善格式（PPT/Word） | 3.3 | 20 人天 | 2027-06-24 ~ 07-20 | M3 |
| R15 | 文档模板应用及附件管理 | 3.4 | 25 人天 | 2027-07-21 ~ 08-20 | M3 |
| R16 | 元数据操作简化与智能打标 | 3.5 | 20 人天 | 2027-08-23 ~ 09-17 | M3 |
| R17 | 前端文件管理页面隐藏 | 3.6 | 2 人天 | 2027-09-20 ~ 09-22 | M3 |
| R18 | 知识分享外链管理 | 3.7 | 12 人天 | 2027-09-23 ~ 10-11 | M3 |
| R19 | 审批流程完善 | 4.2 | 5 人天 | 2027-11-02 ~ 11-08 | M4 |
| R20 | 防截图泄露安全管控 | 4.3 | 15 人天 | 2027-11-09 ~ 11-26 | M4 |

> 说明：18 项需求中「权限体系」「审批流程」各拆为基础版/完善版两期实施（R01/R02、R08/R19），故开发任务条目共 20 条。里程碑：M1（2026-09-28 方案与环境）→ M2（2027-06-01 MVP）→ M3（2027-10-11 体验优化）→ M4（2027-12-10 全量测试）→ M5（2027-12-31 上线）。

## 2.2 需求—扩展层映射总表（核心）

本表是全文的纲：每项需求的实现层、承载组件、与 RAGFlow 的集成方式一览。详细设计见第 4 章对应小节。

| 需求 | 扩展层 | 承载组件 | RAGFlow 集成方式 | 是否新增仓库文件 |
|---|---|---|---|---|
| R01/R02 权限体系 | L1（+网关） | ext：权限中心 | 只读主库（知识库/用户映射）+ 网关拦截校验 | 否 |
| R03 知识空间多层级 | L1 | ext：空间树 | 空间节点 ↔ Dataset 映射表；经 `/api/v1/datasets` 管理载体 | 否 |
| R04 部门筛选 | **L1**（原生能力复用） | ext：组织架构同步 + 检索代理 | 文档元数据写部门字段（原生 metadata API）；检索用原生 `metadata_condition`/`doc_ids` 过滤 | 否 |
| R05 标签筛选修复 | L3（例外） | 主服务缺陷修复 | 根因定位后最小 patch | 是（补丁，例外流程） |
| R06 解析模型智能匹配 | L1 | ext：解析路由引擎 | 上传前经 ext 判定 parser_id，经原生上传 API 指定 | 否 |
| R07 PDF/Excel 解析优化 | L0+L1 | 配置选择 + ext 评测与路由 | 原生多 parser（naive/book/table/OCR/docling 等）+ dataset parser_id 配置 | 否 |
| R14 PPT/Word 解析优化 | L0+L1 | 同上 | 同上 | 否 |
| R08/R19 审批流程 | L1 | ext：审批流引擎 | 「先审后执」：上传暂存 ext，审批通过后代调 RAGFlow 上传/删除 API | 否 |
| R09 文档版本管理 | L1 | ext：版本中心 | 复用原生 file commits/versions API + ext 版本策略与快照（MinIO 独立桶） | 否 |
| R10 答案调优 | L0+L1 | 配置 + ext 评测平台 | 原生 assistant/chat 配置 API（Prompt、检索参数）+ ext 评测集回归 | 否 |
| R11 数据加密 | L0 | 配置 | TLS 证书、MinIO SSE-S3、MySQL 加密选项、漏洞扫描 | 否 |
| R12 搜索历史 | L1 | ext：搜索历史 | 前端检索经 ext 代理记录 | 否 |
| R13 热词推荐 | L1 | ext：热词 | 基于 R12 数据计算（频次+时效加权） | 否 |
| R15 模板与附件管理 | L1 | ext：模板/附件 | 模板产物经原生 API 入库；附件存 ext MinIO 桶 | 否 |
| R16 智能打标 | L1 | ext：打标服务 | 关键词提取（NLP/LLM）→ 人工确认 → 原生批量元数据 API 写入 | 否 |
| R17 页面隐藏 | L0+L1 | 网关路由 + ext 前端 | 按角色在网关/入口层控制路由可见性 | 否 |
| R18 分享外链 | L1 | ext：外链 | 外链校验通过后，ext 以服务账号代理拉取内容并渲染 | 否 |
| R20 防截图 | L1(+L2 可选) | ext 水印服务 + 网关 | 内容经 ext 代理输出时叠加动态水印；前端禁复制管控 | 前端新增组件文件（L2，可选） |

## 2.3 MVP 范围界定

依据工作计划 M2（2027-06-01）验收口径：

**MVP 内**（R01、R03、R04、R05、R06、R07、R08 基础版、R09、R10、R11）：
- 权限：自定义角色 + 部门维度 + 知识库级权限矩阵 + 审计日志（文档级/字段级细粒度授权**不在** MVP，归 R02）；
- 空间：多层级树 + 权限继承 + 存量数据迁移脚本；
- 审批：基础三节点流程（发起→审核→发布），配置化与加签转签归 R19；
- 解析：智能匹配 + PDF/Excel 达标（验收标准见 4.6）。

**MVP 外**（M3/M4）：R12~R18 体验优化项、R02/R19/R20 完善项。

**明确不做**（本期边界）：
- 不修改 RAGFlow 检索内核（`rag/nlp/search.py` 的 `Dealer`、分词器 `rag_tokenizer.py`）；
- 不启用/修改 Go 后端实现（`internal/`），始终以 Python 后端为运行基线；
- 不替换向量库选型（保持 ES 8，切换能力留给原生 `DocStoreConnection` 抽象）。

---

# 3. 总体架构设计

## 3.1 架构总览

```
                        ┌────────────────────────────────────────────────┐
                        │                统一入口网关（nginx）              │
                        │  · TLS 1.3 终结    · 路由分发    · 访问拦截        │
                        │  · /api/ext/*  → ragflow-ext                    │
                        │  · 其余        → RAGFlow 主服务                  │
                        │  · 角色路由控制（文件管理页隐藏 R17）              │
                        └───────┬───────────────────────────┬────────────┘
                                │                           │
              ┌─────────────────▼─────────┐   ┌─────────────▼──────────────────┐
              │   ragflow-ext 扩展服务     │   │   RAGFlow 主服务（0.26.4）      │
              │   FastAPI :9390           │   │   nginx:80 + Quart API :9380   │
              │  ┌─────────────────────┐  │   │  （不做源码修改，L3 除外）        │
              │  │ 权限中心   空间树     │  │   │  · /api/v1 官方 RESTful API    │
              │  │ 审批流引擎 版本中心   │  │   │  · /v1 前端内部 API             │
              │  │ 检索代理   解析路由   │  │   │  · task_executor（解析任务）     │
              │  │ 搜索历史   热词       │  │   │  · deepdoc 解析内核             │
              │  │ 外链       模板/附件  │  │   └───────────────┬───────────────┘
              │  │ 智能打标   水印       │  │                   │ 官方 API
              │  │ 组织架构同步 审计日志  │  │   HTTP + API Token│（服务账号）
              │  └─────────────────────┘  │───────────────────►│
              │  ext-web 扩展前端 :8390   │  唯一集成通道：/api/v1
              └───────┬───────────┬───────┘
                      │           │ 只读（权限映射查询，可选直连主库）
        ┌─────────────▼──┐   ┌────▼─────────┐   ┌────────────────────────────┐
        │ 扩展库          │   │ 主库（ragflow）│   │ 基础设施（共用）             │
        │ schema:        │   │ schema:       │   │ Redis：缓存/会话/热词        │
        │ ragflow_ext    │   │ ragflow       │   │ MinIO：附件桶/版本快照桶      │
        │ (MySQL 8)      │   │ (MySQL 8)     │   │ Elasticsearch：向量/全文检索  │
        └────────────────┘   └───────────────┘   └────────────────────────────┘
```

**核心设计原则**：

1. **唯一集成通道**：ragflow-ext 与 RAGFlow 主服务之间**只通过官方 HTTP API（`/api/v1`，Bearer API Token）通信**，不 import RAGFlow 代码、不写主库（权限映射查询允许只读直连主库，见 3.4 说明）；
2. **数据隔离**：全部二开数据落在独立 schema `ragflow_ext`，RAGFlow 升级、重装、回滚互不影响；
3. **单点收敛**：用户所有扩展能力入口统一走网关 `/api/ext/v1/*`，便于鉴权、审计、限流；
4. **RAGFlow 可替换性**：因只依赖官方 API 契约，官方版本升级时扩展层代码零改动或极小改动（见第 7 章）。

## 3.2 ragflow-ext 扩展服务设计

**技术栈**：Python 3.13 + FastAPI（async）+ SQLAlchemy 2.x（异步引擎 asyncmy）+ Alembic（迁移）+ Redis（缓存/会话）+ httpx（调用 RAGFlow API）。选型理由：与 RAGFlow 同语言降低维护成本；FastAPI 原生 async 与 OpenAPI 文档契合接口规范交付（任务 1.3）。

**模块划分**（对应第 4 章各需求）：

| 模块 | 目录（ragflow-ext/） | 承载需求 | 说明 |
|---|---|---|---|
| 认证与用户映射 | `app/auth/` | 全局 | 企业登录对接、ext JWT 签发、用户 ↔ RAGFlow 用户映射 |
| RAGFlow 客户端 | `app/ragflow_client/` | 全局 | `/api/v1` 封装：重试、限流、Token 池、错误翻译 |
| 权限中心 | `app/permission/` | R01/R02 | 角色定义、ACL、权限矩阵、缓存、鉴权 SDK |
| 空间树 | `app/space/` | R03 | 多层级空间、Dataset 映射、权限继承、迁移工具 |
| 组织架构同步 | `app/org/` | R04/R01 | 部门/人员/岗位同步、降级机制 |
| 检索代理 | `app/search_proxy/` | R04/R12/R13/R20 | 部门过滤检索、历史记录、水印注入 |
| 解析路由 | `app/parser_route/` | R06/R07/R14 | 类型识别、模型路由、效果评测 |
| 审批流引擎 | `app/approval/` | R08/R19 | 流程定义、状态机、任务通知 |
| 版本中心 | `app/version/` | R09 | 版本快照、回溯对比、回滚 |
| 评测平台 | `app/eval/` | R10 | 评测集管理、批量回归、指标报表 |
| 热词 | `app/hotword/` | R13 | 统计算法、缓存、管理后台 |
| 模板/附件 | `app/template/` | R15 | 模板引擎、附件管理 |
| 智能打标 | `app/tagging/` | R16 | 关键词抽取、推荐队列、确认写回 |
| 外链 | `app/share/` | R18 | 外链生成/校验/统计 |
| 水印与内容防护 | `app/watermark/` | R20 | 动态水印、内容代理输出 |
| 审计日志 | `app/audit/` | 全局 | 关键操作审计、查询、导出 |
| 公共设施 | `app/common/` | 全局 | 错误码、返回体、分页、幂等、任务调度（APScheduler） |

**ext-web 扩展前端**（独立 React 应用，`:8390`）：审批中心、权限管理后台、空间管理、热词管理、模板管理、打标工作台、外链管理、审计查询等管理页面；业务用户侧的搜索历史/热词组件、外链访问页、带水印阅读页。

**为何独立前端而非改 RAGFlow 前端**：RAGFlow 前端（`web/`）迭代频繁且属于"谨慎区"，独立 ext-web 使二开前端与官方前端解耦，升级零冲突；两者经网关同域部署，共享登录态（Cookie/JWT），用户无感知切换。业务用户高频页面（检索、问答）仍用 RAGFlow 原生界面（经网关加参数增强，见 4.3/4.12）。

## 3.3 认证与集成链路设计

### 3.3.1 三级身份链路

```
企业用户 ──登录──► 企业统一认证（LDAP/OAuth2/OIDC，RAGFlow 原生已支持 OIDC/OAuth）
                        │
                        ▼
              ┌──────────────────────┐
              │ 网关（同域 Cookie/JWT）│  用户浏览器同时持有：
              └──┬───────────────┬───┘  ① RAGFlow 会话（访问原生页面/API）
                 │               │      ② ext JWT（访问 /api/ext/v1/*）
     ┌───────────▼─────┐   ┌─────▼──────────────┐
     │ RAGFlow 主服务    │   │ ragflow-ext        │
     │ JWT 会话/API Token│   │ ext JWT 鉴权        │
     └─────────────────┘   └────────┬───────────┘
                                     │ 以「服务账号」身份调 /api/v1
                                     ▼
                            RAGFlow 官方 API（Bearer Token）
```

**关键机制**：

1. **用户统一**：ragflow-ext 不自建用户体系，用户表为 RAGFlow 用户表 + 组织架构的**镜像映射**（`ext_user` 表，含 `ragflow_user_id`、`dept_id`、`employee_no` 等映射字段）。账号生命周期以组织架构同步（R04）与 RAGFlow 用户为准。
2. **登录打通**：优先复用 RAGFlow 原生 OIDC/OAuth 对接企业统一认证（`api/apps/auth/oidc.py`、`oauth.py` 已原生支持）；ext 侧在网关层共享认证 Cookie。若企业暂无统一认证，则以 RAGFlow 原生账号体系为准，ext 登录接口代为校验后签发 ext JWT。
3. **服务账号**：在 RAGFlow 中注册专用机器账号（如 `svc_ext_gateway`），经原生「API Token」机制（`APIToken` 表，`api/apps/__init__.py` 的 `AUTH_API` 路径，`Authorization: Bearer <token>`）调用全部 `/api/v1` 接口。**服务账号是 ext 写 RAGFlow 数据的唯一身份**，便于审计与最小授权。
4. **权限叠加次序**：用户可见性 = RAGFlow 原生权限（owner/team，`Knowledgebase.permission`） ∩ ext 权限中心判定。ext 只做**收紧**，不做放大——即 ext 判定无权限即拒绝，判定有权限时仍受 RAGFlow 原生 ACL 约束。

### 3.3.2 ragflow_client 集成契约（L1 核心组件）

`app/ragflow_client/` 统一封装对 `/api/v1` 的调用，约束如下：

| 契约项 | 设计 |
|---|---|
| 认证 | 服务账号 API Token，热加载（支持轮换） |
| 重试 | 幂等 GET 失败指数退避重试 3 次；写操作依赖幂等键（见 6.1） |
| 限流 | 令牌桶（默认 50 QPS），防止压垮主服务 |
| 超时 | 连接 3s / 读 30s；上传与解析类长操作异步化 + 轮询回调 |
| 错误翻译 | RAGFlow `RetCode`（code/message/data 结构）→ ext 错误码（见 6.3） |
| 依赖面白名单 | 仅允许调用第 10 章附录 A 列出的原生 API，禁止调用 `/v1` 前端内部接口（契约外，官方不保证兼容） |

### 3.3.3 权限校验执行点（R01 的落地点）

| 校验点 | 位置 | 时机 | 机制 |
|---|---|---|---|
| E1 网关拦截 | nginx `auth_request` → ext `/api/ext/v1/authz` | 每个经网关的 RAGFlow 页面/API 请求 | 按 `用户×路径×方法` 判定，结果缓存 60s |
| E2 检索过滤 | ext 检索代理 | 发起检索前 | 将部门/密级等条件转为 `metadata_condition`/`doc_ids`，从源头限制召回范围 |
| E3 业务校验 | ext 各模块 | 写操作（上传/删除/分享/审批） | 服务层显式调用 `permission_service.check()` |
| E4 兜底 | RAGFlow 原生 | 所有 `/api/v1` 调用 | 原生 owner/team 语义兜底，ext 不做放大 |

> **E1 说明**：nginx `auth_request` 子请求方案无需改 RAGFlow 任何代码，仅在其部署层 nginx 配置追加 location 规则（部署配置不属于源码修改）；对性能的影响通过 60s 结果缓存 + Redis 本地缓存消除（P99 < 5ms，见 8.1）。

## 3.4 数据架构

| 数据域 | 存储 | 访问方 | 说明 |
|---|---|---|---|
| 二开业务数据 | MySQL `ragflow_ext`（28 张表，见第 5 章） | ragflow-ext 读写 | 权限/空间/审批/版本/历史/热词/外链/模板/打标/审计等 |
| RAGFlow 业务数据 | MySQL `ragflow` | 仅 RAGFlow 读写 | **ext 不写入**；权限映射查询经官方 API 拉取后缓存（首选），只读账号直连为备选优化 |
| 附件与版本快照 | MinIO 独立桶 `ext-attachments`、`ext-versions` | ragflow-ext 读写 | 不占用 RAGFlow MinIO 业务桶 |
| 缓存 | Redis db1（与 RAGFlow db0 隔离） | ragflow-ext | 权限结果、热词、解析路由表 |
| 向量/全文索引 | Elasticsearch | 仅 RAGFlow 读写 | ext 永不直接访问 |

**主库只读访问的边界说明**：权限中心需要「用户↔知识库」归属关系。首选方案是经 `/api/v1/datasets` 分页拉取并增量缓存（纯 API）；当知识库数量超过千级时，可启用备选方案：为主库建立**只读账号**，ext 仅 SELECT `knowledgebase`、`user_tenant` 等少量映射表（无写权限、连接数上限 5）。两方案均不改变 RAGFlow 数据。

## 3.5 部署拓扑（三套环境）

| 环境 | RAGFlow 主服务 | ragflow-ext | ext-web | 数据 | 用途 |
|---|---|---|---|---|---|
| dev（开发） | 源码运行（`launch_backend_service.sh`） | 源码运行 uvicorn --reload | Vite dev server | 独立 MySQL/Redis 容器 | 日常开发联调 |
| test（测试） | Docker 镜像 0.26.4 | Docker 镜像 | Docker 镜像（nginx 托管） | 独立中间件实例 | 功能/集成/回归测试 |
| pre（预发布） | Docker 镜像（与生产同版本） | Docker 镜像 | Docker 镜像 | 独立中间件实例 | 验收/UAT/性能压测 |

- **部署单元**：`ragflow`（官方镜像，不重建）、`ragflow-ext`、`ext-web` 三个容器 + 复用基础中间件（MySQL/ES/MinIO/Redis 已运行实例按环境隔离）。
- **网关**：在 ragflow 容器 nginx 配置（`docker/nginx/` 部署配置层）追加 `/api/ext/` 与 ext-web 静态资源转发规则；生产另置独立 nginx/LB 终结 TLS 1.3。
- **CI/CD**（衔接任务 1.2）：Git 分支策略 `main`（受保护，对应生产）← `release/*`（预发布）← `develop`（测试）← `feature/*`；ext 仓库与 RAGFlow fork 仓库**分离**，RAGFlow fork 仅用于跟踪上游 + 承载 L2/L3 增量，PR 评审流程一致。

## 3.6 原生能力复用清单（避免重复造轮子）

经代码核实（0.26.4），以下原生能力直接复用，是本方案"少写代码"的基础：

| 原生能力 | 代码/接口位置 | 服务的需求 |
|---|---|---|
| 官方 RESTful API 31 个模块（`/api/v1`） | `api/apps/restful_apis/`（dataset/document/chunk/search/chat/file_commit 等） | 全部 L1 集成 |
| API Token 认证 | `api/apps/__init__.py`（AUTH_API，`APIToken` 表） | 服务账号 |
| 文档元数据管理与批量更新 | `document_api.py`：`POST /datasets/<id>/metadata/update`、`PATCH /datasets/<id>/documents/metadatas`、`GET /datasets/<id>/metadata/summary` | R04 部门字段、R16 打标写回 |
| 检索元数据过滤 | `openai_api.py`（`extra_body.metadata_condition` → `common/metadata_utils.py: meta_filter` → `doc_ids`）；SDK `ragflow_sdk/ragflow.py`（`metadata_condition` 参数）；`rag/nlp/search.py: Dealer.retrieval(doc_ids=...)` | R04 部门过滤检索 |
| 文档上传指定解析器 | `document_api.py: POST /datasets/<id>/documents`（`parser_id` 参数 + PATCH 修改） | R06 智能匹配 |
| 多解析内核 | deepdoc 自带（naive/book/table/laws/paper/qa/presentation 等 `rag/app/` chunker）+ 第三方适配器（`deepdoc/parser/`：docling/mineru/mistral/paddleocr 等已预置） | R06/R07/R14 |
| 文件提交与版本 API | `file_commit_api.py`：commits CRUD / diff / tree / `workspace-files/<id>/versions` | R09 版本管理 |
| 会话与检索配置 | `search_api.py`（searches CRUD + completion SSE）、`chat_api.py`（assistant 配置、Prompt、检索参数） | R10 调优、R04/R12 代理 |
| OIDC/OAuth 登录 | `api/apps/auth/oidc.py`、`oauth.py` | 3.3 统一认证 |
| RAGFlow 原生文档预览/下载 | `/v1` 文档接口 + MinIO 签名 URL | R18 外链、R20 水印代理 |
| ES/向量库抽象 | `rag/utils/*_conn.py`（DocStoreConnection） | 向量库选型无关 |
| 官方 Python SDK | `sdk/python/ragflow_sdk/` | ext 内部直接引用 SDK 调主服务，减少手写 HTTP |

---

# 4. 功能需求技术方案

> 每项方案统一按「需求分析 → 原生能力评估 → 技术方案 → 数据模型 → 接口设计 → 交互契约 → 风险与限制」结构展开。表结构仅列关键字段，完整 DDL 见第 5 章。

## 4.1 精细化权限管理体系（R01 基础版 / R02 完善版）

**计划任务**：2.1（2026-09-29~10-23，15 人天）/ 4.1（2027-10-12~11-01，15 人天）

### 4.1.1 需求分析

工作计划要求：①扩展用户角色模型，支持**自定义角色**与**可读/可编辑权限区分**；②实现**部门维度**权限管控，对接组织架构；③知识库级**权限矩阵**配置；④权限**缓存**机制优化查询性能；⑤关键操作**审计日志**。完善版追加：⑥文档级/字段级细粒度授权；⑦岗位、密级维度；⑧权限继承与冲突解决；⑨批量校验、缓存预热；⑩渗透测试。

### 4.1.2 原生能力评估

- RAGFlow 原生权限仅两级语义：`Knowledgebase.permission = "me" | "team"`（`api/db/db_models.py:846`），团队成员角色仅 owner/invitee 粗粒度（`UserTenant.role`）。**无自定义角色、无读写分离、无部门维度**——必须由 ext 权限中心补齐；
- 原生 `login_required` 支持 JWT/API 双认证（`api/apps/__init__.py`），具备网关侧统一拦截的执行条件；
- 原生无审计日志表，需 ext 自建。

### 4.1.3 技术方案（权限模型：RBAC + 部门/岗位/密级扩展）

**模型**：`用户 → 角色（自定义）→ 资源（空间/知识库/文档/字段）× 动作（read/edit/manage/share/approve）`，叠加**主体维度扩展**（部门、岗位、密级）形成 ACL 条目。判定函数：

```
allow(user, resource, action) =
    if 全局拒绝条目命中: False                      # 显式 deny 优先
    elif 用户直接授权命中 action: True
    elif 用户部门/岗位继承授权命中（含空间树向上继承）: True
    elif 系统角色（超管/空间管理员）: True
    else: False
```

**冲突解决规则**（R02）：deny > allow；**就近优先**（文档级条目优先于知识库级，知识库级优先于空间级，用户直接授权优先于部门继承）；同层级多授权取并集。

**继承机制**：空间树（4.2）中权限随层级向下继承，子节点可收紧（override=deny）但**不可放大**超过父节点授权——与 3.3.3「ext 只收紧不放大」原则一致。

**密级维度**（R02）：文档元数据新增 `security_level`（由 R16 元数据体系承载），用户档案新增可用密级上限，检索时代理层自动追加过滤（联动 4.3 机制）。

**缓存设计**（对应计划要求④）：三级缓存——
1. L1 进程内 LRU（TTL 30s）；
2. L2 Redis `authz:{user}:{resource}:{action}`（TTL 60s，权限变更时按用户失效）；
3. 变更广播：权限写操作 → 删除相关 Redis key → 网关 auth_request 下次查询回源。批量校验接口 `POST /authz/batch-check`（R02）支持一次请求 ≤500 个资源判定，供检索代理列表过滤使用；缓存预热任务在权限变更后 5s 内重建热点用户判定。

**审计日志**：所有经 ext 的写操作 + 网关拦截的拒绝事件 + 登录事件写入 `ext_audit_log`（异步落库，不阻塞主链路），支持按用户/时间/资源/动作检索导出。

### 4.1.4 数据模型（详见 5.3）

`ext_role`（自定义角色，含权限点集合 JSON）、`ext_role_user`、`ext_permission_entry`（ACL：subject_type=user/dept/position，subject_id，resource_type=space/kb/doc/field，resource_id，action，effect=allow/deny，inherit）、`ext_user`（含 dept_id/position/安全密级上限）、`ext_audit_log`。

### 4.1.5 接口设计（`/api/ext/v1`）

| 接口 | 方法 | 说明 |
|---|---|---|
| `/roles` | GET/POST/PUT/DELETE | 角色管理（名称、描述、权限点集合） |
| `/roles/{id}/users` | POST/DELETE | 角色成员绑定 |
| `/permissions` | GET/POST/DELETE | ACL 条目管理（支持批量） |
| `/permissions/matrix` | GET/PUT | 知识库级权限矩阵视图（用户×角色×动作） |
| `/authz` | POST | 单点判定（网关 auth_request 调用，返回 204/403） |
| `/authz/batch-check` | POST | 批量判定（检索列表过滤） |
| `/authz/visible-datasets` | GET | 返回用户可见 dataset_id 列表（检索代理用） |
| `/audit/logs` | GET | 审计日志查询（分页、多条件、导出） |

### 4.1.6 与 RAGFlow 交互契约

- **读**：经 `/api/v1/datasets` 拉取知识库清单与归属，构建资源映射（或只读直连主库 `knowledgebase`/`user_tenant`，见 3.4）；
- **写**：无（权限数据全部在 ext 库；对 RAGFlow 的访问控制经 E1 网关拦截与 E2 检索过滤实现，不改主服务）；
- **建议原生配置**：企业内所有知识库 `permission="team"`（由空间管理模块在创建时统一设置），原生语义作为 E4 兜底。

### 4.1.7 风险与限制

| 风险 | 应对 |
|---|---|
| 网关拦截对原生页面 API 路径的误拦截/漏拦截 | 建立路径规则测试集（含全部 `web/src/services` 引用的 `/v1` 接口清单），回归验证；规则默认拒绝高风险写路径、放行只读路径 |
| `visible-datasets` 大知识库量级性能 | 批量接口 + Redis Set 缓存 + 增量失效 |
| R02 字段级权限的实现边界 | **明确边界**：字段级加密展示依赖内容代理（R20 水印通道），对"检索答案中不出现敏感字段"通过检索代理 Prompt+过滤实现，不承诺 DB 级字段加密 |
| 缓存不一致窗口（≤60s） | 管理端提供"立即生效"按钮强制失效；审计兜底 |

## 4.2 知识空间多层级管理（R03）

**计划任务**：2.2（2026-10-26 ~ 11-27，30 人天，MVP 最大单项）

### 4.2.1 需求分析

①设计多层级知识空间数据模型（**父子层级、空间类型差异化**）；②空间创建、编辑、归档、层级调整；③**权限继承机制**；④存量数据迁移脚本；⑤前端空间管理页面。

### 4.2.2 原生能力评估

RAGFlow 原生 `Dataset` 是**扁平结构**（仅 `tenant_id` 归属，无父子层级、无分组树）；`file_commit_api.py` 已有 folder（`_resolve_folder_id`）概念但仅作用于文件工作区。结论：层级模型必须由 ext 空间树承载，**Dataset 作为叶子载体**。

### 4.2.3 技术方案（多层级知识空间架构）

**数据模型——空间树 + 载体映射**：

- 空间节点 `ext_space`：`id / parent_id / name / space_type / status / sort_no`，`space_type ∈ {group(纯分组，无载体), kb(映射 1 个 Dataset), space_group(部门空间模板)}`；
- **承载规则**：
  - `group` 节点：纯逻辑分组，不对应任何 Dataset，仅用于挂载子节点与权限；
  - `kb` 节点：`dataset_id` 外键指向 RAGFlow Dataset，**一个 Dataset 至多挂载到一个 kb 节点**（唯一约束）；
  - 一个知识空间（子树）= 多个 group 节点 + 若干 kb 叶子节点；
- **空间类型差异化**：`ext_space_type_config` 定义各类型差异（图标、默认解析配置、默认权限模板、是否允许再建子空间），创建向导按类型加载配置。

**核心操作设计**：

| 操作 | 实现 |
|---|---|
| 创建 | ext 落 `ext_space`；若为 kb 类型 → 调 `POST /api/v1/datasets` 创建 Dataset（统一 `permission="team"`、统一 embedding 模型）→ 回写 dataset_id |
| 编辑/归档 | 归档 = ext 状态置 `archived` + 权限判定时视为不可见；**不删 Dataset**（可恢复）；真正删除走审批流（4.7） |
| 层级调整 | `ext_space.parent_id` 变更（移动子树）；**约束**：移动前校验目标子树无环、无 kb 重复挂载；权限按新位置重新继承计算（预检差异展示给管理员确认） |
| 删除 | 仅允许删除空 group；含 kb 的节点必须先移出或删除（走审批） |

**权限继承**：ACL 挂在任意空间节点（`resource_type=space`），判定时**自节点向根聚合**（见 4.1.3 继承机制）；kb 节点权限 = 空间继承 ∩ kb 级条目。层级调整后由缓存失效机制触发重算。

**存量数据迁移脚本**（计划要求④）：`ext-migrate spaces`（CLI，幂等可重跑）：
1. 扫描主库/RAGFlow API 获取全部存量 Dataset（含 name/owner/doc_num）；
2. 按迁移映射表（Excel 导入：Dataset 名 → 目标空间路径）创建 group 链与 kb 节点；
3. 未映射的 Dataset 落「待归档」根节点下，管理端批量认领；
4. 校验器：Dataset 总数 = kb 节点数（无遗漏/重复），输出迁移报告。
迁移在 test 环境预演 ≥2 轮后生产执行，**全程只读主库 + 经 API 创建，不动存量数据**。

### 4.2.4 数据模型

`ext_space`、`ext_space_type_config`、`ext_space_dataset_map`（迁移与审计用映射日志）。

### 4.2.5 接口设计

| 接口 | 方法 | 说明 |
|---|---|---|
| `/spaces/tree` | GET | 全量空间树（按用户权限过滤可见性，含每节点统计：子空间数/文档数） |
| `/spaces` | POST | 创建（group/kb，kb 类型透传 Dataset 参数） |
| `/spaces/{id}` | GET/PATCH | 详情/编辑（改名、排序、类型配置） |
| `/spaces/{id}/archive` `/restore` | POST | 归档/恢复 |
| `/spaces/{id}/move` | POST | 层级调整（含权限差异预检 `?dry_run=true`） |
| `/spaces/{id}/datasets` | GET | 节点下 Dataset 与文档列表（聚合原生 API） |
| `/spaces/migrate/preview` `/commit` | POST | 存量迁移预演/执行（管理员工具） |

### 4.2.6 与 RAGFlow 交互契约

- 创建/删除 Dataset：`POST/DELETE /api/v1/datasets`（服务账号）；
- 读取文档统计：`GET /api/v1/datasets/{id}` 及 documents 分页接口（网关缓存 5min）；
- **禁止**：任何直接 UPDATE 主库 dataset 行的操作。

### 4.2.7 风险与限制

| 风险 | 应对 |
|---|---|
| 30 人天最大单项，Dataset 双写一致性（ext 落库成功而 API 创建失败） | 创建流程改为「先 API 后落库」+ 补偿任务（孤儿 Dataset 自动认领/回收）；dataset_id 唯一约束兜底 |
| 层级调整引发权限放大 | 移动预检必须输出权限差异并二次确认；deny 条目随子树整体迁移（不留继承漏洞） |
| RAGFlow 前端（原生知识库页）展示扁平列表与空间树不一致 | R17 页面隐藏方案收敛入口：全员经 ext-web 空间页访问；管理员保留原生页（管理用途） |
| 迁移期间用户新建 Dataset 造成遗漏 | 迁移窗口内网关临时封禁原生新建入口（E1 规则），迁移后解除 |

## 4.3 部门维度筛选与组织架构对接（R04）

**计划任务**：2.3（2026-11-30 ~ 12-17，15 人天）；前置调研 1.4（组织架构接口规范、数据质量、降级机制、联调，2026-09-23~09-28）

### 4.3.1 需求分析

①扩展文档元数据，增加**部门字段**；②实现按部门维度的**检索过滤条件集成**；③部门数据**同步机制（定时+手动）**；④前端搜索页部门筛选组件；⑤与权限体系联动。

### 4.3.2 原生能力评估（重要：本项为零源码改动的关键）

经代码核实，RAGFlow 0.26.4 **已原生支持检索元数据过滤**，这是本方案得以纯 L1 实现的基石：

1. **元数据体系**：`document_api.py` 提供元数据配置与批量写入：`GET /datasets/{id}/metadata/summary`（元数据字段清单）、`POST /datasets/{id}/metadata/update`（批量更新文档元数据）、`PATCH /datasets/{id}/documents/metadatas`（批量修改）；
2. **检索过滤链路**：OpenAI 兼容接口 `openai_api.py`（chat completions）读取 `extra_body.metadata_condition`（`conditions + logic: and/or`），经 `common/metadata_utils.py:30 meta_filter()` 将条件转换为**文档 ID 集合**，注入 `Dealer.retrieval(doc_ids=...)`（`rag/nlp/search.py:549`）；官方 SDK `ragflow_sdk/ragflow.py` 的问答接口同样支持 `metadata_condition` 参数；
3. `search_api.py` 的检索应用（searches/completion）原生支持 `kb_ids` 范围限定。

**结论**：部门字段 = RAGFlow 原生文档元数据（无需扩展 ES mapping、无需改检索内核）；部门过滤 = 原生 `metadata_condition`/`doc_ids` 能力。

### 4.3.3 技术方案

**① 部门元数据设计**：每个知识库统一注入元数据字段 `department`（部门全路径编码，如 `集团/华东大区/上海分公司` 存为 `0001/0012/0035` 编码路径），另含 `department_name`（展示名）。写入时机：上传/打标/部门变更补刷（经原生 metadata API，服务账号批量执行）。

**② 检索过滤集成（ext 检索代理）**：

```
用户在搜索页选择「本部门 + 下级」
   → ext-web 调 ext /api/ext/v1/search/proxy（或经网关注入）
   → ext 权限中心求用户可见部门集合（含子部门展开）
   → ext 转换为原生过滤参数：
       metadata_condition = {
         "logic": "or",
         "conditions": [{"field":"department","op":"prefix","value":"0001/0012"},
                        {"field":"department","op":"prefix","value":"0001/0013"}]
       }
       或部门集合过大时（>N 文档）改用 doc_ids 预过滤
   → 以服务账号调用原生 completion 接口（附加用户级可见 kb_ids ∩ 过滤条件）
   → SSE 流式透传给前端（保持原生 data: 协议不变）
```

**③ 组织架构同步**（衔接任务 1.4《组织架构对接方案》）：

- 同步源：企业 HR 系统/OA 组织架构接口（部门树、人员、岗位、在职状态）；
- 模型：`ext_department`（编码、父编码、名称、状态、全路径）、`ext_user`（人员 ↔ RAGFlow 用户映射、主部门、岗位）；
- 机制：定时全量对账（每日凌晨）+ 增量同步（每 15 分钟拉取变更，若接口支持）+ 手动触发（管理端按钮，带锁防并发）；同步任务幂等（按编码 upsert）；
- **降级机制**（计划明确要求）：同步失败 → 沿用最近一次成功快照 + 管理端告警；连续 3 个周期失败 → 冻结部门变更类操作（部门权限调整）并通知管理员；HR 接口不可用时支持 Excel 导入兜底；
- 联动：部门合并/撤销 → `ext_department` 状态变更 → 触发受影响文档元数据补刷任务（队列化，限速调用原生 API）。

**④ 前端部门筛选组件**：ext-web 搜索页（对原生搜索页的增强入口）提供部门级联多选（默认勾选「本部门+下级」）；组件数据源 `GET /api/ext/v1/org/departments/tree`（缓存 10min）。

**⑤ 与权限联动**：部门筛选条件与 R01 的 `visible-datasets`/ACL 结果做**交集**（部门筛选只可在有权限的部门范围内进一步过滤）。

### 4.3.4 数据模型

`ext_department`（dept_code/parent_code/full_path/name/status/synced_at）、`ext_user`（ragflow_user_id/employee_no/dept_code/position/secondary_depts）、`ext_org_sync_log`（同步批次、来源、结果统计、降级事件）。

### 4.3.5 接口设计

| 接口 | 方法 | 说明 |
|---|---|---|
| `/org/departments/tree` | GET | 部门树（权限过滤后） |
| `/org/sync` | POST | 手动触发同步（管理员） |
| `/org/sync/status` | GET | 最近同步状态/降级状态 |
| `/org/users` | GET | 人员映射查询（管理端） |
| `/search/proxy` | POST | 部门过滤检索代理（SSE 透传原生流） |
| `/search/suggest` | GET | 检索建议（含历史 R12 与热词 R13 聚合） |

### 4.3.6 与 RAGFlow 交互契约

- 元数据写入：`POST /datasets/{id}/metadata/update`、`PATCH /datasets/{id}/documents/metadatas`（批量，≥1000 文档分批）；
- 检索执行：`POST /api/v1/chats/{id}/completions`（或 OpenAI 兼容端点）附 `metadata_condition`；检索应用经 SDK `retrieval` 同参；
- 元数据字段清单登记：`GET /datasets/{id}/metadata/summary` 用于校验部门字段已存在，缺失时自动补建（管理任务）。

### 4.3.7 风险与限制

| 风险 | 应对 |
|---|---|
| `metadata_condition` 转 doc_ids 在超大知识库下的过滤开销 | 条件命中文档数阈值监控；部门树多级编码前缀匹配降低条件数量；极端场景预热 doc_ids 缓存 |
| 组织架构接口数据质量（部门编码变更、人员兼职多部门） | 同步对账报告人工确认机制；主部门+兼职部门多值支持（元数据字段多值数组） |
| 元数据补刷量级（部门重组时全量文档刷新） | 队列化 + 限速（默认 20 文档/秒）+ 夜间窗口；进度可查 |
| 代理转发 SSE 的稳定性 | httpx 流式透传 + 断线重连（Last-Event-ID）；直连模式降级（前端直调原生接口，仅失去部门过滤时提示） |

## 4.4 标签筛选功能修复（R05）

**计划任务**：2.4（2026-12-18 ~ 12-24，5 人天）

### 4.4.1 需求分析

①定位标签筛选与组合搜索失效根因；②修复按标签搜索（单标签/多标签组合）；③回归测试不影响其他检索功能；④增加单元测试。

### 4.4.2 原生能力评估

标签 = RAGFlow 原生文档元数据的一种取值。标签筛选链路与 4.3.2 相同（`meta_filter`）。初步判断失效根因候选（按优先级排查）：

1. 前端标签选择器提交的 `metadata_condition` 结构与 `meta_filter` 期望的 `conditions` 结构不匹配（字段名/操作符拼写）；
2. 标签字段在部分知识库未登记进元数据清单（`metadata/summary` 缺失）导致过滤条件被忽略；
3. 多标签组合的 `logic`（and/or）默认值与用户预期不符（用户预期"任一命中"而实际按 and 全量求交为空）；
4. 标签值大小写/首尾空格不一致；
5. 若以上均为否 → 根因在 `common/metadata_utils.py` 的 `meta_filter` 内部逻辑（核心代码缺陷）。

### 4.4.3 技术方案（分级处置）

**第一阶段（复现与归因，~1.5 人天）**：构建标签检索用例集（单标签、多标签 and/or、跨知识库、含特殊字符标签）在 test 环境复现；按上述候选逐一取证，产出《根因分析报告》。

**第二阶段（按归因分级修复）**：

| 根因归属 | 处置层 | 方案 |
|---|---|---|
| 候选 1/2/3/4（参数构造或数据问题） | **L1** | ext 检索代理接管标签组合搜索：前端组件规范化构造 `metadata_condition`（标准化标签值 trim/lower、显式 logic）；上传/打标时校验标签字段登记。**零源码改动** |
| 候选 5（`meta_filter` 核心缺陷） | **L3 例外** | 走 9.2 例外流程：最小 patch（仅修改 `common/metadata_utils.py` 缺陷行）+ 单元测试 + 全检索功能回归 + 登记《升级重放清单》 |

**第三阶段（回归与测试，~1.5 人天）**：单元测试（`meta_filter` 边界：空条件/空结果/或逻辑/字段缺失）+ 集成回归（BM25/向量/混合检索、问答引用、部门过滤联动不受影响——因部门过滤同走 `meta_filter`，修复必须同步验证 4.3 功能）。

### 4.4.4 风险与限制

- 5 人天工期紧张，若归因结论为 L3，patch 评审与回归可能超出窗口 → 提前在 12 月初完成第一阶段归因（与 R06 并行）；
- 该缺陷同时影响部门过滤（同一链路），**修复优先级 = M2 关键路径**。

## 4.5 文档解析模型智能匹配（R06）

**计划任务**：2.5（2026-12-25 ~ 2027-01-27，25 人天）

### 4.5.1 需求分析

①集成多种解析模型（通用/BOOK/Table/OCR 等）；②文档类型**自动识别**（扩展名+文件头+内容特征）；③**模型路由策略**自动选最优解析模型；④支持手动指定覆盖；⑤解析效果对比测试与调优。

### 4.5.2 原生能力评估

- RAGFlow 解析器体系：`rag/app/` 提供 naive/book/table/laws/paper/qa/presentation/picture/audio/email/manual/resume/one 等 chunker，`task_executor.py` 的 `FACTORY`（111 行）按 `parser_id` 分发；deepdoc 另预置 docling/mineru/mistral/paddleocr 等第三方解析内核适配器（`deepdoc/parser/`）；
- 上传 API `POST /datasets/{id}/documents` 支持 `parser_id` 参数；`PATCH /datasets/{id}/documents/{doc_id}` 可修改（受文件类型约束：图片→picture、ppt→presentation 等，见 `document_api.py:285` 校验规则）；
- **结论**：解析器集成与切换是原生能力（dataset/doc 级配置），二开的核心工作是「**路由决策**」——把正确的 parser_id 放进上传请求。

### 4.5.3 技术方案（解析路由引擎）

**三层识别（上传前的预处理流水线，在 ext 侧执行）**：

| 层 | 手段 | 输出 |
|---|---|---|
| L1 扩展名 | mime/后缀映射表（可配置） | 候选 parser 集合 |
| L2 文件头 | 魔数检测（PDF %PDF、ZIP PK、OOXML content_types 等） | 修正伪装扩展名 |
| L3 内容特征 | 抽样探测：PDF 文本层可提取率（无文本层→OCR 增强）、Excel 合并单元格密度与多 Sheet 结构（→BOOK/Table）、章节结构特征（长文档多级标题→BOOK/laws） | 最终 parser_id + 置信度 |

**路由决策表**（`ext_parser_route_rule`，管理端可配置、支持优先级与条件组合）：

```
示例规则（可运营化调整）：
  ext=.pdf  ∧ 文本层可提取率<0.2        → parser=naive + OCR增强配置
  ext=.xlsx ∧ sheet数>1 ∨ 合并单元格多   → parser=book
  ext=.doc/.docx ∧ 含表格>10            → parser=naive（deepdoc 表格模式）
  ext=.ppt/.pptx                        → parser=presentation
  长文档(>200页) ∧ 章节标题规律          → parser=book
  默认                                   → dataset 默认 parser（naive）
```

**执行链路**：

```
上传请求（ext-web 或 API 网关）
  → ext 解析路由：拉取文件前 1MB 探测（文件已在 ext 附件暂存区，见 R08 审批暂存）
  → 决策 parser_id（记录决策依据到 ext_parser_route_log）
  → 调原生上传 API 携带 parser_id（或 PATCH 修正既有文档）
  → 手动覆盖：用户在上传页手动选择 parser（UI 优先级高于自动决策，记录 override 标记）
```

**效果对比与调优（计划要求⑤）**：`ext_parser_eval` 记录同文档不同 parser 的解析产物指标（分块数、表格还原率抽检、文本完整性、chunk 平均长度），管理端提供 A/B 对比视图；评测结论回流优化路由决策表（联动 4.9/R10 评测平台）。

### 4.5.4 数据模型

`ext_parser_route_rule`（规则：条件表达式、目标 parser_id、优先级、启用状态）、`ext_parser_route_log`（文档、识别特征、决策、置信度、是否人工覆盖、解析效果指标）。

### 4.5.5 接口设计

| 接口 | 方法 | 说明 |
|---|---|---|
| `/parser-route/rules` | GET/POST/PUT/DELETE | 路由规则管理 |
| `/parser-route/decide` | POST | 给定文件（暂存 key）返回推荐 parser_id 与依据（前端展示"为什么"） |
| `/parser-route/logs` | GET | 决策日志与效果指标查询 |
| `/parser-route/evaluate` | POST | 对指定文档/样本集发起多 parser 对比评测任务 |

### 4.5.6 与 RAGFlow 交互契约

- 上传：`POST /datasets/{id}/documents`（带 parser_id）或先上传后 `PATCH` 修正；
- 解析触发与状态：`POST /documents/{id}/parse`（原生 chunk/parse 接口）+ 轮询 `run` 状态；
- parser 合法值清单以 `ParserType` 枚举为准（`api/constants.py`），**路由表输出约束在枚举白名单内**，避免非法值被原生校验拒绝。

### 4.5.7 风险与限制

| 风险 | 应对 |
|---|---|
| 内容特征探测需读取文件（大文件 IO） | 仅抽样头部/尾部页；探测在暂存阶段完成，不增加上传主链路延迟（异步补判+PATCH 修正兜底） |
| 路由错误导致解析质量下降 | 手动覆盖入口 + 决策日志可追溯 + 灰度：新规则默认"建议模式"（仅提示不自动执行）运行两周 |
| 与 R07/R14 解析优化耦合 | 路由决策表是 R07/R14 成果的运营出口：解析优化结论直接落为规则配置 |

## 4.6 多格式解析优化—核心格式 PDF/Excel（R07）

**计划任务**：2.6（2027-01-28 ~ 03-11，30 人天）

### 4.6.1 需求分析

①PDF：**表格错位修复、文字错漏校正、扫描件 OCR 增强**；②Excel：**合并单元格切片对应、多 Sheet 处理、BOOK 模型应用**；③建立解析效果**验收标准**；④分格式 POC 验证达标；⑤预留**人工修正兜底**方案。

### 4.6.2 原生能力评估

- PDF：deepdoc 的 `pdf_parser.py` 与视觉层（OCR `deepdoc/vision/ocr.py`、版面 `layout_recognizer.py`、表格结构 `table_structure_recognizer.py`）深度耦合，属**禁区/谨慎区**（改错崩坏且难回归）；预置 docling/mineru 第三方内核适配；
- Excel：`deepdoc/parser/excel_parser.py` + `rag/app/book.py`（BOOK 模型）原生支持合并单元格处理与多 Sheet 行转列；
- **关键认知**：解析优化不等于改解析器——RAGFlow 的解析质量问题多数可通过 **parser 选择 + chunk 配置（`parser_config`：分块 token 数、分隔符、表格识别开关等）+ 换解析内核（docling/mineru）** 达成，全部是 dataset/文档级配置项。

### 4.6.3 技术方案（L0 配置 + L1 评测与路由）

**总体思路**：不改 deepdoc 内核，以「**样本评测 → 最优配置组合 → 路由规则固化**」闭环优化，与 R06 路由引擎无缝衔接。

**① 建立解析效果验收标准**（计划要求③，先行）：

| 指标 | 定义 | 目标值（企业可用标准） |
|---|---|---|
| 文本完整率 | 解析文本字符数 / 原文可见字符数（抽样比对） | ≥98% |
| 表格还原率 | 表格结构正确（行列表头对齐）比例（人工抽检 50 表/格式） | ≥95% |
| 乱码率 | 乱码字符占比 | ≤0.5% |
| 切片合理性 | chunk 语义完整（不截断句子/单元格）、平均长度 200~800 token | 抽检合格率 ≥95% |
| 扫描件 OCR 准确率 | 抽样字符准确率 | ≥96% |

**② PDF 优化路径**（按问题归因）：

| 问题 | 处置（全部 L0/L1） |
|---|---|
| 表格错位 | 评测对比三种组合：原生 naive+表格识别开关 / docling 内核 / mineru 内核，按表格还原率择优固化到路由规则；扫描表格走 OCR+表格结构识别配置 |
| 文字错漏 | 调整 `parser_config`（分块尺寸、页眉页脚剔除配置）；对比 docling 输出取优 |
| 扫描件 OCR 增强 | dataset 配置启用 OCR；R06 路由规则前置识别"无文本层 PDF"自动走 OCR 路径；GPU 场景下 deepdoc_server 独立部署提速（部署配置） |

**③ Excel 优化路径**：

- 合并单元格切片对应：BOOK 模型（`rag/app/book.py`）原生将合并单元格按行列展开为逐行记录（每行含完整表头上下文），评测验证展开正确性后作为 Excel 默认路由；
- 多 Sheet 处理：BOOK/naive 对多 Sheet 的分块策略对比（每 Sheet 独立分块 vs 跨 Sheet 连续），按检索命中质量选择；
- 大表性能：>10MB Excel 走预处理（ext 侧拆分后分批上传，作为兜底手段）。

**④ 人工修正兜底（计划要求⑤）**：ext 打标工作台（R16）提供「解析修正」入口——对解析失败的 chunk 支持人工编辑修正后经原生 chunk API（`PUT /chunks`）写回；对整体解析失败的文档支持下载原文、线下修正后以新版本上传（联动 R09 版本管理）。

**⑤ POC 计划**：每格式准备 20 份企业真实样本（含扫描件、复杂表格、合并单元格典型样本），在 test 环境跑 3 轮评测，产出《分格式 POC 报告》达标后固化路由规则。

### 4.6.4 数据模型

`ext_parser_eval`（评测任务：样本集、parser 组合、指标结果）、`ext_parser_eval_sample`（样本管理：文件、格式、难点标签、期望指标）。

### 4.6.5 接口设计

`/parser-route/evaluate`（见 4.5.5）扩展：`POST /parser-route/eval/samples`（样本上传）、`GET /parser-route/eval/reports/{id}`（评测报告）。

### 4.6.6 与 RAGFlow 交互契约

- 配置调优：`PUT /datasets/{id}`（parser_id、parser_config）、文档级 PATCH；
- 内核切换：dataset 的 parser 配置选择 docling/mineru 通道（原生支持，无代码改动）；
- chunk 人工修正：`PUT /datasets/{id}/documents/{doc_id}/chunks/{chunk_id}`（原生 chunk API）。

### 4.6.7 风险与限制

| 风险 | 应对 |
|---|---|
| 原生与第三方内核均不达标的问题样本 | 人工修正兜底（已内置）；极端样本不阻塞达标口径（验收标准按样本达标率而非 100%） |
| docling/mineru 依赖下载与算力成本 | POC 阶段实测资源占用；必要时 deepdoc_server/独立解析容器扩容 |
| 「解析优化」预期为改内核算法的误解 | 方案已明确边界：以配置与内核选型为主；若确需改 `deepdoc` 内核（禁区），走 9.2 例外评审并评估放弃或换内核替代 |

## 4.7 多格式解析优化—完善格式 PPT/Word（R14）

**计划任务**：3.3（2027-06-24 ~ 07-20，20 人天）

### 4.7.1 需求分析

①PPT：**全页呈现、图文混排版面还原、图片文字顺序校正**；②Word：**表格层级处理、后置表格修复、复杂排版适配**；③全格式解析效果**回归测试**；④解析**性能优化**；⑤输出《解析效果评测报告》。

### 4.7.2 原生能力评估

- PPT：`deepdoc/parser/ppt_parser.py` + `rag/app/presentation.py`（presentation chunker）原生支持按页提取；图片文字顺序依赖 OCR 结果排序；
- Word：`deepdoc/parser/docx_parser.py` 原生处理段落/表格流；后置表格（文档末尾集中表格）与复杂排版为已知短板，docling 内核对版面还原较好。

### 4.7.3 技术方案

延续 4.6 的「评测→配置→路由」闭环，与 R06/R07 复用同一引擎，差异化点：

- **PPT 全页呈现**：presentation parser 配置按页分块，每页 chunk 附带页码与缩略图（原生 chunk 支持图片，`rag/app/picture.py` 通道）；图文混排：抽取页内文本框顺序 + 图片 OCR 文本，按视觉位置排序（ext 侧用 python-pptx 复核阅读顺序，作为评测器而非修改解析器）；
- **PPT 图片文字顺序**：评测器输出乱序样本清单；对乱序率高的模板，路由到 docling 内核对比取优；
- **Word 表格层级/后置表格**：评测器检测「表格被切分到 chunk 边界」与「表格引用与表格内容远离」两类问题；处理手段：调整分块 token 数、表格独立分块配置；仍不达标样本走人工修正兜底或 docling 内核；
- **性能优化**：解析任务并发参数（task_executor 实例数与 worker 配置——**部署配置层**）、大文件异步与限流（ext 上传侧）、deepdoc_server 独立部署加速视觉模型推理；
- **回归测试**：将 R07+R14 全部样本纳入 `ext_parser_eval` 固定回归集，任何配置/路由变更触发自动回归（联动第 7 章升级回归套件）。

### 4.7.4 ~ 4.7.7 数据模型/接口/契约/风险

与 4.6 共用（`ext_parser_eval*`、`/parser-route/*`）。补充风险：

| 风险 | 应对 |
|---|---|
| PPT 视觉还原依赖 OCR 质量 | 复用 4.6 OCR 增强结论；不达标样本转人工 |
| 性能优化与稳定性平衡 | 压测基准（见 8.1）确定 task_executor 并发数；灰度调整 |

## 4.8 知识审批流程（R08 基础版 / R19 完善版）

**计划任务**：2.7（2027-03-12 ~ 04-14，25 人天）/ 4.2（2027-11-02 ~ 11-08，5 人天）

### 4.8.1 需求分析

基础版：①审批流引擎设计（**状态机、节点配置、审批人规则**）；②文档**新增/更新/删除**的审批流程；③发布前**审核校对**机制；④审批**通知**；⑤审批中心前端页面。完善版：⑥配置化（自定义节点、条件分支）；⑦**委托审批、加签、转签**；⑧超时提醒与自动升级；⑨审批数据统计报表；⑩与权限体系联动。

### 4.8.2 原生能力评估

RAGFlow 无任何审批概念（文档上传/删除直接生效）。**必须由 ext 审批流引擎承载**。设计关键约束：审批期间文档**不得进入 RAGFlow 生效**，因此采用「**先审后执**」模式——文件先暂存 ext 侧，审批通过后才写入 RAGFlow。

### 4.8.3 技术方案（审批流引擎设计）

**① 状态机**（基础版固定三节点，完善版配置化）：

```
                    ┌────────┐
   发起提交          │ DRAFT  │（暂存于 ext，可编辑/撤回）
   ────────►        └───┬────┘
                          │ submit
                    ┌─────▼──────┐   驳回（回到发起人修改重报）
                    │ REVIEWING  │◄──────────────┐
                    │（审核校对） │───────────────┤
                    └─────┬──────┘   reject      │
                          │ approve              │
                    ┌─────▼──────┐                │
                    │ PUBLISHING │（自动执行写RAGFlow）
                    └─────┬──────┘                │
                     成功 │   失败（重试/转人工）   │
                    ┌─────▼──────┐ ┌───────────┐ │
                    │ PUBLISHED  │ │ FAILED    │─┘
                    └────────────┘ └───────────┘
      任一在途状态 → CANCELLED（发起人撤回/管理员终止）
```

**② 流程定义模型**：`ext_approval_flow`（流程定义：名称、适用范围 space/dataset、动作类型 upload/update/delete、节点序列 JSON）+ `ext_approval_node`（节点：类型 submit/review/publish、审批人规则、超时策略）。基础版内置默认流程模板（三节点）；完善版开放节点编排（顺序节点 + 条件分支表达式：按文档密级/部门/大小路由到不同审批链）。

**③ 审批人规则**（节点配置）：`{type: role|user|dept_leader|space_admin|initiator_leader, value, fallback}`——支持指定角色/用户/发起人部门主管/空间管理员；fallback 保证规则解析为空时升级到管理员而非卡死。

**④ 审批实例执行**：`ext_approval_instance`（实例：业务单据、当前节点、状态）+ `ext_approval_task`（任务：审批人、动作、意见、时间）。引擎为纯 ext 内部实现（无外部工作流引擎依赖），用「乐观锁 + 状态迁移校验」保证任务不并发冲突。

**⑤ 发布前审核校对**（计划要求③）：REVIEWING 节点审批页面提供**校对视图**——暂存文档的解析预览（ext 侧调原生 API 在**独立校对知识库**（`draft` dataset，不对外授权）中试解析并取回 chunk 预览）与原文对照、元数据核对、与线上版本的 diff（联动 R09）。

**⑥ 「先审后执」执行器**：PUBLISHING 状态由异步任务执行，按动作类型调原生 API：

| 动作 | 执行（服务账号） |
|---|---|
| upload | `POST /datasets/{id}/documents`（暂存文件流转发）→ 元数据注入（部门/标签）→ 触发 parse → 轮询解析状态 |
| update | 旧文档入版本快照（R09）→ 新版本上传替换（原生支持同 doc 重新上传/更新 chunk） |
| delete | 版本快照留档 → `DELETE /datasets/{id}/documents` |

失败处理：指数退避重试 3 次 → FAILED 状态转人工（管理端告警）；**保证不出现"半发布"**（以 RAGFlow 文档可见性为准的幂等校验：重试前先查文档是否已创建）。

**⑦ 通知**：ext 通知中心（站内信 + 对接企业 IM，RAGFlow 已有钉钉/飞书/企微渠道实现可参考其消息模板，但 ext 自建通知不走 RAGFlow 渠道）——待办提醒（即时）、超时提醒（24h/48h）、审批结果通知（发起人）、发布失败告警（管理员）。

**⑧ 完善版扩展**（R19，5 人天）：加签（任务级动态追加会签人）、转签（任务转派）、委托（用户休假期间预设代理人，`ext_approval_delegate` 表）；超时自动升级（节点超时 → 升级到上级/空间管理员）；统计报表（时效、通过率、驳回原因分布）。

**⑨ 与权限联动**：审批人 = `approve` 动作 ACL 命中者（4.1）；发布后的文档自动继承空间权限；发起人须有 `edit` 权限才可发起。

### 4.8.4 数据模型

`ext_approval_flow`、`ext_approval_node`、`ext_approval_instance`、`ext_approval_task`、`ext_approval_delegate`、`ext_notification`。

### 4.8.5 接口设计

| 接口 | 方法 | 说明 |
|---|---|---|
| `/approvals/flows` | GET/POST/PUT/DELETE | 流程定义管理 |
| `/approvals/instances` | POST | 发起审批（携带暂存文件 key 与元数据） |
| `/approvals/instances` | GET | 实例列表（我发起的/待我审批/已办） |
| `/approvals/instances/{id}` | GET | 详情（含校对预览数据） |
| `/approvals/instances/{id}/submit` `/cancel` | POST | 提交/撤回 |
| `/approvals/tasks/{id}/approve` `/reject` `/return` | POST | 审批操作（意见必填） |
| `/approvals/tasks/{id}/add-sign` `/transfer` | POST | 加签/转签（R19） |
| `/approvals/delegates` | GET/POST/DELETE | 委托设置（R19） |
| `/approvals/stats` | GET | 统计报表（R19） |
| `/notifications` | GET | 站内通知 |

### 4.8.6 与 RAGFlow 交互契约

- 暂存→发布：`POST /datasets/{id}/documents`（文件流）、`POST /datasets/{id}/documents/{doc}/parse`、`DELETE /datasets/{id}/documents`、元数据 API；
- 校对预览：独立「校对库」上执行（创建后立即删除或定期清理，避免污染业务库）；
- **所有写操作经服务账号 + 幂等键**（instance_id 作为请求幂等标识），防重试重复发布。

### 4.8.7 风险与限制

| 风险 | 应对 |
|---|---|
| 双系统状态不一致（ext 已 PUBLISHED 而 RAGFlow 写失败） | 幂等校验 + FAILED 人工通道 + 对账任务（每日核对 instance 与 RAGFlow 文档存在性） |
| 大文件暂存的存储成本 | 暂存桶生命周期策略（发布成功 7 天后清理；驳回/撤回立即清理） |
| 直连原生上传绕过审批 | E1 网关规则：业务用户对 `POST /datasets/*/documents`、`DELETE` 的直连请求一律拦截重定向到审批入口；服务账号路径放行（IP+Token 双校验） |
| 基础版三节点不满足复杂场景 | R19 配置化覆盖；基础版设计已预留节点 JSON 结构，升级平滑 |

## 4.9 文档版本管理（R09）

**计划任务**：2.8（2027-04-15 ~ 04-30，15 人天）

### 4.9.1 需求分析

①版本存储策略设计（**增量/全量**）；②版本记录留存（**时间、操作人、版本号**）；③版本**回溯与对比**；④**回滚**机制；⑤前端版本管理页面。

### 4.9.2 原生能力评估

RAGFlow 0.26.4 已内置文件提交体系：`file_commit_api.py` 提供 `POST/GET /api/v1/workspaces/{id}/commits`（创建/列出提交）、`GET .../commits/{id}`（详情）、`GET .../commits/diff`（对比）、`GET .../commits/{id}/tree|files|files/{fid}/content`（树/文件/内容）、`GET /workspace-files/{fid}/versions`（文件版本历史）。**该体系面向「文件工作区（workspace）」**，而知识库文档（dataset documents）不在此体系内。结论：复用其设计思想与（workspace 场景下的）API；dataset 文档版本由 ext 版本中心承载。

### 4.9.3 技术方案

**① 版本存储策略**：`ext_document_version` 记录每次发布（联动 R08 审批）与直改事件：

- **全量快照**（默认）：源文件对象存 MinIO `ext-versions` 桶（`version/{doc_id}/{version_no}`）；元数据（名称、部门、标签、parser 配置）存表；
- **增量**（可选，针对 >20MB 文档）：源文件全量 + **chunk 级差异**（`ext_version_chunk_diff`：新增/删除/修改的 chunk 清单，从原生 chunks API 拉取快照）；
- 保留策略：可配置（默认保留最近 N=20 个版本 + 全部重大版本标记），桶生命周期自动清理。

**② 版本号规则**：`v{major}.{minor}`——审批发布的更新 major+1；元数据/解析配置调整 minor+1；记录 `operator（操作人）、operated_at（时间）、source（approval/manual/migration）`（对应计划要求②）。

**③ 回溯与对比**：
- 版本列表：文档维度时间线（版本号、操作人、时间、变更摘要、审批单链接）；
- 内容对比：两版本间 **chunk 级 diff**（新增/删除/修改 chunk 数量与明细，文本 diff 视图）；元数据 diff；源文件不同版本可下载比对。

**④ 回滚机制**：回滚 = 以历史版本源文件发起一次「update 审批」（默认走审批；低密级知识库可配置管理员直接回滚）→ 审批通过后由 R08 执行器重新上传解析。**回滚本身也是新版本**（不物理删除中间版本），保证链路完整可审计。

**⑤ 与原生 workspace commits 的关系**：若企业使用 RAGFlow「文件工作区」协作场景，其版本能力直接用原生 API（零开发）；ext 版本中心专注 dataset 文档，并在页面上提供统一入口链接两种场景。

### 4.9.4 数据模型

`ext_document_version`（doc_id、dataset_id、version_no、source、operator、file_object_key、meta_snapshot(JSON)、chunk_snapshot_key）、`ext_version_chunk_diff`。

### 4.9.5 接口设计

| 接口 | 方法 | 说明 |
|---|---|---|
| `/documents/{doc_id}/versions` | GET | 版本时间线 |
| `/documents/{doc_id}/versions/{no}` | GET | 版本详情 |
| `/documents/{doc_id}/versions/diff` | GET | 两版本对比（`?from=&to=`） |
| `/documents/{doc_id}/versions/{no}/file` | GET | 下载历史版本源文件（水印联动 R20） |
| `/documents/{doc_id}/versions/{no}/rollback` | POST | 发起回滚（走审批/直接执行，按配置） |

### 4.9.6 与 RAGFlow 交互契约

- chunk 快照：`GET /datasets/{id}/documents/{doc}/chunks`（分页拉取）；
- 回滚执行：复用 R08 执行器（上传/替换/触发解析）；
- 文档删除前置：网关删除请求 → ext 自动做最终版本快照（在 R08 delete 审批 PUBLISHING 阶段完成）。

### 4.9.7 风险与限制

| 风险 | 应对 |
|---|---|
| 大量历史版本的存储增长 | 保留策略 + 生命周期清理；存储量监控告警 |
| chunk 快照拉取对主服务的压力 | 夜间批量任务 + 限速；仅快照发生变更的文档 |
| 回滚窗口期间检索到新旧混合状态 | 回滚走审批+原子替换（原生 update 语义），过程分钟级可接受；页面提示"回滚中" |

## 4.10 答案准确率企业场景调优（R10）

**计划任务**：2.9（2027-05-06 ~ 05-25，12 人天）

### 4.10.1 需求分析

①收集企业典型问题集，建立**评测基准**；②**Prompt 工程**优化，定制企业场景系统提示词；③**检索参数调优**（切片大小、召回数量、重排序权重）；④RAG 策略配置优化；⑤准确率验证 **>90%**（M2 验收口径 >85%）。

### 4.10.2 原生能力评估

RAGFlow 对话/检索应用的全部调优旋钮均为**原生配置项**：assistant 的 system prompt、`similarity_threshold`（相似度阈值）、`vector_similarity_weight`（向量/全文权重，见 `search_api.py` `_full_text_weight`）、`top_n`（召回数量）、rerank 模型与权重、keyword 提取开关、多知识库检索、`parser_config` 切片参数（dataset 级）。**全部可经 API 配置，零代码改动**。

### 4.10.3 技术方案（评测驱动的调优闭环）

```
构建评测集（企业典型问题 200+ 条，业务负责人确认标准答案与命中文档）
   → ext 评测平台批量执行（调原生 chat completion API，逐题采集答案+引用）
   → 指标计算：命中率（引用到正确文档）、忠实度（答案与引用一致）、相关度、拒答正确率
   → 参数网格实验：切片大小 × top_n × 相似度阈值 × 向量权重 × rerank 开关 × Prompt 变体
   → 每轮实验产出指标对比报表，择优固化配置
   → 回归：调优后的配置在全量评测集复跑，目标 >90% 后冻结为生产基线
```

**① 评测基准**（`ext_eval_dataset`/`ext_eval_case`）：按业务场景分组（制度问答/数据查询/流程指引/跨库综合），每条含问题、标准答案要点、应命中文档 ID；由业务负责人评审入库（计划要求①）。

**② Prompt 工程**：企业场景系统提示词模板库（`ext_prompt_template`）——角色设定、引用规范（"仅依据引用内容回答，未命中如实告知"）、格式规范（分点/表格）、敏感场景应答策略；经评测对比择优。

**③ 检索参数调优**：参数实验框架支持变量组合批量运行与指标归集（`ext_eval_run`/`ext_eval_result`）；重点实验项与初始网格：

| 参数 | 实验网格 | 备注 |
|---|---|---|
| 切片 token 数 | 256/512/768/1024 | 影响需重解析，实验在独立校对库进行 |
| top_n | 4/6/8/12 | 召回数量 |
| vector_similarity_weight | 0.2/0.3/0.5/0.7 | 向量 vs BM25 权重 |
| similarity_threshold | 0.1/0.2/0.3 | 阈值（影响拒答率） |
| rerank | 关/开（可用模型） | 重排序 |
| Prompt | 3~5 个模板变体 | 与检索参数正交实验 |

**④ RAG 策略**：多知识库加权、GraphRAG（原生能力，密集关系型知识场景开启对比）、关键词加权（原生 query 分析配置）。

**⑤ 验证口径**：命中率 ≥90%（M2 阶段 ≥85% 里程碑口径），输出《准确率验证报告》。

### 4.10.4 数据模型

`ext_eval_dataset`、`ext_eval_case`、`ext_eval_run`（实验配置快照）、`ext_eval_result`（逐题结果：答案、引用、指标、耗时）、`ext_prompt_template`。

### 4.10.5 接口设计

`/eval/datasets`（评测集管理）、`/eval/runs`（发起/查询实验）、`/eval/runs/{id}/report`（指标报表）、`/prompts`（模板管理）。

### 4.10.6 与 RAGFlow 交互契约

- 应用配置：`PUT /api/v1/chats/{id}`（prompt、检索参数）——实验用**独立实验助手**（克隆生产配置），不直接改生产；
- 问答执行：`POST /api/v1/chats/{id}/completions`（stream=false 批量）；
- 切片实验：独立 dataset 上调整 `parser_config` 并重新解析。

### 4.10.7 风险与限制

| 风险 | 应对 |
|---|---|
| 评测集代表性不足导致过拟合 | 评测集分训练/验证两份；上线后持续收集 badcase 回流（用户点踩） |
| 切片参数变更需全量重解析（成本高） | 仅对指标显著提升的变更执行；变更经审批（R08 联动：dataset 配置变更纳入审批范围） |
| 90% 目标受知识库内容质量制约 | 调优前输出「内容缺口清单」（评测未命中的知识缺口）反哺内容建设，与业务负责人对齐预期 |

## 4.11 数据加密与传输安全配置（R11）

**计划任务**：2.10（2027-05-26 ~ 06-01，5 人天）｜扩展层：**L0 纯配置**

### 4.11.1 需求分析

①HTTPS 传输加密（**TLS 1.3 证书**）；②验证 MinIO **服务端加密（SSE-S3）**；③评估**数据库敏感字段加密**方案；④漏洞扫描与安全加固；⑤安全配置验证与文档输出。

### 4.11.2 技术方案（全配置层，不改代码）

| 项 | 方案 |
|---|---|
| HTTPS/TLS 1.3 | 生产入口 LB/nginx 终结 TLS 1.3（企业 CA 签发证书，禁用 TLS 1.0/1.1/1.2 弱协议与弱加密套件）；内部服务间（ext↔RAGFlow↔中间件）在同一受控网络平面，按企业安全基线决定是否双向 mTLS |
| MinIO SSE | MinIO 桶策略启用 SSE-S3（`x-amz-server-side-encryption: AES256`）；验证方式：上传对象后直读 MinIO 磁盘验证落盘密文；ext 附件/版本桶（4.9/R15）同样启用 |
| 数据库敏感字段 | **评估结论**：①RAGFlow 主库敏感字段（用户邮箱/手机号等）——不改表结构（避免侵入），采用**磁盘级加密**（MySQL InnoDB 表空间加密 / 云盘加密）满足静态加密要求；②ext 库自行实施字段级加密（AES-GCM，密钥经 KMS/环境变量注入）——`ext_user` 的身份映射字段、外链密码哈希等；③传输加密 MySQL `require_secure_transport=ON` |
| 漏洞扫描与加固 | 镜像扫描（trivy）+ 端口暴露收敛（仅网关 443 对外，MySQL/Redis/MinIO/ES 收内网）+ 依赖 CVE 扫描（pip-audit/npm audit）+ 弱口令治理 + nginx 安全头（CSP/HSTS/X-Frame-Options，联动 R20） |
| 验证与文档 | 安全配置核查清单（checklist）逐项验证留痕，输出《安全配置验证报告》 |

### 4.11.3 风险与限制

- TLS 证书需企业 CA 流程，提前 2 周申请（跨部门依赖）；
- MinIO SSE-S3 与既有桶对象兼容性：仅影响新写入对象，存量对象可经 copy 重写加密；
- **边界说明**：数据库「字段级」加密在 RAGFlow 主库侧不做（侵入源码/表结构），以磁盘级加密 + ext 侧字段加密组合达成合规目标，此结论写入《评估报告》供安全部门确认。

## 4.12 搜索历史记录功能（R12）

**计划任务**：3.1（2027-06-02 ~ 06-08，5 人天）｜扩展层：L1

### 4.12.1 需求分析

①搜索历史存储表结构；②历史记录**增删查**接口；③前端搜索框**历史下拉展示**；④历史**清除与隐私控制**；⑤性能优化。

### 4.12.2 技术方案

- **记录时机**：经 ext 检索代理（`/search/proxy`）发起的检索自动记录；记录字段：用户、查询词（trim 归一化）、检索范围（空间/部门过滤快照）、命中数、时间；
- **存储**：`ext_search_history`（按用户分区索引 idx(user_id, created_at DESC)）；热数据 Redis ZSet（最近 50 条）+ 冷数据 MySQL（保留 90 天，可配置）；
- **下拉展示**：`GET /api/ext/v1/search/history?limit=10` 返回去重后的最近检索词（前缀匹配当前输入可优先）；热词（R13）与历史在下拉中分区展示；
- **隐私控制**：用户可①单条删除、②一键清空、③开启"无痕检索"（会话级不记录，`ext_user_setting`）；管理员**不可见**用户明文历史（统计口径经 R13 聚合脱敏）；
- **性能**：写入异步（不阻塞检索主链路）；查询走 Redis 热路径（P99 < 10ms）。

### 4.12.3 数据模型与接口

`ext_search_history`（user_id、query、query_norm、scope_snapshot(JSON)、hit_count、created_at）；`ext_user_setting`（无痕开关等）。
接口：`GET /search/history`、`DELETE /search/history/{id}`、`DELETE /search/history`（全清）、`PUT /settings/incognito`。

### 4.12.4 风险与限制

- 历史含查询词（可能含敏感信息）：表数据 90 天滚动清理；如安全要求高，开启"仅存词频不存原文"模式（牺牲展示换取隐私）；
- 用户绕过代理直连原生检索导致漏记：E1 网关将原生检索入口收敛（R17 页面收敛后唯一入口即代理）。

## 4.13 热词推荐功能（R13）

**计划任务**：3.2（2027-06-10 ~ 06-23，10 人天）｜扩展层：L1

### 4.13.1 需求分析

①热词统计算法（**频次+时效性加权**）；②热词计算与**缓存**；③前端热词推荐模块；④热词**管理后台**；⑤**冷启动**方案。

### 4.13.2 技术方案

**① 统计算法**（数据源：R12 检索历史 + 知识库文档标题/标签语料）：

```
score(word) = Σ_over_windows [ freq(w, window) × decay(window) ]
  滑动窗口：近1h/1d/3d/7d/30d，衰减因子 1.0/0.6/0.4/0.25/0.1
  归一化项：÷ max_score，输出 Top N（默认 10）
停用词过滤 + 同义词归并（复用 RAGFlow 分词或轻量词典）+ 敏感词过滤（管理端配置）
可选增强项：点击率加权（热词被点击 → 加分）、去个人化（全局口径）
```

**② 计算与缓存**：APScheduler 每 10 分钟增量计算（新增历史按窗口累加），全量重算每日一次；结果写 Redis `hotword:global`（List）+ MySQL `ext_hotword`（快照留档，供报表）。

**③ 前端模块**：搜索页热词区（ chips 样式，点击即发起检索经代理）；数据源 `GET /api/ext/v1/hotwords?limit=10`（Redis 直读，P99 < 5ms）。

**④ 管理后台**：热词列表（当前榜单+历史趋势）、置顶/屏蔽/手动补充运营词（`ext_hotword` 的 pinned/blocked 标记，计算结果与运营词合并输出）、敏感词库维护。

**⑤ 冷启动**（计划要求⑤）：上线初期无历史数据 → 使用文档标题关键词 + 标签 TopN 作为初始热词（语料统计模式）；运营手工补充种子词；随历史积累自动切换统计模式（阈值：日检索量 ≥200 切换）。

### 4.13.3 数据模型与接口

`ext_hotword`（word、score、rank、window_stats(JSON)、pinned、blocked、snapshot_date）；
接口：`GET /hotwords`（推荐）、`/hotwords/manage`（GET/POST/PUT：榜单查询、置顶、屏蔽、补词）、`/hotwords/trends`（趋势报表）。

### 4.13.4 风险与限制

| 风险 | 应对 |
|---|---|
| 热词暴露敏感检索倾向（如"裁员"） | 敏感词过滤 + 管理端屏蔽 + 全局口径脱敏（不暴露检索人） |
| 算法效果不达预期 | 参数（衰减因子/窗口）管理端可调；预留 A/B 位（多榜单实验） |
| 冷启动期推荐质量差 | 语料模式 + 运营种子词兜底；上线首月人工运营节奏写入运维手册 |

## 4.14 文档模板应用及附件管理（R15）

**计划任务**：3.4（2027-07-21 ~ 08-20，25 人天）｜扩展层：L1

### 4.14.1 需求分析

①设计**模板引擎**与模板存储结构；②模板创建、编辑、删除；③**基于模板生成文档**；④增强**附件管理**（分类、预览、关联）；⑤前端模板管理页面。

### 4.14.2 技术方案

**① 模板引擎与存储**：

- 模板 = 模板文件（docx/xlsx/pptx/xlsx 骨架，存 MinIO `ext-attachments/templates`）+ 结构描述（`ext_template.template_schema`：占位符清单、字段类型、校验规则、适用空间/文档类型）；
- 占位语法：`{{field_name}}`（Jinja2 兼容子集），字段支持文本/数字/日期/选项/表格行集；
- 模板版本化（复用 R09 版本表结构思路，`ext_template_version`）。

**② 模板管理**：创建（上传骨架文件 → 自动扫描占位符生成字段清单 → 补充字段元数据）→ 编辑（骨架替换/字段调整，新版本）→ 启用/停用/删除（有引用时停用而非删除）；权限：空间级模板（挂空间节点）+ 全局模板，`manage` 权限者可维护。

**③ 基于模板生成文档**：用户选择模板 → 表单化填写字段（前端按 schema 渲染动态表单）→ ext 渲染引擎（docxtpl/python-docx、openpyxl 按格式）生成文件 → 用户下载完善或**直接走审批流发布**（R08 联动：生成文档默认挂审批）。生成记录留档（`ext_template_instance`，含字段值快照，满足审计与批量重生成）。

**④ 附件管理**（计划要求④）：

- 附件独立于 RAGFlow 文档体系，存 ext MinIO `ext-attachments/files`；`ext_attachment` 记录：名称、分类（自动分类：按扩展名+模板类型）、上传人、关联对象（**可关联**：知识库文档 doc_id、模板、审批单、空间节点）；
- 预览：常见格式（office/pdf/图片）经 ext 预览服务（LibreOffice headless 转 PDF + pdf.js 渲染，水印联动 R20）；大附件限制（>50MB 仅提供下载）；
- 生命周期：与关联对象解绑后进入回收站 30 天清理。

### 4.14.3 数据模型与接口

`ext_template`（name、space_id、doc_type、file_key、template_schema(JSON)、status）、`ext_template_version`、`ext_template_instance`（模板使用记录）、`ext_attachment`（name、category、file_key、size、uploader、link_type、link_id）。

接口：`/templates`（CRUD）、`/templates/{id}/fields`（字段维护）、`/templates/{id}/render`（POST 表单值 → 生成文件）、`/templates/instances`（使用记录）；`/attachments`（CRUD）、`/attachments/{id}/preview`（预览 URL）、`/attachments/{id}/link`（关联绑定）、`/attachments/categories`（分类管理）。

### 4.14.4 与 RAGFlow 交互契约

生成文档发布复用 R08 执行器（上传 API）；附件与 RAGFlow 文档的关联仅在 ext 表中维护（`link_type=document, link_id=doc_id`），不写 RAGFlow。

### 4.14.5 风险与限制

| 风险 | 应对 |
|---|---|
| office 模板渲染保真度（复杂排版） | 模板规范约束（占位符不跨表格单元格/文本框限制）；POC 验证主流模板；不达标模板建议用简单骨架 |
| LibreOffice 预览资源占用 | 独立预览 worker 容器（限并发），转换结果缓存（同附件只转一次） |

## 4.15 元数据操作简化与智能打标（R16）

**计划任务**：3.5（2027-08-23 ~ 09-17，20 人天）｜扩展层：L1

### 4.15.1 需求分析

①优化元数据管理交互，支持**标签批量打标**；②**可视化筛选组件**；③引入**关键词自动提取**（开源 NLP 或大模型 API）；④智能打标**推荐与人工确认**；⑤标签管理后台优化。

### 4.15.2 原生能力评估

原生元数据 API 完备（4.3.2 已列：summary/update/批量 metadatas），原生前端元数据交互较弱（逐文档操作）；无自动打标能力。**打标 = 元数据写入，走原生 API；智能推荐 = ext 能力**。

### 4.15.3 技术方案

**① 批量打标**：ext 打标工作台——文档列表（支持按空间/部门/解析状态筛选）→ 多选 → 批量设置标签（增量添加/移除，防覆盖）→ 调原生 `PATCH /datasets/{id}/documents/metadatas` 批量写入；进度可视化（分批 100/次）。

**② 可视化筛选组件**：ext-web 提供标签云 + 标签树 + 级联筛选器（部门联动 R04）；筛选条件转换为 `metadata_condition`（复用 4.3 链路）——**该组件同时是 R05 标签筛选修复的用户侧出口**。

**③ 关键词自动提取**（双引擎可配置）：
- 轻量引擎（默认）：keybert/RAKE 类开源方案 + 自定义业务词典（企业术语表），输入 = 文档标题 + 首 N 个 chunk 摘要（经原生 chunks API 拉取）；
- LLM 引擎（可选）：调企业 LLM（复用 RAGFlow 已配置的对话模型或企业独立 LLM 网关）批量抽取（Prompt 约束输出 JSON：标签 + 置信度 + 理由）；
- 标签候选受控词表约束（管理端维护标签体系 `ext_tag`，树形结构；自动提取只能推荐词表内标签或走"新词审核"队列）。

**④ 推荐与人工确认**（计划要求④）：`ext_tagging_task` 异步任务（新文档解析完成后自动触发或手动批量发起）→ 生成候选标签（含置信度）→ 打标工作台「待确认」队列 → 人工一键确认/修改/驳回 → 写入原生元数据；置信度 ≥ 阈值且低密级文档可配置"自动采纳"模式（仍留审计）。

**⑤ 标签管理后台**：标签体系 CRUD（树形）、合并/拆分（联动元数据批量改写）、停用（不删除历史数据）、使用统计（文档覆盖率、TopN）。

### 4.15.4 数据模型与接口

`ext_tag`（标签树：name、parent_id、status、usage_count）、`ext_tagging_task`（batch、doc_id、candidates(JSON)、status）、`ext_tag_dict`（业务词典）。
接口：`/tagging/tasks`（发起/查询）、`/tagging/tasks/{id}/confirm`（批量确认）、`/tagging/candidates/{doc_id}`（单文档候选）、`/tags`（体系管理）、`/tags/merge`（合并）、`/tags/stats`（统计）。

### 4.15.5 风险与限制

| 风险 | 应对 |
|---|---|
| LLM 批量抽取成本与速率 | 分级策略：轻量引擎跑全量、LLM 只跑低置信度样本；夜间批量 |
| 自动标签质量差污染检索 | 默认"人工确认"模式；置信度阈值可调；标签写回审计 |
| 词表外新词处理 | 新词审核队列（管理员每周处理），避免标签体系膨胀 |

## 4.16 前端文件管理页面隐藏（R17）

**计划任务**：3.6（2027-09-20 ~ 09-22，2 人天）｜扩展层：L0+L1（网关配置）

### 4.16.1 需求分析

①设计**路由权限控制**方案，按角色控制页面可见性；②隐藏面向全员的**文件管理页面**入口；③保留**管理员**完整文件管理入口；④前端仅保留**问答与检索**入口；⑤兼容性测试与用户引导。

### 4.16.2 技术方案（网关层路由控制，零源码改动）

RAGFlow 前端为 SPA（React），页面路由在前端，但页面数据依赖后端 API。方案：**网关按角色屏蔽对应页面的 API 前缀**，页面本身入口经 ext-web 统一门户收敛：

1. **E1 网关规则**（R01 已建机制，本项仅配置化）：非管理员访问文件管理相关 API 路径（`/v1/file/*`、`/v1/document/*` 管理类操作等，以 `web/src/services` 实际引用清单为准）→ 403；前端路由守卫在 ext-web 门户层隐藏入口（业务用户从 ext-web 门户进入，只见「问答、检索、（受限）空间」）；
2. **管理员通道**：管理员组（ACL `manage` 权限）访问原生完整界面（含文件管理），供运维与内容管理使用；
3. **角色-路由映射表**：`ext_route_permission`（path pattern、method、角色集），管理端可视化维护，网关热加载；
4. **用户引导**：被隐藏功能首次上线前公告 + ext-web 门户提供功能导引页。

### 4.16.3 数据模型与接口

`ext_route_permission`（pattern、methods、roles、effect）；接口：`/routes/permissions`（管理）、`GET /routes/my`（当前用户可用入口清单，门户渲染依据）。

### 4.16.4 风险与限制

| 风险 | 应对 |
|---|---|
| SPA 深链直访（用户收藏旧 URL） | API 层已拦截（数据不可得，页面空态提示"无权限"）；不追求前端路由 404 |
| 误拦问答/检索依赖的共享 API | 路径清单评审 + 兼容性测试矩阵（2 人天内：全员/管理员两角色 × 核心页面走查） |
| 官方升级新增页面路径 | 升级检查清单（第 7 章）含「路由映射表与新版前端菜单对齐」步骤 |

## 4.17 知识分享外链管理（R18）

**计划任务**：3.7（2027-09-23 ~ 10-11，12 人天）｜扩展层：L1

### 4.17.1 需求分析

①外链**生成与校验**机制（**带密码/有效期**）；②外链创建、编辑、撤销；③外链**访问统计与审计追踪**；④外发流程管控（**审批+权限校验**）；⑤前端分享管理页面。

### 4.17.2 技术方案

**① 生成与校验**：

- 链接形态：`https://kb.corp/s/{token}`——token 为 128bit 随机（不可枚举），`ext_share_link` 存储映射（目标文档/版本、密码哈希（bcrypt）、有效期、次数上限、水印策略）；
- 访问流程：`GET /s/{token}` → 有效性校验（未过期、未撤销、未超次）→ 密码校验（表单页，防爆破限速 5 次/10 分钟）→ 生成**短时会话票据**（15 分钟，绑定 IP+UA 指纹）→ 进入**只读代理视图**；
- 只读视图：ext 以服务账号拉取文档内容（原生下载/预览接口），经 R20 水印服务渲染（动态水印：访问者 IP/时间/链接归属人），**永不暴露 MinIO 原始签名 URL**；禁下载可配置。

**② 创建/编辑/撤销**：创建者须对目标文档有 `share` 权限（ACL）；编辑（改密码/有效期/次数/水印策略）即时生效；撤销立即失效（Redis 黑名单 + DB 状态）。

**③ 统计与审计**：每次访问记录 `ext_share_access`（时间、IP、UA、结果、会话时长）；管理端报表（访问趋势、TopN 链接、异常访问告警——如单链接高频异地访问）。

**④ 外发流程管控（计划要求④）**：

```
用户发起分享 → 若文档密级 ≥ 阈值（可配置）或跨部门分享
   → 强制走「外发审批」轻量流程（R08 引擎的 share 动作分支：审批通过才可生成链接）
   → 低密级文档可配置直接生成（仍留审计）
```

**⑤ 前端**：ext-web「我的分享」管理页（列表/编辑/撤销/统计）+ 外链访问页（密码页 + 阅读页）。

### 4.17.3 数据模型与接口

`ext_share_link`（token、doc_id、version_no、password_hash、expire_at、max_access、access_count、status、watermark_policy、creator）、`ext_share_access`。
接口：`/shares`（POST 创建/GET 列表/PATCH 编辑）、`/shares/{id}/revoke`（撤销）、`/shares/{id}/stats`（统计）；公开接口 `GET /s/{token}`、`POST /s/{token}/verify`（密码校验）、`GET /s/{token}/content`（票据拉取内容，SSE/分页）。

### 4.17.4 与 RAGFlow 交互契约

- 内容获取：服务账号调原生文档下载/预览接口（`/api/v1`），按需缓存（15 分钟）；
- **禁止**：向外部直接透出 RAGFlow 会话或 MinIO 签名 URL。

### 4.17.5 风险与限制

| 风险 | 应对 |
|---|---|
| 链接泄露扩散 | 有效期默认 7 天 + 次数上限 + 撤销即时生效 + 水印溯源（泄露可定位归属人） |
| 内容被爬取 | 密码 + 限速 + 票据短时效 + UA 指纹 + 高频访问告警 |
| 分享内容含敏感信息 | 密级阈值强制审批；高密级文档禁止外链（路由级配置） |

## 4.18 防截图泄露安全管控（R20）

**计划任务**：4.3（2027-11-09 ~ 11-26，15 人天）｜扩展层：L1（+可选 L2 前端组件）

### 4.18.1 需求分析

①调研前端防截图技术方案（**水印追踪、加密展示、禁用右键**）；②实现**动态水印**（用户信息+时间戳）；③开发防截图/防复制前端管控；④敏感文档内容**加密展示**；⑤安全效果验证与体验平衡。

### 4.18.2 技术方案（分层防御，明确技术边界）

**技术边界声明（重要）**：浏览器环境**无法从根本上阻止截图/拍照**（系统级截屏不可被 Web 页面拦截）。本方案以「**威慑 + 溯源 + 提高泄露成本**」为目标组合防御，该边界需与业务负责人书面确认：

| 防线 | 手段 | 效果定位 |
|---|---|---|
| ① 动态水印 | 全页面/文档视图叠加 Canvas 水印层：用户名+工号+时间戳+会话指纹，平铺半透明、随机微偏移防涂抹；DOM 变化自动重绘（MutationObserver 防删） | 泄露后可溯源到人 |
| ② 交互管控 | 禁右键/禁选择/禁拖拽（CSS user-select + 事件拦截）、禁用浏览器打印（beforeprint 置空）、复制时注入来源水印文本 | 提高搬运成本 |
| ③ 加密展示 | 敏感（高密级）文档不走原生预览，经 ext **内容代理**输出：分片渲染（Canvas 绘制而非明文 DOM）、按会话短时解密密钥（SRTP 风格一次性密钥，服务端下发，页面关闭即失效）、请求级防盗链 | 明文不出现在 DOM 与网络抓包中 |
| ④ 通道收敛 | 网关禁外链直下（配合 R18 票据）、附件预览强制走水印代理（R15） | 收敛泄露面 |
| ⑤ 行为审计 | 浏览/下载/打印尝试全量审计（联动 R01 审计日志）；异常行为（高频访问敏感文档）告警 | 事前威慑、事后追责 |

**②③ 实现位置**：水印与管控组件位于 **ext-web 前端**（独立应用，不修改 RAGFlow 前端源码）；所有需要防护的内容视图（外链阅读页、附件预览、敏感文档阅读页）均在 ext-web 渲染。若确需在 RAGFlow 原生页面注入水印（如原生问答引用预览），启用 **L2 方案**：在 `web/src/` **新增** `ext-watermark/` 独立组件文件并在扩展入口挂载（新增文件不改既有文件，经评审后实施）。

**⑤ 验证与体验平衡**（计划要求⑤）：安全效果验证（尝试截图/复制/打印/抓包/爬取的对抗测试用例）；体验平衡点：水印透明度/密度可按密级分档（低密级轻水印、高密级重防护），普通问答场景不受影响。

### 4.18.3 数据模型与接口

`ext_watermark_policy`（密级 → 水印样式/管控策略映射）、复用 `ext_audit_log`（行为审计）。
接口：`GET /watermark/policy?level=`（策略下发）、`POST /content-proxy/documents/{id}`（加密内容分片获取，含会话密钥协商）、审计事件上报 `POST /audit/events`。

### 4.18.4 与 RAGFlow 交互契约

- 内容源：服务账号调原生下载/预览接口获取字节流，代理层加密分片下发；
- 原生页面直链防护：E1 网关对高密级文档的直连下载路径默认拒绝，统一走内容代理。

### 4.18.5 风险与限制

| 风险 | 应对 |
|---|---|
| 截图不可根本阻止的预期偏差 | 方案评审时书面确认边界（威慑+溯源定位）；如企业要求系统级防护，引入桌面管理/DLP 等企业级方案（超出本项目范围，列入建议） |
| Canvas 分片渲染性能（大文档） | 分页/懒加载；首屏 < 1.5s 验收线 |
| 水印被技术手段移除 | MutationObserver 重绘 + 水印信息散布多 DOM 层 + 移除行为触发审计告警 + 视图层水印（服务端合成到图片内） |

---

# 5. 扩展数据库设计汇总

> 本章为任务 1.3《数据库设计文档》的输入基线。全部扩展表位于 MySQL 独立 schema `ragflow_ext`（utf8mb4_0900_ai_ci），**不触碰 RAGFlow 主库 schema**。

## 5.1 设计原则

1. 表名统一前缀 `ext_`，与主库表（无前缀）物理区分；
2. 所有表含公共审计列：`id BIGINT UNSIGNED AUTO_INCREMENT PK`、`created_at`、`updated_at`、`created_by`、`updated_by`；
3. 引用 RAGFlow 实体（user_id/dataset_id/doc_id）存其字符串主键（VARCHAR(64)），**不建跨库外键**（跨 schema 不强约束，靠应用层校验）；
4. JSON 字段用于可演化结构（schema/快照/规则），核心检索列冗余为独立列并建索引；
5. 迁移统一由 Alembic 管理（版本化、可回滚）。

## 5.2 表清单总览（28 张）

| 分组 | 表 | 服务的需求 | 说明 |
|---|---|---|---|
| 用户与组织 | `ext_user` | R01/R04 | RAGFlow 用户 ↔ 组织架构映射（含密级上限） |
| | `ext_department` | R04 | 部门树（编码、全路径） |
| | `ext_org_sync_log` | R04 | 组织架构同步日志（含降级事件） |
| | `ext_user_setting` | R12 | 用户偏好（无痕检索等） |
| 权限 | `ext_role` | R01/R02 | 自定义角色（权限点集合 JSON） |
| | `ext_role_user` | R01/R02 | 角色-用户绑定 |
| | `ext_permission_entry` | R01/R02 | ACL 条目（主体×资源×动作×效果） |
| | `ext_route_permission` | R17 | 角色-路由映射（页面隐藏） |
| | `ext_audit_log` | R01/R20 | 审计日志（分区表，按月） |
| 空间 | `ext_space` | R03 | 空间树节点 |
| | `ext_space_type_config` | R03 | 空间类型差异化配置 |
| | `ext_space_dataset_map` | R03 | 空间↔Dataset 映射与迁移日志 |
| 解析路由 | `ext_parser_route_rule` | R06 | 路由决策规则 |
| | `ext_parser_route_log` | R06 | 决策日志与效果指标 |
| | `ext_parser_eval` | R07/R14 | 解析评测任务 |
| | `ext_parser_eval_sample` | R07/R14 | 评测样本 |
| 审批 | `ext_approval_flow` | R08/R19 | 流程定义 |
| | `ext_approval_node` | R08/R19 | 节点定义（审批人规则、超时策略） |
| | `ext_approval_instance` | R08/R19 | 审批实例 |
| | `ext_approval_task` | R08/R19 | 审批任务 |
| | `ext_approval_delegate` | R19 | 委托设置 |
| | `ext_notification` | R08/R19 | 站内通知 |
| 版本 | `ext_document_version` | R09 | 文档版本（快照元数据） |
| | `ext_version_chunk_diff` | R09 | chunk 级差异 |
| 检索增强 | `ext_search_history` | R12 | 搜索历史 |
| | `ext_hotword` | R13 | 热词榜单快照与运营词 |
| 模板与附件 | `ext_template` / `ext_template_version` / `ext_template_instance` | R15 | 模板三件套 |
| | `ext_attachment` | R15 | 附件管理 |
| 打标 | `ext_tag` / `ext_tagging_task` / `ext_tag_dict` | R16 | 标签体系与打标任务 |
| 外链 | `ext_share_link` / `ext_share_access` | R18 | 外链与访问记录 |
| 水印 | `ext_watermark_policy` | R20 | 密级-防护策略映射 |
| 调优 | `ext_eval_dataset` / `ext_eval_case` / `ext_eval_run` / `ext_eval_result` / `ext_prompt_template` | R10 | 评测五件套 |

## 5.3 核心表 DDL（关键列示例）

以下为最核心的 8 张表 DDL 摘要（完整版在《数据库设计文档》交付）：

```sql
-- 用户映射（R01/R04）
CREATE TABLE ext_user (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  ragflow_user_id VARCHAR(64) NOT NULL COMMENT 'RAGFlow user.id',
  employee_no VARCHAR(64) DEFAULT NULL,
  dept_code VARCHAR(64) DEFAULT NULL COMMENT '主部门编码',
  secondary_depts JSON DEFAULT NULL COMMENT '兼职部门编码数组',
  position VARCHAR(128) DEFAULT NULL,
  security_level TINYINT DEFAULT 1 COMMENT '可访问密级上限 1-5',
  status TINYINT DEFAULT 1,
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_ragflow_user (ragflow_user_id),
  KEY idx_dept (dept_code)
) COMMENT='用户-组织架构映射';

-- 权限 ACL 条目（R01/R02）
CREATE TABLE ext_permission_entry (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  subject_type ENUM('user','role','dept','position') NOT NULL,
  subject_id VARCHAR(64) NOT NULL COMMENT '用户ID/角色ID/部门编码/岗位码',
  resource_type ENUM('global','space','dataset','document','field') NOT NULL,
  resource_id VARCHAR(64) NOT NULL DEFAULT '*' COMMENT '资源ID，* 表全局',
  action ENUM('read','edit','manage','share','approve') NOT NULL,
  effect ENUM('allow','deny') NOT NULL DEFAULT 'allow',
  inherit TINYINT DEFAULT 1 COMMENT '是否随空间树向下继承',
  expires_at DATETIME(3) DEFAULT NULL,
  created_by VARCHAR(64), created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  KEY idx_subject (subject_type, subject_id),
  KEY idx_resource (resource_type, resource_id),
  KEY idx_action (action)
) COMMENT='权限ACL条目';

-- 空间树（R03）
CREATE TABLE ext_space (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  parent_id BIGINT UNSIGNED DEFAULT NULL COMMENT 'NULL=根',
  name VARCHAR(128) NOT NULL,
  space_type ENUM('group','kb','space_group') NOT NULL DEFAULT 'group',
  dataset_id VARCHAR(64) DEFAULT NULL COMMENT 'kb 类型挂载的 RAGFlow dataset.id',
  status ENUM('active','archived') DEFAULT 'active',
  sort_no INT DEFAULT 0,
  path VARCHAR(512) DEFAULT NULL COMMENT '物化路径 /root/a/b',
  created_by VARCHAR(64), created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_dataset (dataset_id),
  KEY idx_parent (parent_id)
) COMMENT='多层级知识空间树';
```

```sql
-- 审批实例（R08/R19）
CREATE TABLE ext_approval_instance (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  flow_id BIGINT UNSIGNED NOT NULL,
  action ENUM('upload','update','delete','share','config') NOT NULL,
  biz_ref_type ENUM('document','dataset','space') NOT NULL,
  biz_ref_id VARCHAR(64) DEFAULT NULL,
  target_dataset_id VARCHAR(64) DEFAULT NULL,
  stash_file_key VARCHAR(256) DEFAULT NULL COMMENT '暂存文件对象键',
  meta_snapshot JSON DEFAULT NULL COMMENT '提交元数据快照',
  status ENUM('draft','reviewing','publishing','published','failed','cancelled') NOT NULL,
  current_node_id BIGINT UNSIGNED DEFAULT NULL,
  initiator VARCHAR(64) NOT NULL,
  version_no VARCHAR(32) DEFAULT NULL COMMENT '关联版本（update/delete）',
  error_msg TEXT, retry_count TINYINT DEFAULT 0,
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  updated_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
  KEY idx_status (status), KEY idx_initiator (initiator),
  KEY idx_target (target_dataset_id)
) COMMENT='审批实例（先审后执）';

-- 审批任务（R08/R19）
CREATE TABLE ext_approval_task (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  instance_id BIGINT UNSIGNED NOT NULL,
  node_id BIGINT UNSIGNED NOT NULL,
  assignee VARCHAR(64) NOT NULL,
  task_type ENUM('review','countersign') DEFAULT 'review',
  status ENUM('pending','approved','rejected','returned','transferred','timeout','cancelled') NOT NULL DEFAULT 'pending',
  comment TEXT, acted_at DATETIME(3) DEFAULT NULL,
  due_at DATETIME(3) DEFAULT NULL,
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  KEY idx_instance (instance_id), KEY idx_assignee_status (assignee, status)
) COMMENT='审批任务';

-- 文档版本（R09）
CREATE TABLE ext_document_version (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  doc_id VARCHAR(64) NOT NULL COMMENT 'RAGFlow document.id',
  dataset_id VARCHAR(64) NOT NULL,
  version_no VARCHAR(32) NOT NULL COMMENT 'v{major}.{minor}',
  source ENUM('approval','manual','migration','rollback') NOT NULL,
  operator VARCHAR(64) NOT NULL,
  file_object_key VARCHAR(256) NOT NULL COMMENT 'MinIO ext-versions 桶对象键',
  meta_snapshot JSON DEFAULT NULL,
  chunk_snapshot_key VARCHAR(256) DEFAULT NULL,
  summary VARCHAR(512) DEFAULT NULL,
  approval_instance_id BIGINT UNSIGNED DEFAULT NULL,
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_doc_version (doc_id, version_no),
  KEY idx_dataset (dataset_id)
) COMMENT='文档版本快照';

-- 搜索历史（R12）
CREATE TABLE ext_search_history (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  user_id VARCHAR(64) NOT NULL,
  query VARCHAR(512) NOT NULL,
  query_norm VARCHAR(512) NOT NULL COMMENT 'trim+lower 归一化',
  scope_snapshot JSON DEFAULT NULL,
  hit_count INT DEFAULT 0,
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  KEY idx_user_time (user_id, created_at DESC),
  KEY idx_norm (query_norm)
) COMMENT='搜索历史（90天滚动清理）';

-- 外链（R18）
CREATE TABLE ext_share_link (
  id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  token VARCHAR(64) NOT NULL COMMENT '128bit 随机 hex',
  doc_id VARCHAR(64) NOT NULL,
  dataset_id VARCHAR(64) NOT NULL,
  version_no VARCHAR(32) DEFAULT NULL,
  password_hash VARCHAR(128) DEFAULT NULL COMMENT 'bcrypt',
  expire_at DATETIME(3) DEFAULT NULL,
  max_access INT DEFAULT NULL,
  access_count INT DEFAULT 0,
  watermark_policy JSON DEFAULT NULL,
  download_enabled TINYINT DEFAULT 0,
  status ENUM('active','revoked','expired') DEFAULT 'active',
  creator VARCHAR(64) NOT NULL,
  created_at DATETIME(3) DEFAULT CURRENT_TIMESTAMP(3),
  UNIQUE KEY uk_token (token), KEY idx_creator (creator), KEY idx_doc (doc_id)
) COMMENT='知识分享外链';
```

## 5.4 数据迁移与初始化方案

| 场景 | 方案 |
|---|---|
| 全新部署 | Alembic `upgrade head` 一键建 schema + 种子数据（默认超管角色、默认审批流模板、默认路由映射） |
| 存量知识库接入空间树 | 见 4.2.3 迁移脚本（只读主库 + API 创建，幂等可重跑） |
| 存量文档补部门元数据 | 批量任务：按上传人部门映射推断 + 人工复核队列（R16 工作台），分批调原生 metadata API |
| ext 版本升级 | Alembic 增量迁移（向前兼容：只加列不改列语义，删改列需两阶段） |
| 回滚 | Alembic downgrade + ext 服务镜像回滚；主服务与主库不受影响（隔离性收益） |

---

# 6. 接口规范

> 本章为任务 1.3《接口规范文档》的输入基线。

## 6.1 通用约定

| 项 | 约定 |
|---|---|
| 基础路径 | `/api/ext/v1`（对外扩展接口）；外链公开页 `/s/{token}` |
| 协议风格 | RESTful（GET 查询 / POST 创建 / PATCH 部分更新 / PUT 全量更新 / DELETE 删除） |
| 内容类型 | `application/json; charset=utf-8`；文件上传 `multipart/form-data`；SSE 流 `text/event-stream` |
| 认证 | `Authorization: Bearer <ext-jwt>`（用户态）；网关内部调用 `X-Ext-Internal-Token` |
| 幂等 | 写操作支持 `X-Idempotency-Key` 头（服务端 24h 去重窗口），审批发布等关键操作强制 |
| 分页 | `?page=1&page_size=20`（默认 20，上限 200）；列表响应含 `total` |
| 排序 | `?order_by=created_at&order=desc`（白名单列） |
| 时间格式 | ISO 8601（`2026-09-10T16:30:00+08:00`） |
| 命名 | 路径 kebab-case 或资源复数名词；JSON 字段 snake_case |
| 版本策略 | URL 版本（v1）；不兼容变更升 v2 并保留 v1 ≥ 6 个月 |

## 6.2 统一返回格式

```json
// 成功
{ "code": 0, "message": "success", "data": { ... }, "trace_id": "..." }

// 失败
{ "code": 40301, "message": "无权访问该知识空间", "data": null,
  "trace_id": "...", "details": { "resource": "space:12", "action": "read" } }

// 分页数据体
{ "code": 0, "message": "success",
  "data": { "items": [ ... ], "total": 128, "page": 1, "page_size": 20 } }
```

SSE 流式接口沿用 RAGFlow 原生 `data: {json}\n\n` 帧协议（`{"code":0,"data":{...}}`），保证前端组件可复用。

## 6.3 错误码设计（5 位分段）

| 段 | 含义 | 示例 |
|---|---|---|
| 0 | 成功 | — |
| 1xxxx | 通用参数/请求错误 | 10001 参数校验失败 |
| 2xxxx | 认证与会话 | 20001 Token 无效；20002 Token 过期 |
| 3xxxx | 权限（权限中心） | 30001 无权限（对应 ACL 拒绝）；30002 密级不足 |
| 4xxxx | 业务规则 | 40001 审批状态机非法迁移；40002 空间层级成环；40003 外链已过期；40004 热词被屏蔽 |
| 5xxxx | 下游依赖（RAGFlow/组织架构/存储） | 50001 RAGFlow API 调用失败（透传原生 code 于 details）；50002 组织架构同步降级中 |
| 9xxxx | 系统内部错误 | 90001 数据库异常 |

错误码与 RAGFlow 原生 `RetCode` 的映射关系在 `app/ragflow_client/error_mapper.py` 维护（原生 code → ext 段 + message 翻译），映射表纳入《接口规范文档》附录。

## 6.4 OpenAPI 与契约管理

- ext 服务启用 FastAPI 自带 OpenAPI（`/api/ext/v1/docs`），**接口即文档**；
- 每次发布自动导出 openapi.json 入库（契约快照），接口变更走 PR 评审（diff 可视化）；
- 与《接口规范文档》的关系：本文档定基线（6.1~6.3 + 各需求接口清单），《接口规范文档》为逐接口字段级细化（请求/响应 schema、示例、错误场景）。

---

# 7. 官方版本升级兼容策略

## 7.1 依赖面控制（契约清单）

ext 对 RAGFlow 的全部依赖收敛为**三层白名单**，任何白名单外的依赖视为技术债并限期清理：

| 依赖层 | 内容 | 变更风险评估 |
|---|---|---|
| A 层：官方 REST API（`/api/v1`） | 附录 A 所列接口（datasets/documents/metadata/chats/search/chunks/file 等） | 低——官方对外契约，语义稳定；每版本核对参数 |
| B 层：原生数据语义 | 文档元数据机制（metadata_condition）、`Knowledgebase.permission` 语义、`APIToken` 认证、`ParserType` 枚举值 | 中——版本间可能扩展；升级前 diff 核对 |
| C 层：前端内部接口（`/v1`） | 网关拦截路径清单、页面 URL 结构（R17 路由映射依赖） | 中高——迭代频繁；升级必须重新核对 `web/src` 引用 |

**明确禁止依赖**：RAGFlow Python 内部模块 import、主库表结构（除 3.4 声明的只读映射表）、Go 侧实现、Redis 队列协议、ES 索引 mapping。

## 7.2 L2 增量文件重放机制

若启用 L2（`api/apps/restful_apis/ext_*.py` 新增文件 + 前端新增组件），升级流程：

1. 官方新版本发布 → fork 仓库 `git merge upstream/main`（**新增文件与官方文件无交集，冲突概率≈0**）；
2. CI 自动验证：路由自动注册机制是否识别 ext 文件（启动冒烟：`GET /api/v1/ext/health`）；
3. 重放脚本（`ext/tools/replay_l2.sh`）：将 `ext_l2/` 目录文件复制进仓库并跑冒烟测试——即使误删也可一键重放恢复。

## 7.3 L3 补丁管理（若触发）

- 全部 L3 patch 以 `git format-patch` 形式存放于 ext 仓库 `patches/` 目录（命名 `NNNN-description.patch`）；
- 升级时逐个 `git apply --3way`，冲突即停下人工评估（官方可能已修复 → 弃 patch；未修复 → 手工迁移）；
- 《升级重放清单》记录每个 patch 的：对应需求、根因 issue、官方修复跟踪状态。

## 7.4 升级验证流程（回归测试套件）

```
官方版本发布通知
 → 第 1 步 版本差异分析（CHANGELOG + API diff + web 路由 diff）→ 更新契约清单核对表
 → 第 2 步 升级窗口在 test 环境：merge/replay L2 + apply L3 patch → Alembic 不涉及（ext 库独立）
 → 第 3 步 自动回归套件（全部自动化，目标 ≤ 4 小时）：
     R1 认证链路（登录/Token/authz 拦截规则 × 角色矩阵）
     R2 元数据过滤链路（部门/标签/密级检索 20 用例——重点！B 层契约）
     R3 审批先审后执全链路（上传→审批→发布→版本→回滚）
     R4 空间树与迁移校验器（Dataset ↔ kb 节点一致）
     R5 解析路由冒烟（每 parser 一份样本）+ 解析评测回归集（4.7）
     R6 评测集回归（R10 固定集 ≥85% 基线）
     R7 外链/水印/审计冒烟
 → 第 4 步 性能基线对比（8.1 指标劣化 >10% 阻断）
 → 第 5 步 pre 环境验收（业务代表 UAT 抽查）→ 生产发布（灰度 10% 流量 → 全量）
```

**升级频率建议**：跟随官方 minor 版本每季度评估一次，仅采纳含安全修复或关键缺陷修复的版本；不追新功能版本。

---

# 8. 非功能设计

## 8.1 性能设计

**性能指标基线（pre 环境压测口径，M4 全量测试验收）**：

| 指标 | 目标 |
|---|---|
| 网关 auth_request 鉴权延迟 | P99 < 5ms（缓存命中路径 < 1ms） |
| ext 接口（读） | P99 < 200ms（不含下游 RAGFlow 调用） |
| 检索代理端到端 | 较直连原生检索增加延迟 P99 < 100ms |
| 权限批量判定（500 资源） | P99 < 150ms |
| 审批发布（20MB 文档） | 端到端 < 3min（含解析排队，视 task_executor 容量） |
| 并发 | 200 并发用户在线检索/问答无错误率劣化；上传并发 20 |
| ext 服务可用性 | 99.9%（月度）；主服务不受 ext 故障影响（网关降级放行策略见 8.3） |

**关键优化手段**：

- 权限三级缓存（4.1.3）+ `visible-datasets` Redis Set；
- 网关拦截规则命中缓存（60s）+ 规则静态化下发到 nginx map 文件（变更时 reload）；
- 检索代理：上游连接池复用、流式透传零缓冲、条件编译（无过滤时 302 直连原生接口的旁路模式）；
- 元数据批量任务全局限速器（默认 20 QPS，保护主服务）；
- 解析吞吐：task_executor 实例数按 CPU/内存配置（部署层），deepdoc 视觉服务独立容器可横向扩。

## 8.2 安全设计

| 领域 | 措施 |
|---|---|
| 传输 | TLS 1.3（4.11）；内部服务间隔离网络平面 |
| 认证 | ext JWT 短时效（2h）+ 刷新令牌；服务账号 Token 定期轮换（90 天，热加载）；密码 bcrypt |
| 授权 | 全部接口经 E1~E4 四执行点；默认拒绝（deny by default） |
| 审计 | 关键操作 + 拒绝事件 + 敏感文档访问全量审计（不可篡改：仅追加 + 按月归档） |
| 输入安全 | Pydantic 全量校验；标签/热词等自由文本 XSS 过滤；SQL 全参数化（SQLAlchemy） |
| 限流防刷 | 外链密码爆破限速；API 全局限流（用户级 100 QPS）；上传大小/类型白名单 |
| 密钥管理 | 密钥经环境变量/KMS 注入，不入库不入 git；审计日志脱敏 |
| 渗透测试 | M4 阶段（任务 4.4）：越权（水平/垂直）、注入、XSS、CSRF、外链绕过、水印绕过专项 |

## 8.3 可观测性与运维

- **日志**：ext 结构化日志（JSON，trace_id 贯穿网关→ext→RAGFlow 调用链）；
- **指标**：Prometheus 指标（QPS、延迟直方图、RAGFlow 调用错误率/延迟、缓存命中率、队列积压、审批 SLA）；
- **追踪**：OpenTelemetry（ext + nginx），RAGFlow 侧不注入（不侵入）；
- **告警**：RAGFlow API 错误率 >1%、审批 FAILED、组织架构降级、外链异常访问、磁盘/桶容量 80%；
- **降级预案**：ext 故障 → 网关自动切换「降级放行」模式（仅基础问答/检索可用，扩展功能暂停并公告）——保证主服务永不被 ext 拖垮；恢复后自动切回；
- **备份**：`ragflow_ext` 每日全备 + binlog 增量；MinIO ext 桶版本化；恢复演练每季度一次。

## 8.4 CI/CD 与环境策略（衔接任务 1.2）

```
feature/* → PR（lint: ruff/eslint + 单测 + OpenAPI diff 检查）→ develop
develop  → 自动部署 test（每夜回归套件）
release/* → 手动批准 → 部署 pre（UAT + 性能基线）
main     → 发布生产（灰度 10% → 全量；保留上一版本镜像一键回滚）
```

- 单测覆盖率：ext 核心模块（权限判定/审批状态机/路由决策/过滤转换）≥ 85%；
- 数据库变更：Alembic 迁移脚本必须可在空库与存量库双场景通过（CI 内置 MySQL 容器验证）；
- 配置管理：三套环境配置文件分离（`deploy/{dev,test,pre}`），敏感项走环境变量注入。

---

# 9. 风险与应对

## 9.1 风险登记册（Top 10）

| # | 风险 | 概率 | 影响 | 应对策略 | 责任 |
|---|---|---|---|---|---|
| 01 | R05 标签筛选根因在核心（触发 L3 补丁） | 中 | 高 | 提前归因（12 月初）；例外流程预案（9.2）；patch 面最小化 | 开发 |
| 02 | 双写一致性（ext↔RAGFlow 状态漂移：空间/审批/版本） | 中 | 高 | 「先 API 后落库」次序 + 幂等键 + 每日对账任务 + 孤儿回收 | 开发 |
| 03 | 网关拦截规则误伤原生功能（升级后路径变化） | 中 | 高 | 路径清单测试集纳入回归套件（R1 用例）；C 层契约每版本核对 | 开发/测试 |
| 04 | 组织架构接口质量差/不稳定 | 中 | 中 | 降级机制（快照沿用 + 告警冻结）+ Excel 兜底（4.3.3）；提前联调（任务 1.4） | 开发/业务 |
| 05 | 解析效果不达企业验收线 | 中 | 高 | 双内核（原生+docling/mineru）评测择优 + 人工修正兜底 + 验收口径按达标率（4.6） | 开发/业务 |
| 06 | 官方升级破坏 B 层契约（metadata_condition 语义变化） | 低 | 高 | 回归套件 R2 专项覆盖；升级前 API diff 分析；锁定版本策略（7.4） | 开发 |
| 07 | 单人开发（李燕文）进度与知识风险 | 高 | 中 | 文档先行（本方案+前置总结）；L1 架构降低代码耦合面；关键模块结对评审（借调评审人）；里程碑缓冲（M2/M3 尾部留 1 周缓冲） | 项目 |
| 08 | ext 代理链路引入性能劣化 | 中 | 中 | 旁路直连模式（无过滤时不代理）；性能基线门禁（劣化 >10% 阻断发布） | 开发 |
| 09 | 审批流与权限模型复杂度超出 25/15 人天预算 | 中 | 中 | 基础版严格三节点；完善版配置化延后（M4）；状态机表驱动实现降低复杂度 | 开发 |
| 10 | 防截图预期与 Web 技术边界冲突 | 高 | 中 | 评审阶段书面确认边界（威慑+溯源定位）；分档策略平衡体验（4.18.2） | 业务/开发 |

**共性应对**：所有高风险项在双周项目例会跟踪状态；《与目标的差异/原因分析/解决方案》四列（工作计划表预留）随周报更新。

## 9.2 策略边界例外流程（L3 触发条件与审批）

「避免修改源码」策略存在 3 处已识别的边界情况，统一按以下例外流程处置（未经流程批准，任何人不得修改 RAGFlow 源码）：

**已识别的边界情况**：

1. **R05 标签筛选缺陷根因在 `common/metadata_utils.py` 核心**（4.4.2 候选 5）；
2. **解析优化遇到 deepdoc 内核缺陷且双内核均无法绕过**（4.6.7 评估结论为必须改内核时）；
3. **R17/R20 需要在原生前端进程内注入能力且网关/ext-web 手段无法覆盖**（如原生问答引用预览强制水印）。

**例外流程**：

```
① 论证申请：提出人提交《L3 例外评审单》——问题复现证据、L0~L2 不可行的论证、
   拟修改文件与 diff 预览、影响面分析、官方 issue/PR 跟踪情况
② 双人评审：开发负责人 + 1 名评审人（技术委员会/架构组）会签；
   评审要点：diff 最小化（优先 1 个文件内）、是否可 upstream 化、回滚方案
③ 实施约束：patch 以 git format-patch 存档（7.3）；补充针对性单测；
   全检索/解析回归通过；记录《升级重放清单》
④ 上游对齐：向官方仓库提 issue/PR 跟踪修复；官方修复后移除本地 patch
⑤ 汇报：每次 L3 触发向项目干系人通报（累计数量纳入架构健康度指标，目标 ≤3 个）
```

**兜底原则**：若某需求的 L3 方案评审不通过（影响面不可控），该需求功能范围与业务负责人重新协商（降级实现或调整排期），**不默认以修改源码换取进度**。

---

# 10. 附录

## 附录 A：RAGFlow 0.26.4 原生 API 能力对照表（A 层契约白名单）

ext 服务允许调用的官方 REST API（`/api/v1`，Bearer Token 认证，响应结构 `{code, message, data}`）：

| 能力域 | 接口 | 用途（本方案） |
|---|---|---|
| 数据集 | `POST /datasets`、`GET /datasets`、`GET /datasets/{id}`、`PUT /datasets/{id}`、`DELETE /datasets/{id}` | 空间管理（R03）、配置调优（R10） |
| 文档上传 | `POST /datasets/{id}/documents`（支持 `parser_id`）、`POST /documents/upload`（预检信息） | 审批发布（R08）、解析路由（R06） |
| 文档管理 | `GET /datasets/{id}/documents`（列表/过滤）、`PATCH /datasets/{id}/documents/{doc_id}`（含 parser 修正）、`DELETE /datasets/{id}/documents`、`POST /datasets/{id}/documents/status` | 发布/版本/回滚（R08/R09） |
| 元数据 | `GET /datasets/{id}/metadata/summary`、`POST /datasets/{id}/metadata/update`、`PATCH /datasets/{id}/documents/metadatas`、`PUT /datasets/{id}/documents/{doc}/metadata/config` | 部门字段（R04）、打标（R16） |
| 解析 | `POST /datasets/{id}/chunks`（触发解析）、`GET /datasets/{id}/documents/{doc}/chunks`、`PUT /datasets/{id}/documents/{doc}/chunks/{chunk_id}` | 解析触发/快照/人工修正（R06/R07/R09） |
| 对话 | `POST /chats`、`PUT /chats/{id}`（prompt/检索参数）、`POST /chats/{id}/completions`（SSE，支持 `doc_ids`/`metadata_condition` 透传于 OpenAI 兼容端点） | 调优（R10）、检索代理（R04/R12） |
| 检索应用 | `POST /searches`、`GET/PUT/DELETE /searches/{id}`、`POST /searches/{id}/completions`（SSE，`kb_ids` 范围） | 检索代理（R04） |
| OpenAI 兼容 | `POST /api/v1/openai/chat_completions`（`extra_body.metadata_condition`） | 部门/密级过滤问答（R04/R02） |
| 文件工作区 | `POST/GET /workspaces/{id}/commits`、`GET .../commits/{id}`、`GET .../commits/diff`、`GET /workspace-files/{fid}/versions` 等 | 版本管理（R09，workspace 场景复用原生） |
| 用户与租户 | `GET /users`（信息）、`GET /tenants`（团队） | 用户映射构建（3.3） |
| 模型 | `GET/POST /llms`（模型配置） | 调优环境准备（R10） |

> 完整参数以 `api/apps/restful_apis/` 源码与官方 SDK（`sdk/python/ragflow_sdk/`）为准；《接口规范文档》附录将逐接口字段化。**白名单外接口（如 `/v1` 前端内部接口）禁止在 ext 代码中出现（CI 静态检查）**。

## 附录 B：需求—计划任务—章节对照索引

| 需求 | 工作计划任务 | 方案章节 | 扩展层 |
|---|---|---|---|
| R01/R02 权限体系 | 2.1 / 4.1 | 4.1 | L1 |
| R03 知识空间 | 2.2 | 4.2 | L1 |
| R04 部门筛选/组织架构 | 1.4 / 2.3 | 4.3 | L1 |
| R05 标签筛选修复 | 2.4 | 4.4 | L1 或 L3 |
| R06 解析智能匹配 | 2.5 | 4.5 | L1 |
| R07 PDF/Excel 优化 | 2.6 | 4.6 | L0+L1 |
| R14 PPT/Word 优化 | 3.3 | 4.7 | L0+L1 |
| R08/R19 审批流程 | 2.7 / 4.2 | 4.8 | L1 |
| R09 版本管理 | 2.8 | 4.9 | L1 |
| R10 答案调优 | 2.9 | 4.10 | L0+L1 |
| R11 数据加密 | 2.10 | 4.11 | L0 |
| R12 搜索历史 | 3.1 | 4.12 | L1 |
| R13 热词 | 3.2 | 4.13 | L1 |
| R15 模板/附件 | 3.4 | 4.14 | L1 |
| R16 智能打标 | 3.5 | 4.15 | L1 |
| R17 页面隐藏 | 3.6 | 4.16 | L0+L1 |
| R18 分享外链 | 3.7 | 4.17 | L1 |
| R20 防截图 | 4.3 | 4.18 | L1(+L2 可选) |

## 附录 C：本方案引用的 RAGFlow 源码位置速查

| 引用 | 位置 | 验证基线 |
|---|---|---|
| 路由自动注册（L2 机制依据） | `api/apps/__init__.py`（`search_pages_path`/`register_page`，`/api/v1` 前缀） | 0.26.4 已核实 |
| API Token 认证（服务账号依据） | `api/apps/__init__.py`（AUTH_API 分支，`APIToken` 表查询） | 0.26.4 已核实 |
| 知识库权限语义（原生边界） | `api/db/db_models.py:846`（`permission = "me|team"`） | 0.26.4 已核实 |
| 元数据过滤链路（R04/R05 依据） | `api/apps/restful_apis/openai_api.py`（`metadata_condition` → `meta_filter`）+ `common/metadata_utils.py:30` + `rag/nlp/search.py:549`（`Dealer.retrieval(doc_ids=)`）+ `sdk/python/ragflow_sdk/ragflow.py` | 0.26.4 已核实 |
| 文档上传/元数据 API | `api/apps/restful_apis/document_api.py`（upload/PATCH/metadata 系列接口） | 0.26.4 已核实 |
| 文件提交/版本 API | `api/apps/restful_apis/file_commit_api.py`（commits/diff/versions） | 0.26.4 已核实 |
| 解析器分发 | `rag/svr/task_executor.py`（FACTORY）+ `rag/app/*`（14 个 chunker）+ `deepdoc/parser/*`（含 docling/mineru 等适配器） | 0.26.4 已核实 |
| OIDC/OAuth 登录 | `api/apps/auth/oidc.py`、`oauth.py` | 0.26.4 已核实 |
| nginx 网关切换 | `docker/entrypoint.sh`（API_PROXY_SCHEME）、`docker/nginx/` | 0.26.4 已核实 |

## 附录 D：待评审确认事项清单

| # | 事项 | 影响 | 建议决策时点 |
|---|---|---|---|
| 1 | 企业统一认证方式（OIDC 对接参数） | 3.3 认证链路 | 任务 1.2 环境搭建期 |
| 2 | 组织架构接口规范与同步频率 | 4.3 | 任务 1.4（2026-09-28 前） |
| 3 | 防截图技术边界书面确认 | 4.18 | 方案评审会 |
| 4 | 字段级加密边界（磁盘级替代）确认 | 4.11 | 与安全部门，M2 前 |
| 5 | 密级分级标准（1-5 级定义与标签） | 4.1/4.18 | R01 开发前（2026-09 底） |
| 6 | 审批默认流程与审批人规则（业务口径） | 4.8 | R08 开发前（2027-03 前） |
| 7 | 知识空间初始树结构（业务分类口径） | 4.2 | R03 开发前（2026-10 底） |
| 8 | LLM 供应商与预算（打标/热词/评测用） | 4.15/4.10 | M3 前 |

---

**（全文完）**

本文档基于 RAGFlow 0.26.4 源码核实与《20260819-RAGFlow知识库二开工作计划》编制，作为二开项目的纲领性技术设计。后续《数据库设计文档》《接口规范文档》《组织架构对接方案》以本文档第 5、6 章与 4.3 节为基线展开。
