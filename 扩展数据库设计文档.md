# RAGFlow 二开扩展数据库设计文档（五大核心域）

| 项目 | 内容 |
|---|---|
| 文档编号 | RAGFLOW-EXT-DDD-001 |
| 文档版本 | V1.0（评审版） |
| 编制依据 | 《二开技术方案设计文档》RAGFLOW-EXT-HLD-001 V1.0 · 第 5 章「扩展数据库设计汇总」 |
| 代码基线 | RAGFlow 0.26.4（分支 my-dev） |
| 目标库 | MySQL 8.0+ / InnoDB / utf8mb4_0900_ai_ci，独立 schema **ragflow_ext** |
| 覆盖范围 | 域A 权限扩展(9) · 域B 知识空间(3) · 域C 审批流程(6) · 域D 搜索历史(3) · 域E 版本管理(2)，共 **22 张表** |
| 配套交付 | `ddl/ragflow_ext_schema.sql`（建表，610 行）、`ddl/ragflow_ext_seed.sql`（种子数据，幂等） |
| 验证状态 | ✅ 已在 docker-mysql-1（mysql:8.0.40）临时库完整执行 + 12 项功能冒烟测试通过（见 §13） |

**修订记录**

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| V1.0 | 2026-09-16 | 架构组 | 初稿：HLD §5 基线展开为全列/约束/索引/种子数据的可执行设计 |

---

## 目录

§1 设计范围与表清单 → §2 设计原则与公共约定 → §3 ER 总览 → §4 域A 权限扩展 → §5 域B 知识空间 → §6 域C 审批流程 → §7 域D 搜索历史 → §8 域E 版本管理 → §9 索引与关键查询对照 → §10 容量估算与保留策略 → §11 种子数据与演进策略 → §12 与 HLD 的差异/增强清单 → §13 验证记录

---

## 1. 设计范围与表清单

本文档将 HLD 第 5 章 5.2（表清单）/5.3（关键表 DDL 骨架）展开为**可直接评审与执行的完整设计**，覆盖五大核心域（对应 M2/M3 里程碑全部核心需求）。其余 6 域（解析路由、模板附件、打标、外链、水印、调优）遵循同一公共约定，另行批次交付。

> 域D 说明：搜索域的"用户偏好"表为 **ext_user_setting**（无痕检索开关等），因同时服务权限与搜索两域，编入域A 清单 A9；域D 实际新增 2 张 + 复用 1 张。

| # | 域 | 表名 | 承载需求 | 一句话职责 |
|---|---|---|---|---|
| A1 | 权限 | ext_user | R01/R04 | 用户↔组织架构映射（密级/部门/岗位） |
| A2 | 权限 | ext_department | R04 | 部门树（full_path 供检索过滤） |
| A3 | 权限 | ext_org_sync_log | R04 | 组织架构同步批次日志（降级判定） |
| A4 | 权限 | ext_role | R01/R02 | 自定义角色（功能权限点集合） |
| A5 | 权限 | ext_role_user | R01/R02 | 角色-用户绑定（可限空间范围） |
| A6 | 权限 | ext_permission_entry | R01/R02 | ACL 判定核心表（主体×资源×动作×效果） |
| A7 | 权限 | ext_route_permission | R17 | 角色-前端路由可见性（页面隐藏） |
| A8 | 权限 | ext_audit_log | R01/R20 | 审计日志（按月 RANGE 分区） |
| A9 | 权限 | ext_user_setting | R12 | 用户偏好（无痕检索开关，复用于域D） |
| B1 | 空间 | ext_space | R03 | 多层级知识空间树（物化路径） |
| B2 | 空间 | ext_space_type_config | R03 | 空间类型差异化配置 |
| B3 | 空间 | ext_space_dataset_map | R03 | 空间-Dataset 挂载/迁移事件流水 |
| C1 | 审批 | ext_approval_flow | R08/R19 | 审批流程定义（版本化） |
| C2 | 审批 | ext_approval_node | R08/R19 | 审批节点定义（规则/会签/超时/条件） |
| C3 | 审批 | ext_approval_instance | R08/R19 | 审批实例（先审后执状态机） |
| C4 | 审批 | ext_approval_task | R08/R19 | 审批任务（乐观锁/加签/转签） |
| C5 | 审批 | ext_approval_delegate | R19 | 审批委托设置 |
| C6 | 审批 | ext_notification | R08/R19 | 站内通知（待办/超时/结果/告警） |
| D1 | 搜索 | ext_search_history | R12 | 搜索历史（冷存储，90 天滚动） |
| D3 | 搜索 | ext_hotword | R13 | 热词榜单快照与运营词 |
| E1 | 版本 | ext_document_version | R09 | 文档版本快照（v{major}.{minor}） |
| E2 | 版本 | ext_version_chunk_diff | R09 | chunk 级版本差异 |

---

## 2. 设计原则与公共约定

### 2.1 命名与字符集

- 表名一律 `ext_` 前缀 + 业务实体名（蛇形），与 RAGFlow 主库 `rag_flow` 物理隔离，杜绝误迁移/误删；
- 字符集 utf8mb4 / utf8mb4_0900_ai_ci（MySQL 8 默认，大小写不敏利于字符串主体匹配）；
- 索引命名：唯一键 `uk_`、普通键 `idx_`；主键一律代理键 `id BIGINT UNSIGNED AUTO_INCREMENT`。

### 2.2 公共列规则（对 HLD 5.1 的细化，差异见 §12-2）

| 表类型 | 公共列 | 说明 |
|---|---|---|
| 实体表（17 张） | id / created_at / updated_at / created_by / updated_by | DATETIME(3) 毫秒精度；*_by 存 rag_flow.user.id |
| 事件表（B3/D1/E1/E2） | id / created_at + 业务主体列（user_id/operator） | 追加只写，updated_* 无意义故省略 |
| 特例 A8 ext_audit_log | occurred_at（分区键，入 PK）+ ragflow_user_id | 分区表结构要求 + 审计不可篡改语义 |

### 2.3 与 RAGFlow 主库的引用映射（不建跨库外键）

| ext 侧列 | 引用主库/组件 | 原生类型 | 校验方式 |
|---|---|---|---|
| *.ragflow_user_id / user_id / initiator / assignee / operator / delegator | rag_flow.user.id | CHAR(32) | 应用层校验 + 每日对账 |
| ext_space.dataset_id / A6.resource_id(dataset) / C3.target_dataset_id / E1.dataset_id | rag_flow.knowledgebase.id | CHAR(32) | 同上 |
| C3.biz_ref_id / C3.published_doc_id / E1.doc_id / E2.doc_id / A6.resource_id(document) | rag_flow.document.id | CHAR(32) | 同上 |
| A7.route_key | web/src/routes.tsx Routes 枚举路径 | — | 白名单（如 /files） |
| B2.default_parser_id | common/constants.py ParserType 枚举（15 值） | — | 白名单（如 naive） |
| C3.stash_file_key / E1.file_object_key | MinIO ext-approval-stash / ext-versions 桶 | — | 对象存在性校验 |

统一用 VARCHAR(64) 承载主库 CHAR(32) ID，为 ID 格式演化留余量。**库内亦不建外键**（沿用 RAGFlow 自身惯例，便于批量写入与 Alembic 演进），完整性由应用层 + 每日对账任务保证。

### 2.4 枚举与 JSON 约定

- 词表封闭且变更需感知的列用 ENUM（status/action/effect 等）；ENUM 追加值在 MySQL 8 为 INSTANT 级 ALTER，由 Alembic 管理；
- 可演化结构用 JSON（assignee_rule / meta_snapshot / window_stats / permissions 等）；MySQL 限制 JSON 列不能设字面量默认值，必填 JSON 由应用层校验。

### 2.5 其他

- 所有时间列 DATETIME(3)（毫秒），与服务时区一致；不用 TIMESTAMP（避开 2038 与时区转换歧义）；
- 不引入通用软删标记：归档/停用即业务状态（ext_space.status、ext_role.status、ext_user.status）；物理删除仅限事件表过期清理。

---

## 3. ER 总览

```
RAGFlow 主库 rag_flow（只读引用，不建跨库外键）
  user.id ─────────────► 各表主体列（ragflow_user_id/user_id/initiator/assignee/operator）
  knowledgebase.id ────► B1.dataset_id / C3.target_dataset_id / E1.dataset_id / A6.resource_id(dataset)
  document.id ─────────► C3.biz_ref_id,C3.published_doc_id / E1.doc_id / E2.doc_id / A6.resource_id(document)
  ParserType 枚举(15值) ► B2.default_parser_id（白名单）
  Routes 枚举(/files等) ► A7.route_key（白名单）

域A 权限（9）                              域B 空间（3）
  ext_user ──dept_code──► ext_department     ext_space ──self──► ext_space(parent_id)
  ext_role ──1:N──► ext_role_user            ext_space_type_config ──类型默认值──► ext_space
       │                └─scope_id──► ext_space    ext_space_dataset_map ──space_id──► ext_space
  ext_permission_entry ──subject──► user/role(A4)/dept(A2)/position
       └─resource──► ext_space(B1) / knowledgebase / document / global
  ext_route_permission ──role_id──► ext_role

域C 审批（6）                              域D 搜索（3）
  ext_approval_flow ──1:N──► ext_approval_node    ext_search_history（user_id──►user）
        └──1:N──► ext_approval_instance            ext_hotword（独立日快照）
                      ├──1:N──► ext_approval_task（assignee──►user，委托查 C5）  ext_user_setting=A9（复用）
                      ├─flow_version 固化──► C1.version
                      ├─stash_file_key──► MinIO ext-approval-stash
                      └─published_doc_id──► document.id
  ext_notification（user_id──►user，ref──►C3 实例）

域E 版本（2）
  ext_document_version ──doc_id/dataset_id──► document/knowledgebase
        ├─approval_instance_id──► C3
        └──1:N──► ext_version_chunk_diff ──from/to_version_id──► E1.id
```

---

## 4. 域A｜权限扩展（9 张表）

### 4.1 两级授权模型与判定算法

**功能权限点**（A4 ext_role.permissions，控制菜单/功能入口）× **资源 ACL**（A6，控制数据对象）。网关每次跨域 API 调用按下列顺序判定（结果缓存 Redis，见 4.3）：

```
allow(user, resource, action):
  1. 命中任意 deny 条目（任意层级、任意主体）            → 拒绝（deny 优先）
  2. 用户直接授权（subject_type=user；资源就近：
     document > dataset > space > global；同层并集）      → 允许
  3. 部门/岗位/角色继承授权（空间资源沿 ext_space.path
     自当前节点向根聚合；仅 inherit=1 条目参与）           → 允许
  4. super_admin 角色（或主库 is_superuser=1）            → 允许
  5. 默认拒绝；叠加 security_level 密级过滤（R02/R20）
  过期条目（expires_at < now(3)）一律不参与判定
  action 层级语义：manage ⊇ edit ⊇ read（判定时向上展开）
```

### 4.2 各表设计要点

| 表 | 关键设计 |
|---|---|
| A1 ext_user | ragflow_user_id / employee_no 双唯一键（分别对主库与 HR 幂等 upsert）；secondary_depts JSON 承载兼职部门；security_level 供检索代理密级过滤与水印（R20）联动 |
| A2 ext_department | full_path 编码全路径双重用途：① 写文档元数据 department 字段 ② 检索代理 metadata_condition 前缀匹配（本部门+下级展开，HLD 4.3.3）；merged_to 追踪部门合并；hr_updated_at 供对账 |
| A3 ext_org_sync_log | 批次粒度统计 JSON；degraded_events 记录降级链路（连续 3 周期失败 → 冻结部门变更操作并告警） |
| A4 ext_role | permissions JSON 权限点集合（`["*"]`=全部）；is_system 保护内置角色不可删 |
| A5 ext_role_user | uk(role,user,scope_type,scope_id) 允许同一用户同一角色"全局+多空间"并存绑定；expires_at 支持临时授权自动失效 |
| A6 ext_permission_entry | 判定核心：subject_type × resource_type × action × effect；resource_id='*' 表全局；inherit 仅对 space 资源有效；无唯一键（同语义条目可并存，判定取并集） |
| A7 ext_route_permission | uk(role_id,route_key)；**role_id=0 为全局默认规则**（未单独配置的角色全走默认）；route_key 取 Routes 枚举路径（如 /files）；前端菜单与网关双重过滤 |
| A8 ext_audit_log | 见 4.4 分区策略 |
| A9 ext_user_setting | incognito 无痕开关（R12）：检索代理写 D1 前查本表；settings JSON 承载未来偏好 |

### 4.3 缓存与失效联动

- 判定结果缓存 `authz:{user_id}`（TTL 10min）；A5/A6/A9 任何写操作 → 主动删除对应用户缓存键 → 网关下次回源；
- 部门/角色/空间变更影响一批用户 → 按 subject/resource 维度反查受影响用户列表批量清缓存；
- 兜底：缓存异常时默认拒绝并写 A8（source=gateway, result=denied）——权限系统 fail-closed。

### 4.4 审计分区策略（A8）

- 按月 RANGE 分区（TO_DAYS(occurred_at)），PK=(id, occurred_at) 满足分区键入主键约束；
- 初始预建 2026-09 ~ 2027-12 共 16 个分区 + pmax 兜底；**月度维护任务**：① 预建下月分区 ② 超保留期（默认 24 个月，配置项）分区先归档（导出 CSV→对象存储）再 DROP PARTITION；
- 有界时间查询精确裁剪（§13 V10：BETWEEN 查询仅扫 p202609 单分区）；
- 追加只写 + 异步落库（本地队列/Kafka），不阻塞业务主链路。

---

## 5. 域B｜知识空间层级（3 张表）

### 5.1 模型

空间树（B1）= 逻辑容器；Dataset（主库 knowledgebase）= 物理载体。`group` 纯分组不挂载体；`kb` 挂载 Dataset（**uk_dataset 保证一个 Dataset 至多挂载一个节点**，杜绝双重挂载歧义）；`space_group` 为部门空间模板（R03，按部门架构批量生成子树）。

### 5.2 关键设计

| 表 | 关键设计 |
|---|---|
| B1 ext_space | ① **path 物化路径**（/1/5/12）：子树查询免递归（`WHERE path LIKE '/1/5/%'`），节点移动时子树批量 UPDATE 前缀；② depth 冗余深度，配合 B2.max_depth 应用层限深；③ status=archived 归档：权限判定不可见、Dataset 保留可恢复，物理删除必须走审批流（域C）；④ doc_count 异步刷新冗余统计 |
| B2 ext_space_type_config | 类型级默认值（解析器/向量模型/权限模板/可否子节点/最大深度），B1.type_config 节点级覆盖；default_parser_id 受 ParserType 白名单校验（15 值） |
| B3 ext_space_dataset_map | 事件流水（mount/unmount/move/migrate/adopt）：存量迁移脚本与层级调整的审计依据；**当前挂载关系以 B1.dataset_id 为准**（流水不参与判定） |

### 5.3 与主库的分工

RAGFlow 原生无层级概念（knowledgebase 平铺）。ext 空间树只做**组织与授权**，不复制 Dataset 数据；原生 `knowledgebase.permission` 固定 "team"（兜底全拒绝），实际可见性完全由 ext ACL 判定后以服务账号代理访问（HLD 4.3.4 策略 C）。

---

## 6. 域C｜审批流程（6 张表）

### 6.1 状态机（C3.status）

```
            submit               approve(末节点)           执行器以服务账号调 RAGFlow /api/v1
 DRAFT ───────────► REVIEWING ───────────────► PUBLISHING ───────────────► PUBLISHED
  ▲                    │ reject / return                │ 失败：指数退避重试（1m/5m/30m，≤3 次）
  └──── 修改重报 ───────┘（实例回 DRAFT，任务行留痕审计）  └──► FAILED ──重试耗尽──► 转人工
  任一在途态（draft/reviewing/publishing/failed）── cancel/terminate ──► CANCELLED
```

- **先审后执**：文件先暂存 MinIO `ext-approval-stash` 桶（stash_file_key），审批通过才由异步执行器写入 RAGFlow，失败不产生半成品文档；
- **幂等**：执行前检查 published_doc_id（RAGFlow 侧同名文档已存在则直接认领该 id）；重试指数退避，retry_count>3 置 FAILED 并通知管理员（C6 type=failure）；
- 每日对账任务比对 PUBLISHED 实例与主库 document 双向差异，差异写 A8 审计。

### 6.2 流程匹配与版本固化

匹配优先级：dataset 精确 > space 精确（沿 path 自节点向根）> global 兜底（is_default=1）。**C1.version 定义版本化**：修改流程=新 version 发布；在途 C3 固化引用创建时 flow_version + C2 节点定义快照，定义迭代不影响存量实例。

### 6.3 节点执行语义（C2）

- assignee_rule JSON：`{"type":"space_admin|role|user|dept_leader|initiator_leader|initiator|system","value":...,"fallback":"role:super_admin"}`；解析为空 → fallback 兜底（宁升级到管理员不卡死，HLD 4.8.3③）；
- countersign：any 或签（任一 approve 即过）/ all 会签（全部 approve 才过，任一 reject 即驳回）；
- timeout_hours + timeout_action：remind 提醒 / escalate 升级 fallback / auto_approve 自动通过；
- condition_expr（R19 条件分支）：如 `security_level >= 4` 动态插入一级部门领导加签节点。

### 6.4 任务与并发控制（C4）+ 通知（C6）

- 任务粒度=审批人×节点；审批动作走乐观锁 `UPDATE ... WHERE id=? AND version=?`，冲突则重查重试（防两人同时处理同一任务）；
- 加签 add_sign 当前节点动态追加任务；转签 transferred_to；委托查 C5 生效期，任务生成时 assignee 直接取受托人（detail 记录来源）；
- C6 通知：todo 待办即时 / timeout 超 24h、48h 两档 / result 结果通知发起人 / failure 发布失败通知管理员；channel 预留 im（钉钉/飞书/企微）。

---

## 7. 域D｜搜索历史（D1/D3 + 复用 A9）

### 7.1 热/冷分层

| 层 | 载体 | 内容 | 生命周期 |
|---|---|---|---|
| 热路径 | Redis ZSet `hist:{user_id}` | 用户最近 50 条（score=时间戳），搜索框下拉直读 | 用户活跃期 |
| 冷存储 | D1 ext_search_history | 全量检索明细（审计/统计） | 90 天滚动清理（配置项） |

写入链路：检索代理 `/search/proxy` 返回结果后**异步落库**（不阻塞主链路）；A9.incognito=1 的用户跳过两层写入（R12 无痕检索）。

### 7.2 关键设计

- **D1**：query_norm（trim+lower）统一前端去重下拉与 R13 热词统计口径；entry 区分代理检索/下拉点击入口（热词点击率加权预留）；scope_snapshot JSON 记录检索范围快照（空间/部门过滤），支撑"同范围再看"；
- **D3**：每日快照（uk word+snapshot_date）留存榜单演进历史；window_stats 记录 1h/1d/3d/7d/30d 窗口频次；score = Σ窗口频次×衰减因子（1.0/0.6/0.4/0.25/0.1）÷ max_score 归一化到 0-1；**对外输出 = 统计榜 ∪ 运营词（source=manual / pinned 提权）− blocked 敏感词**；冷启动 source=corpus（文档标题/标签 TopN），日检索量 ≥200 自动切换 stats 模式。

---

## 8. 域E｜版本管理（2 张表）

### 8.1 版本号与快照策略

- 版本号 `v{major}.{minor}`：审批发布的内容更新/回滚 → major+1；元数据/解析配置调整 → minor+1；uk(doc_id, version_no)；
- **全量快照**：源文件存 MinIO `ext-versions` 桶 `version/{doc_id}/{version_no}`；meta_snapshot JSON 冻结元数据（名称/部门/标签/密级/parser 配置）；
- **增量快照**（大文档 >20MB 可选）：chunk_snapshot_key + E2 差异表（added/removed/modified 的 chunk 清单 + 内容哈希 + 预览截断）；
- **is_current 一致性**：生成列 `current_doc_key = IF(is_current=1, doc_id, NULL)` + uk —— 数据库层保证每 doc 至多一条 current（已实测 ERROR 1062 拦截，§13 V6）；新版本发布流程=旧 current 置 0 → 插入新 current；
- **保留策略**：最近 N=20 个版本 + 全部 is_major 重大版本永久保留（配合桶生命周期规则）；
- **回滚**：以历史版本源文件发起 update 审批 → 审批通过生成新版本（source=rollback），不物理删中间版本，完整时间线可审计；
- **分工边界**：仅 dataset 文档（rag_flow.document）；workspace 文件版本复用原生 file_commit API，不重复建设（HLD 4.9.4）。

### 8.2 联动关系

E1.approval_instance_id 反查来源审批单（含发起人/审批人/意见全链路）；C3.version_no 与 E1.version_no 双向关联；E2 以 (from_version_id, to_version_id, chunk_id) 唯一定位差异项，前端版本对比视图直接消费。

---

## 9. 索引与关键查询对照

| 查询场景 | 表·索引 | 说明 |
|---|---|---|
| 网关单点判定（拉用户全部相关 ACL） | A6·idx_subject | (subject_type,subject_id) |
| 资源反查（谁有权访问某 dataset） | A6·idx_resource | 管理端权限面板 |
| 空间子树（列表/统计/权限聚合） | B1·idx_path | LIKE 'prefix%' 前缀匹配 |
| 我的待办 | C4·idx_assignee_status | (assignee,status) |
| 超时任务扫描 | C4·idx_timeout_scan | (status,due_at) 定时任务 |
| 我发起的审批 | C3·idx_initiator + idx_status | 组合过滤 |
| 发布失败重试扫描 | C3·idx_publish_retry | (status,retry_count) |
| 审批单反查文档 | C3·idx_biz_ref | (biz_ref_type,biz_ref_id) |
| 搜索历史下拉（最近去重） | D1·idx_user_time | (user_id,created_at **DESC**) 降序索引 |
| 热词窗口统计 | D1·idx_norm_time | (query_norm,created_at) 范围聚合 |
| 审计按人/按时间检索 | A8·idx_user_time / idx_time | 分区裁剪 + 索引 |
| 文档版本时间线 | E1·idx_doc_order | (doc_id,major,minor) |
| 版本对比视图 | E2·uk_diff_chunk | (from,to,chunk_id) 直接定位 |

---

## 10. 容量估算与保留策略

测算基准：1000 活跃用户 / 10 万文档 / 人均日检索 20 次 / 人均日写操作 2 次。

| 表 | 日增（估） | 年规模（估） | 在线保留 | 清理机制 |
|---|---|---|---|---|
| ext_search_history | ~2 万行 | ~700 万行 / ~1.5GB | 90 天滚动 | 每日分批 DELETE |
| ext_audit_log | ~5 万事件 | ~1800 万行 / ~4GB | 24 个月（可配） | 分区归档 + DROP PARTITION |
| ext_permission_entry | 低频变更 | 万级稳态 | 永久（全量缓存） | — |
| ext_approval_instance / task | ~2000 行 | ~70 万行 | 永久（审计） | 可选：1 年后转冷表 |
| ext_document_version | ~500 行 | ~18 万行 | 最近 20 + 全部 major | 定时任务 + 桶生命周期 |
| ext_version_chunk_diff | 随增量策略 | 视开启率 | 随版本快照 | 同上 |
| ext_hotword | ~30 行 | ~1 万行 | 180 天快照 | 定时 DELETE |
| ext_space / role / user / department | 千~万级 | 小表 | 永久 | — |
| MinIO ext-versions / stash | — | 快照主体存储 | 同版本保留 | 对象生命周期规则 |

审批暂存桶 ext-approval-stash：PUBLISHED 后 7 天清理；CANCELLED/驳回即时清理。

---

## 11. 种子数据与演进策略

### 11.1 种子数据（ddl/ragflow_ext_seed.sql，ON DUPLICATE KEY 幂等可重跑）

| 类别 | 内容 |
|---|---|
| 内置角色 | super_admin（`["*"]`）、space_admin（空间管理全集）、knowledge_contributor（提交+编辑）、knowledge_viewer（只读）——前两条 is_system=1 不可删 |
| 默认审批流 | default_doc_upload / update / delete 三条 global 兜底流（is_default=1），各 3 节点：发起 → 空间管理员审核（upload/update 48h 或签、delete 72h 会签；超时 escalate 到 super_admin）→ 自动发布 |
| 空间类型 | group（知识分组）/ kb（知识库，默认解析器 naive）/ space_group（部门空间模板） |
| R17 规则 | `/files`（文件管理页，Routes.Files）全局默认隐藏（role_id=0 规则）；super_admin 单独可见 |

### 11.2 Alembic 演进规则

1. **基线迁移** = 本 DDL 等价结构（ragflow-ext-migrate 服务首启执行，`ragflow_ext_ddl_check` 已验证可执行性）；
2. 只加列不改列语义；删/改列两阶段（先停写 → 双写迁移 → 删旧列）；
3. ENUM 追加值 = INSTANT ALTER；JSON 结构演进不触发 DDL；
4. 分区维护（月度预建/归档/DROP）与过期清理走独立调度任务，不进 Alembic；
5. 种子数据由服务 bootstrap 幂等 upsert，与应用发版解耦。

---

## 12. 与 HLD 的差异/增强点（需评审确认）

| # | 位置 | HLD 基线 | 本设计 | 理由 |
|---|---|---|---|---|
| 1 | A8 | 公共审计列模板 | occurred_at 入 PK + ragflow_user_id 主体列，无 updated_* | 分区表结构要求 + 追加只写不可篡改 |
| 2 | 公共列 | 全表统一 5 列 | 事件表（B3/D1/E1/E2）省略 updated_*/created_by，业务主体列承担 | 消除无意义列（规则见 §2.2） |
| 3 | C3 | 幂等键=instance_id | 新增 published_doc_id 显式落列 | 重试幂等与每日对账免反查主库 |
| 4 | E1 | doc_id+version_no | 新增 major/minor/is_major/is_current + 生成列唯一约束 | 版本排序、保留策略、current 一致性 |
| 5 | A5 | 角色-用户绑定 | 新增 scope_type/scope_id 空间范围绑定 | 支撑 space_admin 限空间生效 |
| 6 | C1 | 流程定义表 | 新增 version + uk(code,version) | 定义迭代不影响在途实例 |
| 7 | D3 | rank 列 | 更名 rank_no | RANK 为 MySQL 8 保留字 |
| 8 | D1 | 基础四列 | 新增 entry 入口列 | 热词点击率加权与入口分析预留 |
| 9 | B1 | parent_id 树 | 新增 path/depth/doc_count | 物化路径免递归（权限聚合/子树列表） |
| 10 | A2 | 部门树 | 新增 merged_to/name_path/level/hr_updated_at | 部门合并追踪 + HR 对账 |
| 11 | C4 | 任务表 | 新增 version 乐观锁列 | 并发审批冲突控制 |
| 12 | 域D | 搜索域 3 表 | 第 2 张复用 A9 ext_user_setting | 无痕偏好跨权限/搜索两域 |

---

## 13. 验证记录

**环境**：docker-mysql-1（mysql:8.0.40），临时库 `ragflow_ext_ddl_check`（验证完成后已 DROP，全程未触碰主库 rag_flow）。

| # | 验证项 | 结果 |
|---|---|---|
| V1 | ragflow_ext_schema.sql 全量执行（22 表 + 审计表 17 个分区） | ✅ 0 错误 |
| V2 | ragflow_ext_seed.sql 执行 + 幂等重跑 | ✅ 角色 4 / 流程 3 / 节点 9 / 空间类型 3 / 路由规则 2，重跑后行数不变 |
| V3 | 表数量核对（information_schema） | ✅ 22/22 |
| V4 | 权限判定数据通路：Q1 部门继承 allow / Q2 用户直接 deny | ✅ |
| V5 | 空间子树物化路径查询 Q3（/1 与 /1/2 均命中） | ✅ |
| V6 | 版本 current 翻转 Q4（v1.0→0、v2.0→1）+ 违反唯一约束 T1（ERROR 1062 命中 uk_current_doc） | ✅ |
| V7 | 审批实例/任务生成 + 我的待办 Q5（due_at=+48h 正确） | ✅ |
| V8 | 搜索历史中文写入 + 热词快照 Q6（归一化 score/rank_no） | ✅ |
| V9 | 审计日志写入 + 查询 Q7 | ✅ |
| V10 | 分区裁剪 T2b：BETWEEN 有界区间 EXPLAIN 仅扫 p202609 单分区 | ✅ |
| V11 | 生成列 current_doc_key STORED + UNIQUE KEY uk_current_doc（SHOW CREATE TABLE） | ✅ |
| V12 | 降序索引 idx_user_time (user_id, created_at DESC)（SHOW CREATE TABLE） | ✅ |

**实现阶段遗留事项**：

1. Alembic 基线迁移脚本（本 DDL 的 SQLAlchemy 模型 + autogenerate 差异校验）；
2. 分区维护任务（月度预建 + 24 个月归档 DROP）与 90 天历史清理任务的调度接入；
3. 种子数据 bootstrap 幂等逻辑落到 ext 服务启动流程；
4. MinIO `ext-approval-stash` / `ext-versions` 桶创建与生命周期规则配置；
5. 权限判定/审批执行器的单元测试用例（本表结构已支撑 Q1-Q7 类判定查询）。

---

（全文完 · 配套文件：`/home/admin/ragflow/ddl/ragflow_ext_schema.sql`、`/home/admin/ragflow/ddl/ragflow_ext_seed.sql`）
