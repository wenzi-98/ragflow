# RAGFlow 二开前后端接口规范文档

| 项目 | 内容 |
|---|---|
| 文档编号 | RAGFLOW-EXT-API-001 |
| 文档版本 | V1.0（评审版） |
| 编制依据 | 《二开技术方案设计文档》RAGFLOW-EXT-HLD-001 V1.0（第 6 章及 4.x.5 各节）；《扩展数据库设计文档》RAGFLOW-EXT-DDD-001 V1.0 |
| 代码基线 | RAGFlow 0.26.4（分支 my-dev，commit c0582b8e1） |
| 适用范围 | ragflow-ext 扩展服务（FastAPI）· ext-web 扩展前端 · 网关（nginx auth_request）· 外链公开页 |
| 接口规模 | A 面 16 模块共 145 个接口操作（其中 B 面公开接口 3 个）；C 面下游调用白名单见附录 B |
| 配套交付 | FastAPI OpenAPI（`/api/ext/v1/docs`）· openapi.json 契约快照（Git 入库）· `app/ragflow_client/error_mapper.py` 错误映射 |

**修订记录**

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| V1.0 | 2026-09-17 | 架构组 | 初稿：将 HLD §6 基线展开为字段级可执行规范，含全量错误码与 16 模块接口清单 |

---

## 目录

§1 文档说明 → §2 通用约定 → §3 统一返回格式 → §4 错误码规范 → §5 RESTful 资源设计规范 → §6 安全规范 → §7 模块接口清单（16 模块）→ §8 典型接口字段级设计 → §9 前端接入规范 → §10 契约管理与变更控制 → 附录 A 错误码↔HTTP 速查 / B 原生 RetCode 映射 / C 通用枚举清单 / D 接口—需求—数据表对照

---

# 1. 文档说明

## 1.1 编制目的与约束力

本规范是《工作计划》任务 1.3 的核心交付物，约束 **ext-web 前端 ↔ ragflow-ext 扩展服务**之间全部 HTTP 接口的设计、实现与消费方式，并约束 ragflow-ext 对 RAGFlow 原生 `/api/v1` 的**调用侧**契约。约束力约定：

1. 前后端任何一方新增/修改接口，必须先更新本规范与 OpenAPI 契约（PR 评审）再编码；
2. 违反 §3（返回格式）、§4（错误码）、§6.1/§6.2（认证）任一条款的接口**不得进入联调与发版**；
3. 前端不得绕过 §9 统一请求层直接调用后端接口（SSE 亦须使用统一 SSE 封装）。

## 1.2 接口体系总览（三张接口面）

| 接口面 | 调用方 → 提供方 | 基础路径 | 认证方式 | 说明 |
|---|---|---|---|---|
| **A 面：扩展接口** | ext-web / 网关（auth_request）/ 管理后台 → ragflow-ext | `/api/ext/v1` | `Authorization: Bearer <ext-jwt>`；网关机器调用 `X-Ext-Internal-Token` | 本规范主体（§2~§9） |
| **B 面：公开外链** | 匿名访客 → ragflow-ext | `/s/{token}` | 无登录态；密码校验 + 15 分钟短时会话票据（IP+UA 指纹绑定） | §7.14、§6.5 |
| **C 面：下游依赖** | ragflow-ext → RAGFlow `/api/v1` | `/api/v1` | 服务账号 API Token（`svc_ext_gateway`） | 仅约束调用侧（§4.5 映射原则 + 附录 B 白名单），原生 schema 以官方源码/SDK 为准 |

> A 面中 `/authz`（网关判定）、`/healthz`（探活）、`/api/ext/v1/docs`（OpenAPI）为机器/运维子形态，认证规则见 §6.2/§6.7。

## 1.3 与其他文档的关系

| 文档 | 关系 |
|---|---|
| HLD（RAGFLOW-EXT-HLD-001） | 第 6 章为本规范基线；各 4.x.5 节的接口语义（业务规则、权限语义）由本规范 §7 汇总收口；两者冲突以本规范为准（评审通过后） |
| 扩展数据库设计文档（RAGFLOW-EXT-DDD-001） | §7/§8 的字段命名、枚举值与 `ddl/ragflow_ext_schema.sql` **逐字对齐**（差异即缺陷，见附录 C） |
| RAGFlow 原生源码 | `api/apps/restful_apis/` 与 `sdk/python/ragflow_sdk/` 为 C 面 schema 唯一权威；`common/constants.py: RetCode` 为错误映射输入 |
| 组织架构对接方案（任务 1.4） | 其接口规范引用本规范 §2/§3/§4 通用条款 |

---

# 2. 通用约定

## 2.1 协议与路径

| 项 | 约定 |
|---|---|
| 协议风格 | RESTful：GET 查询 / POST 创建及动作 / PATCH 部分更新 / PUT 全量更新 / DELETE 删除 |
| 基础路径 | `/api/ext/v1`（A 面）；`/s/{token}`（B 面）；C 面固定 `/api/v1` |
| 版本策略 | URL 版本。不兼容变更升 `/api/ext/v2`，v1 自 v2 发布起保留 ≥ 6 个月；兼容变更不升版本 |
| 路径命名 | 资源复数名词 + kebab-case（如 `/parser-route/rules`）；路径参数 `{id}` 为资源代理主键（字符串） |
| 动作型子资源 | 非 CRUD 的状态迁移用 `POST /{resource}/{id}/{action}`（如 `/archive`、`/move`、`/revoke`、`/rollback`、`/confirm`、`/decide`），action 为 kebab-case 动词 |
| 预检模式 | 支持 `?dry_run=true` 的写接口仅做校验不落库，返回差异预览（如空间移动的权限差异） |

## 2.2 内容类型与编码

| 场景 | Content-Type |
|---|---|
| 常规请求/响应 | `application/json; charset=utf-8` |
| 文件上传（暂存/模板骨架/样本/Excel 导入） | `multipart/form-data`，单文件上限 200MB（网关层限制），文件字段名 `file` |
| SSE 流式（检索代理等） | 响应 `text/event-stream; charset=utf-8`，请求仍为 JSON |
| 文件下载（版本源文件/模板产物/附件） | `application/octet-stream` + `Content-Disposition: attachment` |

## 2.3 请求头规范

| Header | 必填场景 | 说明 |
|---|---|---|
| `Authorization` | A 面全部接口（除 §6.7 白名单） | `Bearer <ext-jwt>`，见 §6.1 |
| `X-Ext-Internal-Token` | 网关机器调用（`/authz` 等） | HMAC 时戳签名，见 §6.2 |
| `X-Idempotency-Key` | 强制幂等接口（§7 清单标 ✚） | UUID v4；服务端 24h 去重窗口；缺失 → 10008 |
| `Accept-Language` | 可选 | 默认 `zh-CN`；错误 message 按其本地化 |
| `X-Trace-Id` | 可选 | 前端/网关传入则透传，否则服务端生成 ULID |
| `Last-Event-ID` | SSE 断线重连 | 续传游标，见 §5.8 |

## 2.4 参数传递与校验

| 项 | 约定 |
|---|---|
| 查询参数 | GET 的过滤/分页/排序全部走 query string；**禁止** GET 携带 body |
| 路径参数 | 资源 ID；不存在 → 40006（HTTP 404） |
| 请求体 | POST/PATCH/PUT 走 JSON body；字段 snake_case |
| 未识别字段 | 服务端**严格拒绝** → 10001（防拼写错误静默失效） |
| 空值语义 | PATCH 中 `field: null` 表示显式清空；未出现的字段不修改 |
| 校验失败响应 | 10001，`details.fields` 返回字段级错误数组（§8.7） |

## 2.5 分页、排序、过滤

| 项 | 约定 |
|---|---|
| 分页参数 | `?page=1&page_size=20`；page 从 1 起；page_size 默认 20、上限 200，越界 → 10005 |
| 分页响应 | `data` 固定为 `{items, total, page, page_size}`（§3.2）；树形/全量接口（如 `/spaces/tree`、`/routes/my`）不分页 |
| 排序 | `?order_by=created_at&order=desc`；order_by 仅允许接口文档声明的白名单列，越界 → 10006 |
| 时间过滤 | `?created_from=&created_to=`（ISO 8601，左闭右开区间 [from, to)） |
| 关键词 | `?keyword=` 模糊匹配（可匹配列在接口文档声明） |

## 2.6 数据类型与序列化

| 类型 | 表示 |
|---|---|
| 时间 | ISO 8601 含时区与毫秒：`2026-09-17T10:30:00.128+08:00`；纯日期 `2026-09-17` |
| ID | 一律字符串（ext 代理主键转字符串；RAGFlow `dataset_id`/`doc_id` 为 32 位 CHAR 原样透传） |
| 布尔 | `true`/`false`，禁止 0/1 |
| 枚举 | 小写蛇形字符串，与 DDL ENUM **逐字一致**（附录 C）；非法值 → 10007 |
| 空集合 | `[]`（禁止用 null 代替）；无值字段直接省略，不返回 null 占位 |
| 大整数 | 字符串（JS Number 精度保护） |
| 嵌套对象 | 原样 JSON；其结构变更属破坏性变更（§10.3） |

## 2.7 命名与前后端字段转换

- JSON 字段一律 **snake_case**；ext-web 请求层统一做 camelCase↔snake_case 双向转换（对齐 RAGFlow 原生前端 `web/src/utils/request.ts` 的 `convertTheKeysOfTheObjectToSnake` 机制，保证组件层习惯一致）；
- 业务字段命名与扩展库表列名保持一致（如 `space_type`、`is_current`、`rank_no`），降低映射成本；
- 布尔字段用 `is_`/`has_` 前缀；时间字段 `_at` 后缀；日期字段 `_date` 后缀。

## 2.8 幂等与重试

| 项 | 约定 |
|---|---|
| 强制幂等接口 | 审批发起/审批操作、空间创建/移动/迁移 commit、外链创建/编辑、同步触发、评测发起等（§7 清单标 ✚）；缺失头 → 10008 |
| 幂等实现 | Redis `SETNX key: sha256(body)` TTL 24h；同键同体 → 重放首次响应（响应头 `X-Idempotent-Replay: true`）；同键异体 → 10009（409） |
| 客户端重试 | 仅对 GET 与 429/502/503/504 做指数退避重试（≤3 次，上限间隔 30s）；写操作重试必须携带**原**幂等键 |
| 超时 | 网关 60s（SSE 路径除外）；ext→RAGFlow 连接 3s / 读 30s（长操作异步化，§5.5） |

## 2.9 限流

- B 面匿名：5 req/min/IP（密码校验更严，见 §6.5）；
- A 面用户级：120 req/min；管理端导出类：5 req/min；
- 触发 → 90006 + HTTP 429 + `Retry-After` 头（秒数）。

---

# 3. 统一返回格式

## 3.1 响应信封（Envelope）

除 §3.4 列举的特殊形态外，**所有 A/B 面接口**返回统一信封：

```json
{
  "code": 0,
  "message": "success",
  "data": { },
  "trace_id": "01JB2X8K9G2QF7W3D5R8T6Y0ZC"
}
```

| 字段 | 类型 | 必现 | 说明 |
|---|---|---|---|
| code | int | 是 | 0 成功；非 0 为错误码（§4） |
| message | string | 是 | 成功固定 `success`；失败为**用户可读**的中文描述（不暴露内部细节） |
| data | any | 是 | 业务数据；失败时为 `null`；无业务数据的成功响应（如 DELETE）可为 `null` |
| trace_id | string | 是 | 26 位 ULID，贯穿 网关→ext→下游 日志链路，报障必填 |
| details | object | 仅失败 | 错误上下文，结构由错误码定义（§8.7）；成功响应不出现该字段 |

> **兼容性说明**：该信封与 RAGFlow 原生 `{code, message, data}` 同构（原生无 trace_id、data 无强制 null 语义），前端沿用原生 `code === 0` 判定成功、`code !== 0` 弹错的语义（见 `web/src/utils/request.ts`），组件层可复用。

## 3.2 分页数据体

分页接口 `data` 固定结构：

```json
{
  "code": 0,
  "message": "success",
  "data": { "items": [ ], "total": 128, "page": 1, "page_size": 20 },
  "trace_id": "01JB2X..."
}
```

`total` 为过滤后精确总数；接口若标注"游标型"则改用 `{items, next_cursor}` 并在 OpenAPI 中声明。

## 3.3 空数据与空集

- 列表无数据：`code=0`、`data.items=[]`，**禁止**返回 4xx；
- 详情不存在：40006 + HTTP 404；
- DELETE 成功：`code=0, data=null`、HTTP 200（统一信封，不强制 204）。

## 3.4 特殊形态（豁免信封，白名单制）

仅以下四类接口豁免 JSON 信封，**新增豁免须架构评审**：

| # | 形态 | 涉及接口 | 形式 |
|---|---|---|---|
| S1 | 网关判定 | `POST /authz` | 成功 HTTP **204 无响应体**；失败 HTTP 403 + 信封（nginx `auth_request` 协议要求） |
| S2 | SSE 流 | `/search/proxy` 等 | `text/event-stream`，帧协议见 §5.8（帧内仍为信封结构） |
| S3 | 文件流 | 版本源文件下载、模板渲染产物、附件下载/预览、内容代理 | `application/octet-stream`；**出错时降级返回 JSON 信封**（按 `Accept` 协商） |
| S4 | 异步任务受理 | 解析评测、打标、迁移 commit、元数据补刷、Excel 导入等 | HTTP **202** + 信封，`data.task_id` + `data.status="queued"`；统一 `GET /tasks/{task_id}` 轮询（§5.5） |

---

# 4. 错误码规范

## 4.1 设计原则

1. **分段继承 HLD**：5 位分段与 HLD §6.3 完全一致（0 / 1xxxx / 2xxxx / 3xxxx / 4xxxx / 5xxxx / 9xxxx），HLD 既有示例码（10001、20001、20002、30001、30002、40001~40004、50001、50002、90001）语义保持不变；
2. **模块子段**：4xxxx 内按百位细分业务子段（4001x 审批、4002x 空间……见下），错误码本身即问题定位线索；
3. **一码一义**：每个码只有一个语义、一个默认 HTTP 状态；实现层以常量枚举维护（`app/common/error_codes.py`），CI 校验唯一性与"码-状态"绑定（§10.5）；
4. **宁可新增不复用**：语义变化时新增码并废弃旧码（旧码保留 ≥ 6 个月，OpenAPI 标注 `deprecated`）；
5. **message 面向用户，details 面向程序**：message 为可展示的中文短句，不含堆栈/SQL/内部路径；机器可判定的上下文全部进 `details`。

## 4.2 分段总表

| 段 | 含义 | 默认 HTTP | 责任方 |
|---|---|---|---|
| 0 | 成功 | 200 / 201 / 202 | — |
| 1xxxx | 参数与请求错误（客户端） | 400 | 前端/调用方 |
| 2xxxx | 认证与会话 | 401（20004/20008 为 403） | 前端重登录/续期 |
| 3xxxx | 权限（权限中心判定） | 403 | 管理员授权 |
| 4xxxx | 业务规则 | 422（特例 404/409/429，见 §4.4） | 用户按提示修正 |
| 5xxxx | 下游依赖故障 | 502 / 503 / 504（50006 为 429） | 运维/平台 |
| 9xxxx | 系统内部错误 | 500（90006 为 429、90007 为 503） | 研发 |

## 4.3 全量错误码明细

### 4.3.1 1xxxx 参数与请求错误（默认 HTTP 400）

| 码 | 名称 | 说明 / details 结构 |
|---|---|---|
| 10001 | 参数校验失败 | `details.fields: [{field, rule, message}]`；含未知字段（严格模式）；枚举非法的通用兜底（专项用 10007） |
| 10002 | 请求体 JSON 解析失败 | — |
| 10003 | Content-Type 不支持 | `details: {expected, actual}` |
| 10004 | 请求体过大 | JSON > 10MB；`details: {limit}` |
| 10005 | 分页参数越界 | page_size > 200 或 page 异常 |
| 10006 | 排序字段不在白名单 | `details: {field, allowed[]}` |
| 10007 | 枚举值非法 | `details: {field, value, allowed[]}`（allowed 与附录 C 一致） |
| 10008 | 幂等键缺失 | 强制幂等接口未携带 `X-Idempotency-Key` |
| 10009 | 幂等键冲突 | 同键不同体；HTTP **409** |

### 4.3.2 2xxxx 认证与会话

| 码 | 名称 | HTTP | 说明 |
|---|---|---|---|
| 20001 | Token 无效 | 401 | 签名/格式错误；前端清态并重定向登录 |
| 20002 | Token 过期 | 401 | 前端静默 refresh 后重试一次（§9.2） |
| 20003 | 未携带认证凭据 | 401 | — |
| 20004 | 账号已禁用或锁定 | 403 | `details: {reason}` |
| 20005 | 内部调用凭据无效 | 401 | `X-Ext-Internal-Token` 校验失败（签名错误/时间戳超窗/重放） |
| 20006 | 登录凭证错误 | 401 | 登录接口专用；连续失败计数触发限速（§6.5） |
| 20007 | 外链会话票据无效或过期 | 401 | B 面；需重新走 `/verify` 换票 |
| 20008 | 票据指纹不匹配 | 403 | IP/UA 与签发票据不一致（疑似票据盗用，记审计） |

### 4.3.3 3xxxx 权限

| 码 | 名称 | HTTP | 说明 / details |
|---|---|---|---|
| 30001 | ACL 判定拒绝 | 403 | 通用无权限；`details: {resource, action, subject}` |
| 30002 | 安全密级不足 | 403 | `details: {required_level, user_level}`（密级标准口径待 HLD 遗留项确认后冻结） |
| 30003 | 需审批前置 | 403 | 高密级分享/删除等需先走审批流程；`details: {required_flow}` |
| 30004 | 知识空间权限不足 | 403 | 空间级动作（创建/移动/归档/迁移）被拒 |
| 30005 | 数据越权 | 403 | 访问非本人可见资源 ID（IDOR 防护，同时记审计） |
| 30006 | 管理权限点缺失 | 403 | `manage:xxx` 权限点未授予；`details: {permission}` |
| 30007 | 路由规则拒绝 | 403 | R17 `ext_route_permission` deny 规则命中（网关/入口层） |

---

### 4.3.4 4xxxx 业务规则（默认 HTTP 422，特例见 §4.4）

**4000x 通用业务**

| 码 | 名称 | HTTP | 说明 |
|---|---|---|---|
| 40005 | 资源已存在 | 409 | 唯一约束冲突（专属码优先，如 40021） |
| 40006 | 资源不存在 | 404 | — |
| 40007 | 当前状态不允许该操作 | 409 | 通用状态前置（状态机错误优先用子段码） |
| 40008 | 并发冲突（乐观锁） | 409 | `details: {expected_version, current_version}` |
| 40009 | 前置条件不满足 | 422 | 如组织架构同步运行中重复触发、迁移窗口未就绪 |
| 40010 | 资源被引用不可删除 | 409 | 如模板被实例引用、系统内置角色删除 |

**4001x 审批中心（R08/R19）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40001 | 审批状态机非法迁移 | `details: {from, to, allowed[]}`（如 published→reviewing） |
| 40011 | 流程定义被在途实例引用不可修改 | 需停用后新建版本 |
| 40012 | 审批任务已被处理 | `details: {processed_by, processed_at}`（含被并发处理） |
| 40013 | 加签/转签目标用户无效 | 目标无审批权限或已不在流程内 |
| 40014 | 委托设置时间重叠 | 同流程/全类型委托区间交叠 |
| 40015 | 撤回条件不满足 | 实例已进入 publishing/published 阶段 |

**4002x 知识空间（R03）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40002 | 空间层级成环 | 移动目标位于自身子树内 |
| 40021 | Dataset 重复挂载 | 同一 dataset 已挂载至其他节点 |
| 40022 | 归档节点下不允许操作 | 归档节点禁止创建/移入子节点 |
| 40023 | 仅空 group 节点可删除 | `details: {doc_count, sub_space_count}` |
| 40024 | 移动预检未通过 | dry_run 已提示差异，实跑时仍存在冲突 |
| 40025 | 迁移映射冲突 | 同一 Dataset 被映射到多个目标节点 |

**4003x 分享外链（R18）**

| 码 | 名称 | HTTP | 说明 |
|---|---|---|---|
| 40003 | 外链已过期 | 404 | B 面对过期/不存在统一表现，避免探测 |
| 40031 | 外链已撤销 | 404 | 同上 |
| 40032 | 超出访问次数上限 | 404 | 次数耗尽 |
| 40033 | 密码错误 | 422 | `details: {remaining_attempts}` |
| 40034 | 密码尝试次数超限 | **429** | 5 次/10 分钟，链接临时锁 10 分钟 |
| 40035 | 高密级文档禁止外链 | 403 | 路由级配置，审批不可豁免 |

**4004x 搜索域（R12/R13）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40004 | 热词被屏蔽 | 点击被屏蔽热词时返回，前端引导检索原词 |
| 40041 | 置顶词数量达上限 | 默认上限 20 个运营置顶词 |

**4005x 版本管理（R09）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40051 | 版本号不存在 | `details: {version_no}` |
| 40052 | 回滚进行中禁止再次回滚 | 同文档已有在途回滚任务 |
| 40053 | diff 两版本相同 | from == to |
| 40054 | 版本受保留策略保护 | major 版本锁定不可清理 |

**4006x 解析路由与解析评测（R06/R07/R14）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40061 | 规则条件表达式非法 | `details: {expression, reason}` |
| 40062 | 规则优先级冲突 | 优先级区间/顺序冲突 |
| 40063 | 评测任务运行中不可重复发起 | 同样本集已有在途评测 |
| 40064 | 样本格式不支持 | 上传样本无法解析 |

**4007x 模板与附件（R15）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40071 | 模板占位符解析失败 | `details: {invalid_placeholders[]}` |
| 40072 | 渲染字段值校验失败 | `details.fields`（同 10001 结构） |
| 40073 | 附件超出大小限制 | 预览 20MB / 直传上传 50MB 上限 |
| 40074 | 附件关联对象类型或 ID 无效 | link_type/link_id 校验失败 |

**4008x 智能打标（R16）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40081 | 标签树层级超限 | 默认 3 级 |
| 40082 | 标签被引用不可删除 | 被文档/规则引用（运营提示用专属码） |
| 40083 | 打标任务状态不允许确认 | 已完成/已驳回的批次 |
| 40084 | 词表外新词需审核 | `details: {word}`，引导走新词审核队列 |

**4009x 评测调优（R10）**

| 码 | 名称 | 说明 |
|---|---|---|
| 40091 | 评测集为空或用例不足 | 最少 3 条有效用例 |
| 40092 | 实验参数组合非法 | 网格外取值 |
| 40093 | 评测运行中不可变更评测集 | 在途实验占用 |

---

### 4.3.5 5xxxx 下游依赖故障

| 码 | 名称 | HTTP | 说明 / details |
|---|---|---|---|
| 50001 | RAGFlow API 调用失败 | 502 | `details: {upstream_code, upstream_message(脱敏截断 200 字), endpoint, elapsed_ms}`；映射规则见 §4.5 |
| 50002 | 组织架构同步降级中 | 503 | 沿用最近快照；`details: {last_success_at, consecutive_failures}` |
| 50003 | 对象存储（MinIO）访问失败 | 502 | 暂存/版本/附件桶读写失败 |
| 50004 | 缓存（Redis）访问失败 | 503 | 可降级直读 DB，触发 P1 告警 |
| 50005 | LLM 服务调用失败 | 502 | 打标抽取/评测问答；`details: {provider, model}` |
| 50006 | 下游限流 | 429 | 上游 429 透传；附 `Retry-After` |
| 50007 | 下游超时 | 504 | 超出连接 3s / 读 30s 预算（长操作应异步化） |

### 4.3.6 9xxxx 系统内部错误

| 码 | 名称 | HTTP | 说明 |
|---|---|---|---|
| 90001 | 数据库异常 | 500 | — |
| 90002 | 未捕获系统异常 | 500 | `details` 仅含 `{hint}`；堆栈凭 trace_id 查日志 |
| 90003 | 服务配置缺失或非法 | 500 | 启动期校验为主，运行期兜底 |
| 90004 | 任务队列投递失败 | 500 | 异步任务受理后入队失败（自动补偿重试 3 次） |
| 90006 | 请求频率超限 | 429 | 本服务限流（§2.9）；`Retry-After` |
| 90007 | 服务熔断/维护中 | 503 | 熔断器打开或发版窗口 |

## 4.4 HTTP 状态码映射总则

**HTTP 状态承载"传输层语义"（网关/监控/重试策略依赖），code 承载"业务语义"（前端分支依赖）；两者必须同时设置且一致（完整速查见附录 A）。**

| 段/码 | 默认 HTTP | 例外（更高优先级） |
|---|---|---|
| 0（成功） | 200 | POST 创建 → 201；异步受理 → 202；`/authz` 放行 → 204 |
| 1xxxx | 400 | 10009 → 409 |
| 2xxxx | 401 | 20004 / 20008 → 403 |
| 3xxxx | 403 | — |
| 4xxxx | 422 | 40005 / 40007 / 40008 / 40010 / 40012 / 40014 → 409；40006 / 40051 → 404；40034 → 429；40035 → 403 |
| 5xxxx | 502 | 50002 / 50004 → 503；50006 → 429；50007 → 504 |
| 9xxxx | 500 | 90006 → 429；90007 → 503 |

设计依据（已评审决策）：4xxxx 表达"业务规则不满足"，选 422（语义化可处理实体）而非笼统 400；资源冲突类（已存在/状态/并发/被引用）升 409 便于前端做刷新重试引导；不存在类降 404 与 REST 惯例及网关 404 页面联动。

## 4.5 与 RAGFlow 原生 RetCode 的映射（C 面）

映射实现于 `app/ragflow_client/error_mapper.py`，附录 B 为评审基线表。决策规则：

1. **默认规则**：ext 以服务账号调用 `/api/v1` 返回 `code ≠ 0` → 一律映射 **50001（HTTP 502）**，原生 code/message 透传至 `details`（脱敏截断）。**禁止**将上游 108（PERMISSION_ERROR）/109（AUTHENTICATION_ERROR）映射为用户侧 30001/20001——调用主体是服务账号 `svc_ext_gateway`，终端用户已在 ext 完成鉴权，此类失败属**系统级故障**而非用户权限问题；
2. **告警升级**：上游 108/109 → 50001 + **P0 告警**（服务账号被锁/令牌失效影响全部功能），触发令牌热加载并自动重试一次；上游 101/102 → P1 告警（多为 ext 请求构造缺陷，需研发介入）；
3. **上游 404 特判**：ext 先查本地资源映射表——资源应存在 → 50001 + P1 告警（疑似主库数据丢失或越权访问，记审计）；资源不应存在 → 映射 40006（404）；
4. **106（RUNNING）非错误**：任务运行中语义映射 `code=0`，run 状态透传至 data（前端继续轮询）；
5. **上游 HTTP 429** → 50006（429）；上游 5xx → 50001；超时/连接失败 → 50007/50001；
6. **契约测试**：映射表纳入 CI——RAGFlow 官方新增 RetCode 时单测强制提醒补充映射（§10.5）。

## 4.6 前端错误处理矩阵

| code / 条件 | 触发层 | 前端统一行为 |
|---|---|---|
| 0 | 拦截器 | 返回 `data`；空态由组件渲染 |
| 20002 | 拦截器 | 静默 refresh → 重试原请求一次；再失败 → 登出重定向 |
| 20001 / 20003 | 拦截器 | 清除本地凭证 → 重定向登录（并发 401 防抖合并） |
| 20004 | 拦截器 | 提示"账号已禁用" → 登出 |
| 10001 | 拦截器→表单层 | `details.fields` 映射到表单项错误（不弹全局错误） |
| 3xxxx（30005 除外） | 拦截器 | `message.error(message)`；不重定向 |
| 30005 | 拦截器 | 静默 + 上报前端监控（疑似越权探测） |
| 40034 / 50006 / 90006 | 拦截器 | 按 `Retry-After` 指数退避自动重试（≤2 次），仍失败提示"请求过于频繁，请稍后再试" |
| 50002 | 业务组件 | 降级 UI：提示"组织架构数据维护中，展示最近快照"（部门树走本地缓存） |
| 5xxxx 其余 | 拦截器 | `notification.error`（message + trace_id 可一键复制，便于报障） |
| 9xxxx | 拦截器 | 同上 + 上报前端监控 |
| SSE error 帧 | SSE 封装 | 按同矩阵处理；流中断自动重连（`Last-Event-ID`，§5.8） |

---

# 5. RESTful 资源设计规范

## 5.1 资源建模

| 规则 | 正例 | 反例 |
|---|---|---|
| 资源复数名词，路径层级 ≤ 2 层 | `/spaces/{id}/datasets` | `/space/getDatasetList` |
| 动作不进 URL（状态迁移 action 除外，§2.1） | `POST /shares/{id}/revoke` | `/revokeShare?id=` |
| 集合与实例成对 | `GET /roles`、`GET /roles/{id}` | `GET /roleDetail` |
| 强从属资源嵌套；弱关联参数化 | 审批任务→实例：`/approvals/tasks/{id}`；附件↔文档：`?link_type=document&link_id=` | `/docs/{id}/atts/{aid}/cats/{cid}` |

## 5.2 方法语义与成功码

| 方法 | 语义 | 成功码 | 幂等性 |
|---|---|---|---|
| GET | 读取（无副作用） | 200 | 天然 |
| POST | 创建 / 状态迁移动作 / 搜索类（有副作用但不落库） | 201 / 200 / 202 | 需幂等键 ✚（创建与动作类） |
| PATCH | 部分更新（仅传变更字段） | 200 | 否（带 version 乐观锁，冲突 → 40008） |
| PUT | 全量替换 / 精确设置 | 200 | 是 |
| DELETE | 删除（业务上多为软删/状态置位） | 200 | 是 |

## 5.3 状态迁移接口约定

- 形式：`POST /{resource}/{id}/{action}`，action ∈ {submit, cancel, approve, reject, return, archive, restore, move, revoke, rollback, confirm, transfer, add-sign, merge, render}；
- 非法迁移 → 4xxxx 状态机码（如审批 40001，`details` 带 `from/to/allowed[]`）；
- 并发敏感迁移携带 `version` 字段做乐观锁，冲突 → 40008（409）；
- 审批动作类接口 `comment`（意见）必填校验在 10001 层面拦截。

## 5.4 批量操作

| 项 | 约定 |
|---|---|
| 路径 | `POST /{resource}/batch-{action}`（如 `/permissions/batch-delete`）或集合型绑定接口（如 `POST /roles/{id}/users`） |
| 上限 | 单次 ≤ 100 项，超限 → 10001 |
| 响应 | 部分成功语义：`data: {succeeded: [], failed: [{id, code, message}]}`，整体 HTTP 200 + code=0 |
| 原生批量透传 | 元数据批量改写分批 100/次（对齐 HLD R16 设计），失败批次入重试队列并计入 failed |

## 5.5 异步任务模式（S4）

长耗时操作（解析评测、打标、迁移 commit、模板渲染大文件、Excel 导入、元数据补刷）统一异步化：

1. **受理**：`POST ...` → HTTP 202 + 信封，`data: {task_id, status: "queued"}`；
2. **查询**：统一入口 `GET /api/ext/v1/tasks/{task_id}` →

```json
{
  "data": {
    "task_id": "task_01H...",
    "type": "tagging",
    "status": "running",
    "progress": { "done": 42, "total": 100 },
    "result": null,
    "error": null,
    "created_at": "2026-09-17T10:00:00.000+08:00",
    "finished_at": null
  }
}
```

   - `status` ∈ {queued, running, succeeded, failed, cancelled}（附录 C）；失败时 `error: {code, message}`；
3. **取消**：`POST /tasks/{task_id}/cancel`（仅 queued/running 可取消，否则 40007）；
4. 进度推送类（迁移、审批执行进度）可用 SSE（§5.8）；任务记录保留 7 天。

## 5.6 幂等键规范

- 强制幂等接口（§7 标 ✚）缺失 `X-Idempotency-Key` → 10008；
- 同键同体 → 重放首次响应 + 响应头 `X-Idempotent-Replay: true`；同键异体 → 10009（409）；
- 服务端实现：Redis `SETNX idem:{key} sha256(body)` TTL 24h（§2.8）。

## 5.7 文件上传与统一暂存

所有"先暂存、后引用"场景（审批附件、解析探测样本、模板骨架、外链内容文件）统一走暂存接口，业务接口仅传 `stash_key`：

1. `POST /api/ext/v1/uploads/stash`（multipart，`file` 字段）→ 201 + `data: {stash_key, file_name, size, checksum_sha256, expire_at}`；
2. stash 有效期 24h、单用户并发 20 个，超限 → 40009/10001；
3. 存储桶：`ext-approval-stash`（审批发布后 7 天清理）/ `ext-attachments`（R15 正式附件）；
4. 上传经网关大小限制（200MB），ext 侧校验扩展名/魔数白名单。

## 5.8 SSE 流式规范（S2）

沿用 RAGFlow 原生帧协议习惯，保证前端组件复用：

| 项 | 规范 |
|---|---|
| 帧格式 | `data: {json}\n\n`，json 为帧级信封 `{code, message?, data, seq}` |
| 事件类型 | 默认 `message`（增量内容）；`event: progress`（进度）；`event: error`（错误帧后必须关流）；`event: done`（收尾帧，含引用/统计） |
| 心跳 | 每 15s 注释帧 `: ping\n\n`（防网关空闲断连） |
| 序号 | 首帧 `{stage:"accepted", seq:0}`，seq 单调递增贯穿整个流 |
| 断线重连 | 客户端带 `Last-Event-ID`（最后已收 seq）重连，服务端从游标续传或重发幂等摘要 |
| 超时 | 服务端 5 分钟无新帧主动 done 并关闭；网关对该路径关闭缓冲（`X-Accel-Buffering: no`） |
| 错误 | 中途错误发 error 帧（信封结构，含 code/trace_id）后关流，禁止静默截断 |

---

# 6. 安全规范

## 6.1 用户认证（ext JWT）

| 项 | 规范 |
|---|---|
| 签发 | `POST /auth/login` 校验通过（企业统一认证优先，无则代办校验 RAGFlow 账号）后签发 JWT（RS256，公钥经 JWKS 暴露给网关与 ext-web） |
| 载荷 | `{sub: ragflow_user_id, emp_no, dept_code, roles, security_clearance, exp, iat, jti}`；不含敏感明文 |
| 有效期 | access 30min；refresh 12h（旋转式：refresh 一次性使用，检测到重放即吊销整个 refresh 家族） |
| 续期 | 20002（401）→ 前端静默 refresh → 重试原请求一次（§9.2） |
| 吊销 | logout/风控触发：Redis 黑名单 jti，TTL = 剩余有效期 |
| 传递 | 仅 `Authorization: Bearer`；**禁止** URL query 携带 token |

## 6.2 网关机器调用（X-Ext-Internal-Token）

- nginx `auth_request` 子请求与网关代理调用携带 `X-Ext-Internal-Token`：`HMAC-SHA256(secret, ts)` + `X-Ext-Timestamp`，±60s 时间窗防重放；
- 校验失败 → 20005（401）；
- 该通道仅允许网关内网 IP 白名单访问。

## 6.3 授权执行点与接口语义

| 执行点 | 接口语义 |
|---|---|
| E1 网关 | `POST /authz`：`{user_id, method, path}` → 204 放行 / 403（30001，details 含 resource/action）；判定结果网关侧缓存 60s |
| E2 检索 | `/search/proxy` 内部先行调用 visible-datasets + 部门/密级过滤，**从不**透传越权内容 |
| E3 业务 | 各模块服务层 `permission_service.check()`，拒绝 → 30001/30002/30004/30005/30006 |
| E4 兜底 | RAGFlow 原生 owner/team 语义不变；ext 权限只收紧、不放大 |

## 6.4 越权防护（IDOR）

- 所有以 ID 为入参的读接口必须做**属主/可见性二次校验**（30005）；
- 列表接口按可见范围过滤，禁止"先查全量再前端过滤"；
- 枚举防护：资源不存在（40006）与无权限（30001）的响应差异最小化（均 4xx，message 不泄露资源属主信息）。

## 6.5 B 面公开外链安全

- 密码校验 `POST /s/{token}/verify`：bcrypt 哈希比对；**5 次/10 分钟/IP** 限速，超限 40034（429）+ 链接临时锁 10 分钟；
- 票据：校验通过签发 15 分钟会话票据（绑定 IP+UA 指纹）；票据失效 → 20007（401）；指纹不匹配 → 20008（403，记审计）；
- 内容输出永不暴露 MinIO 原始签名 URL，一律经水印代理（R20）输出；
- 登录接口防爆破：同账号 5 次失败锁 15 分钟（20006 计数）。

## 6.6 审计事件必记清单

以下事件必须写 `ext_audit_log`（成功与失败都记）：登录/登出、E1 鉴权拒绝（采样汇总）、ACL/角色变更、空间层级变更（move/archive/restore/delete）、审批操作（approve/reject/return/add-sign/transfer/cancel）、发布执行结果、版本回滚、外链创建/编辑/撤销/访问（含 IP/UA/结果）、水印策略变更、水印移除尝试、批量导出。审计查询接口需 `manage:audit` 权限（§7.2）。

## 6.7 运维端点

- `GET /healthz`：免认证（网关内网限流），返回 `{status, version, deps: {mysql, redis, minio, ragflow}}`；外网由网关屏蔽；
- `/api/ext/v1/docs`（Swagger UI）：仅 dev/test 开放；pre/prod 关闭 UI、保留 `openapi.json` 供契约同步（内网访问）。

## 6.8 传输与脱敏

- 全链路 HTTPS（网关终结 TLS 1.3，R11）；
- 错误 message 脱敏：不含 SQL/堆栈/内部主机名；`details` 字段白名单化——50001 的 `upstream_message` 截断 200 字符并过滤敏感标记；
- 日志打印请求体时排除 `password`、`password_hash`、票据类字段；
- 响应头安全基线：`X-Content-Type-Options: nosniff`、`Cache-Control: no-store`（鉴权/内容代理类接口）。

---

# 7. 模块接口清单

> 本章为 A/B 面全量接口清单（字段级定义以 OpenAPI 为准，典型接口字段级设计见 §8）。表格约定：
> - **权限**：`公开`＝免认证（B 面/登录/探活）；`登录`＝任何有效 JWT；`属主`＝资源发起人/相关人；`manage:xxx`＝需对应管理权限点（30006 兜底）；`网关`＝X-Ext-Internal-Token 机器通道；
> - **✚**＝强制幂等（`X-Idempotency-Key`）；**S2/S3/S4**＝§3.4 特殊形态；**HLD**＝对应《二开技术方案设计文档》章节。

## 7.1 模块 M01：认证与会话（HLD §4.1.5）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | POST | `/auth/login` | 公开 | — | 登录：企业统一认证优先；无则代办校验 RAGFlow 账号 → 签发 ext JWT（§6.1）；失败 20006 |
| 2 | POST | `/auth/refresh` | 公开（带 refresh token） | — | 旋转刷新；重放检测吊销整个家族 |
| 3 | POST | `/auth/logout` | 登录 | — | 吊销当前 jti（入黑名单） |
| 4 | GET | `/auth/me` | 登录 | — | 当前用户：工号、部门（含树路径）、角色、密级上限——门户与全前端初始化依赖（HLD 未明示，本规范补充） |
| 5 | GET | `/users/me/settings` | 登录 | — | 用户偏好（含无痕检索开关，R12） |
| 6 | PUT | `/users/me/settings` | 登录 | ✚ | 更新偏好（无痕开关切换即刻生效，热词统计侧联动） |

## 7.2 模块 M02：权限中心与审计（HLD §4.2.5，R01/R02/R20）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/roles` | manage:role | — | 角色列表（分页，?scope_type=&keyword=） |
| 2 | POST | `/roles` | manage:role | ✚ | 创建角色（名称/描述/权限点集合） |
| 3 | GET | `/roles/{id}` | manage:role | — | 角色详情（含成员数、权限点） |
| 4 | PUT | `/roles/{id}` | manage:role | ✚ | 更新角色；系统内置角色禁改禁删（40010） |
| 5 | DELETE | `/roles/{id}` | manage:role | — | 删除角色（需先清空成员，否则 40010） |
| 6 | POST | `/roles/{id}/users` | manage:role | — | 批量绑定成员（≤100，部分成功语义 §5.4） |
| 7 | DELETE | `/roles/{id}/users` | manage:role | — | 批量解绑成员 |
| 8 | GET | `/permissions` | manage:perm | — | ACL 条目查询（subject/resource/action/effect 多条件，分页） |
| 9 | POST | `/permissions` | manage:perm | ✚ | 批量创建 ACL（≤100） |
| 10 | DELETE | `/permissions` | manage:perm | — | 批量删除 ACL（按 id 数组） |
| 11 | GET | `/permissions/matrix` | manage:perm | — | 权限矩阵视图（用户 × 角色 × 动作） |
| 12 | PUT | `/permissions/matrix` | manage:perm | ✚ | 矩阵批量保存（差异应用，返回变更摘要） |
| 13 | POST | `/authz` | 网关 | — | **S1**：单点判定，204 放行 / 403（30001）；网关缓存 60s（§8.1） |
| 14 | POST | `/authz/batch-check` | 登录（服务内部） | — | 批量判定（检索列表过滤、批量按钮态渲染） |
| 15 | GET | `/authz/visible-datasets` | 登录（服务内部） | — | 用户可见 dataset_id 列表（部门+密级+ACL 合并，供检索代理） |
| 16 | GET | `/audit/logs` | manage:audit | — | 审计日志查询（分页、多条件；?export=csv → **S3** 文件流） |
| 17 | POST | `/audit/events` | 登录 | — | R20 行为审计上报（水印移除尝试、打印/下载尝试、外链页行为） |

## 7.3 模块 M03：组织架构（HLD §4.4.5，R04）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/org/departments/tree` | 登录 | — | 部门树（权限过滤；Redis 缓存 10min；降级时 50002 + 前端走本地快照） |
| 2 | POST | `/org/sync` | manage:org | ✚ | 手动触发同步（并发锁：运行中重复触发 → 40009） |
| 3 | GET | `/org/sync/status` | manage:org | — | 最近同步状态/连续失败次数/降级状态（与 50002 联动） |
| 4 | GET | `/org/users` | manage:org | — | 人员映射查询（分页：工号/部门/RAGFlow 账号/密级） |
| 5 | POST | `/org/users/import` | manage:org | ✚ | **S4**：Excel 兜底导入（multipart；异步任务；格式错误行在任务 result 中返回）（本规范补充） |

---

## 7.4 模块 M04：知识空间（HLD §4.3.5，R03）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/spaces/tree` | 登录 | — | 全量空间树（权限过滤 + 节点统计：sub_space_count/doc_count；?space_type=&status=&keyword=）（字段级设计见 §8.2） |
| 2 | POST | `/spaces` | manage:space | ✚ | 创建节点（space_type=group/kb；kb 同步透传原生 Dataset 创建参数） |
| 3 | GET | `/spaces/{id}` | 登录（可见性） | — | 节点详情（含挂载 dataset 元信息、统计） |
| 4 | PATCH | `/spaces/{id}` | manage:space | — | 编辑（改名/排序/描述/类型配置项） |
| 5 | DELETE | `/spaces/{id}` | manage:space | — | 删除（仅空 group → 40023；归档节点需先恢复） |
| 6 | POST | `/spaces/{id}/archive` | manage:space | ✚ | 归档（子树联动；归档后拒绝创建/移入 → 40022） |
| 7 | POST | `/spaces/{id}/restore` | manage:space | ✚ | 恢复 |
| 8 | POST | `/spaces/{id}/move` | manage:space | ✚ | 层级调整；`?dry_run=true` 返回权限/可见性差异预览；成环 → 40002；预检冲突 → 40024 |
| 9 | GET | `/spaces/{id}/datasets` | 登录（可见性） | — | 节点下 Dataset 与文档列表（聚合原生 API；Redis 缓存 5min） |
| 10 | POST | `/spaces/migrate/preview` | manage:space | ✚ | **S4**：存量迁移预演（异步；输出映射差异/孤儿清单） |
| 11 | POST | `/spaces/migrate/commit` | manage:space | ✚ | **S4**：存量迁移执行（幂等可重跑；映射冲突 → 40025） |
| 12 | GET | `/spaces/types` | 登录 | — | 空间类型配置清单（group/kb 的名称、图标、约束） |
| 13 | PATCH | `/spaces/types/{type}` | manage:space | ✚ | 类型配置维护（运营配置，非新增类型） |

## 7.5 模块 M05：审批中心（HLD §4.5.5 + §4.19.5，R08/R19）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/approvals/flows` | manage:approval | — | 流程定义列表（分页，?status=） |
| 2 | POST | `/approvals/flows` | manage:approval | ✚ | 创建流程定义（节点序列版本化） |
| 3 | GET | `/approvals/flows/{code}` | manage:approval | — | 定义详情（含节点、版本历史） |
| 4 | PUT | `/approvals/flows/{code}` | manage:approval | ✚ | 更新（产生新版本；被在途实例引用 → 40011） |
| 5 | DELETE | `/approvals/flows/{code}` | manage:approval | — | 停用（软删，历史实例可追溯） |
| 6 | POST | `/approvals/instances` | 登录 | ✚ | 发起审批（stash_key + 业务元数据；字段级设计见 §8.3） |
| 7 | GET | `/approvals/instances` | 登录 | — | 实例列表（?box=initiated/todo/done/all，分页） |
| 8 | GET | `/approvals/instances/{id}` | 登录（相关人） | — | 详情（含元数据校对预览、流转时间线、当前任务） |
| 9 | POST | `/approvals/instances/{id}/submit` | 登录（发起人） | ✚ | 提交（draft→reviewing） |
| 10 | POST | `/approvals/instances/{id}/cancel` | 登录（发起人） | ✚ | 撤回（reviewing→cancelled；已发布 → 40015） |
| 11 | POST | `/approvals/tasks/{id}/approve` | 登录（审批人/受托人） | ✚ | 同意（comment 必填；会签计数联动） |
| 12 | POST | `/approvals/tasks/{id}/reject` | 登录（审批人/受托人） | ✚ | 驳回（comment 必填；实例→failed 终态） |
| 13 | POST | `/approvals/tasks/{id}/return` | 登录（审批人/受托人） | ✚ | 退回发起人（实例回 draft） |
| 14 | POST | `/approvals/tasks/{id}/add-sign` | 登录（审批人） | ✚ | **R19** 加签（前/后加签；目标无效 → 40013） |
| 15 | POST | `/approvals/tasks/{id}/transfer` | 登录（审批人） | ✚ | **R19** 转签（任务重指派并记录轨迹） |
| 16 | GET | `/approvals/delegates` | 登录 | — | **R19** 我的委托设置列表 |
| 17 | POST | `/approvals/delegates` | 登录 | ✚ | **R19** 设置委托（scope_type=all/flow；区间重叠 → 40014） |
| 18 | DELETE | `/approvals/delegates/{id}` | 登录 | — | **R19** 撤销委托 |
| 19 | GET | `/approvals/stats` | manage:approval | — | **R19** 统计报表（审批时长/驳回率/超时分布） |

> 审批实例状态机：`draft → reviewing → publishing → published`；分支 `reviewing → failed（驳回）`、`draft/reviewing → cancelled`；非法迁移一律 40001。发布执行失败进入 `failed` 可由管理员重试（重试走 publishing 幂等）。

## 7.6 模块 M06：通知（HLD §4.5.5 通知能力，R08/R19 联动）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/notifications` | 登录 | — | 通知列表（?type=&unread=，分页） |
| 2 | GET | `/notifications/unread-count` | 登录 | — | 未读数（前端 30s 轮询） |
| 3 | POST | `/notifications/{id}/read` | 登录 | — | 单条已读（本规范补充：HLD 未明示） |
| 4 | POST | `/notifications/read-all` | 登录 | — | 全部已读（本规范补充） |

---

## 7.7 模块 M07：搜索域（HLD §4.12.5 + §4.13.5，R04/R12/R13）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | POST | `/search/proxy` | 登录 | ✚ | **S2**：部门/权限/密级过滤检索代理（SSE 透传原生流式帧；字段级设计见 §8.4） |
| 2 | GET | `/search/suggest` | 登录 | — | 检索建议（个人历史 + 热词榜聚合，?q= 前缀，?limit=10） |
| 3 | GET | `/search/history` | 登录 | — | 我的搜索历史（去重、?limit= 默认 10） |
| 4 | DELETE | `/search/history/{id}` | 登录 | — | 删除单条历史 |
| 5 | DELETE | `/search/history` | 登录 | — | 一键清空（无痕模式下本地不产生历史） |
| 6 | GET | `/hotwords` | 登录 | — | 热词榜单（Redis 直读，?limit=10；命中屏蔽词标记 blocked） |
| 7 | GET | `/hotwords/manage` | manage:hotword | — | 运营管理视图（榜单 + 运营词 + 屏蔽词，分页） |
| 8 | POST | `/hotwords/manage` | manage:hotword | ✚ | 置顶/屏蔽/补词（运营词；置顶超限 → 40041） |
| 9 | GET | `/hotwords/trends` | manage:hotword | — | 趋势报表（时间窗口聚合） |

## 7.8 模块 M08：版本中心（HLD §4.9.5，R09）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/documents/{doc_id}/versions` | 登录（可见性） | — | 版本时间线（is_current 标记、来源、变更摘要） |
| 2 | GET | `/documents/{doc_id}/versions/{no}` | 登录（可见性） | — | 版本详情（元数据 + chunk 差异统计） |
| 3 | GET | `/documents/{doc_id}/versions/diff` | 登录（可见性） | — | 两版本对比（?from=&to=；相同 → 40053；返回 added/removed/modified chunk 列表） |
| 4 | GET | `/documents/{doc_id}/versions/{no}/file` | 登录（可见性） | — | **S3**：源文件下载（与水印策略联动，§7.15） |
| 5 | POST | `/documents/{doc_id}/versions/{no}/rollback` | 登录 | ✚ | 发起回滚（按配置走审批或直接执行；在途回滚 → 40052） |

## 7.9 模块 M09：解析路由与解析评测（HLD §4.6.5 + §4.7.5 + §4.14.5，R06/R07/R14）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/parser-route/rules` | manage:parser | — | 路由规则列表（优先级排序，分页） |
| 2 | POST | `/parser-route/rules` | manage:parser | ✚ | 新建规则（表达式非法 → 40061） |
| 3 | PUT | `/parser-route/rules/{id}` | manage:parser | ✚ | 更新规则（优先级冲突 → 40062） |
| 4 | DELETE | `/parser-route/rules/{id}` | manage:parser | — | 删除规则 |
| 5 | POST | `/parser-route/decide` | 登录 | — | 暂存文件解析器推荐（多因子打分；字段级设计见 §8.5） |
| 6 | GET | `/parser-route/logs` | manage:parser | — | 决策日志与命中率/override 率指标（分页） |
| 7 | POST | `/parser-route/evaluate` | manage:parser | ✚ | **S4**：发起多 parser 对比评测（运行中重复 → 40063） |
| 8 | POST | `/parser-route/eval/samples` | manage:parser | ✚ | 评测样本批量上传（multipart；格式不支持 → 40064） |
| 9 | GET | `/parser-route/eval/reports/{id}` | manage:parser | — | 评测报告（分指标对比、推荐值） |

---

## 7.10 模块 M10：评测调优（HLD §4.10.5，R10）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/eval/datasets` | manage:eval | — | 评测集列表（分页） |
| 2 | POST | `/eval/datasets` | manage:eval | ✚ | 创建评测集（名称 + 用例集） |
| 3 | PUT | `/eval/datasets/{id}` | manage:eval | ✚ | 更新（含用例增删改；运行中被占用 → 40093） |
| 4 | DELETE | `/eval/datasets/{id}` | manage:eval | — | 删除（被实验引用 → 40010） |
| 5 | POST | `/eval/runs` | manage:eval | ✚ | **S4**：发起实验（参数网格；组合非法 → 40092；用例不足 → 40091） |
| 6 | GET | `/eval/runs` | manage:eval | — | 实验列表（分页，?dataset_id=&status=） |
| 7 | GET | `/eval/runs/{id}/report` | manage:eval | — | 指标报表（检索命中率/回答质量分/各参数组对比） |
| 8 | GET | `/prompts` | manage:eval | — | Prompt 模板列表 |
| 9 | POST | `/prompts` | manage:eval | ✚ | 新建 Prompt 模板 |
| 10 | PUT | `/prompts/{id}` | manage:eval | ✚ | 更新（版本化保留历史） |
| 11 | DELETE | `/prompts/{id}` | manage:eval | — | 删除（被实验引用 → 40010） |

## 7.11 模块 M11：模板与附件（HLD §4.15.5，R15）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/templates` | 登录 | — | 模板列表（?category=&keyword=，分页；按空间过滤） |
| 2 | POST | `/templates` | manage:template | ✚ | 创建（multipart 骨架文件 → 自动扫描占位符；解析失败 → 40071） |
| 3 | GET | `/templates/{id}` | 登录 | — | 详情（含字段 schema：name/type/required/rule） |
| 4 | PUT | `/templates/{id}` | manage:template | ✚ | 编辑（骨架替换/字段调整 → 产生新版本） |
| 5 | DELETE | `/templates/{id}` | manage:template | — | 删除（被实例引用 → 40010，改为停用语义） |
| 6 | GET | `/templates/{id}/fields` | 登录 | — | 字段元数据清单 |
| 7 | PUT | `/templates/{id}/fields` | manage:template | ✚ | 字段维护（类型/必填/校验规则/枚举选项） |
| 8 | POST | `/templates/{id}/render` | 登录 | ✚ | 表单值渲染生成文件（小文件同步返回 stash_key；大文件 **S4** 异步；校验失败 → 40072） |
| 9 | GET | `/templates/instances` | 登录 | — | 模板使用记录（我发起的/相关空间，分页） |
| 10 | GET | `/attachments` | 登录 | — | 附件列表（?link_type=&link_id=&category=，分页） |
| 11 | POST | `/attachments` | 登录 | ✚ | 上传附件（multipart；> 50MB → 40073，提示走网盘链接） |
| 12 | PATCH | `/attachments/{id}` | 登录（属主） | — | 编辑（名称/分类/有效期） |
| 13 | DELETE | `/attachments/{id}` | 登录（属主） | — | 删除（回收站 30 天） |
| 14 | GET | `/attachments/{id}/preview` | 登录 | — | **S3**：预览（> 20MB → 40073 提示下载；水印策略联动） |
| 15 | POST | `/attachments/{id}/link` | 登录（属主） | ✚ | 关联绑定（link_type/link_id 校验 → 40074） |
| 16 | GET | `/attachments/categories` | manage:attachment | — | 分类清单（树形） |

## 7.12 模块 M12：智能打标（HLD §4.16.5，R16）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | POST | `/tagging/tasks` | 登录（manage 或属主） | ✚ | **S4**：发起打标任务（范围：dataset/空间；LLM 抽取） |
| 2 | GET | `/tagging/tasks` | 登录 | — | 任务/批次列表（含待确认队列；分页） |
| 3 | POST | `/tagging/tasks/{id}/confirm` | 登录 | ✚ | 批量确认候选标签（采纳/修改/驳回；状态不允许 → 40083） |
| 4 | GET | `/tagging/candidates/{doc_id}` | 登录（可见性） | — | 单文档候选标签（含置信度、词表外标记 → 40084 引导） |
| 5 | GET | `/tags` | 登录 | — | 标签树（全量，不分页） |
| 6 | POST | `/tags` | manage:tag | ✚ | 新建标签（层级超限 → 40081） |
| 7 | PUT | `/tags/{id}` | manage:tag | ✚ | 更新（改名/移动层级，联动原生元数据批量改写分批 100/次） |
| 8 | DELETE | `/tags/{id}` | manage:tag | — | 删除（被引用 → 40082） |
| 9 | POST | `/tags/merge` | manage:tag | ✚ | 合并标签（源标签引用全部改写至目标） |
| 10 | GET | `/tags/stats` | manage:tag | — | 使用统计（覆盖率、TopN、增长趋势） |

## 7.13 模块 M13：路由权限与门户（HLD §4.17.5，R17）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/routes/permissions` | manage:route | — | 角色-路由规则列表（pattern/methods/roles/effect，分页） |
| 2 | POST | `/routes/permissions` | manage:route | ✚ | 新建规则（保存后网关热加载） |
| 3 | PUT | `/routes/permissions/{id}` | manage:route | ✚ | 更新规则 |
| 4 | DELETE | `/routes/permissions/{id}` | manage:route | — | 删除规则 |
| 5 | GET | `/routes/my` | 登录 | — | 当前用户可用入口清单（门户渲染依据；deny 命中 → 30007） |

---

## 7.14 模块 M14：分享外链（HLD §4.18.5，R18）

**管理面（A 面）**

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | POST | `/shares` | 登录（share 权限） | ✚ | 创建外链（有效期/密码/次数上限/水印策略；高密级 → 30003 强制审批或 40035 禁止） |
| 2 | GET | `/shares` | 登录 | — | 我的分享列表（?status=，分页） |
| 3 | PATCH | `/shares/{id}` | 登录（属主） | ✚ | 编辑（密码/有效期/次数/水印策略；变更即刻生效） |
| 4 | POST | `/shares/{id}/revoke` | 登录（属主） | ✚ | 撤销（即时生效，票据全失效） |
| 5 | GET | `/shares/{id}/stats` | 登录（属主） | — | 访问统计（PV/UV/成功/拒绝） |
| 6 | GET | `/shares/{id}/accesses` | 登录（属主/管理） | — | 访问明细（IP/UA/结果，分页） |

**公开面（B 面，免登录）**

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 7 | GET | `/s/{token}` | 公开 | — | 外链页元信息（标题/摘要/是否需密码/水印策略提示）；过期/撤销/超次统一 40003/40031/40032（404 防探测） |
| 8 | POST | `/s/{token}/verify` | 公开 | — | 密码校验 → 15 分钟票据（IP+UA 指纹）；错误 40033；超限 40034（429） |
| 9 | GET | `/s/{token}/content` | 公开（票据） | — | 票据拉取内容（分页/流式；输出经水印代理，禁直链 MinIO） |

## 7.15 模块 M15：水印与内容代理（HLD §4.20.3，R20）

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/watermark/policy` | 登录 | — | 按密级（?level=1~5）下发水印与管控策略（动态水印/禁打印/禁下载/双水印） |
| 2 | POST | `/content-proxy/documents/{doc_id}` | 登录 | ✚ | **S3**：加密内容分片获取（会话密钥协商；高密级直链默认拒绝）；行为上报走 §7.2-17 `/audit/events` |

## 7.16 模块 M16：系统支撑

| # | 方法 | 路径 | 权限 | 幂等 | 说明 |
|---|---|---|---|---|---|
| 1 | GET | `/healthz` | 公开（内网） | — | 存活 + 依赖探活（MySQL/Redis/MinIO/RAGFlow）（§6.7） |
| 2 | POST | `/uploads/stash` | 登录 | ✚ | 统一文件暂存（multipart → stash_key；§5.7）（本规范补充：HLD 各节引用 stash_key 但未单列接口） |
| 3 | GET | `/tasks/{task_id}` | 登录（属主） | — | 异步任务统一查询（S4 配套；§5.5）（本规范补充） |
| 4 | POST | `/tasks/{task_id}/cancel` | 登录（属主） | — | 取消排队/运行中任务（否则 40007） |
| 5 | GET | `/api/ext/v1/docs` `/openapi.json` | 内网 | — | OpenAPI 文档与契约（环境开关，§6.7） |

## 7.17 清单汇总

- 模块数：16（M01~M16）；接口操作数：145（A 面 142 + B 面 3），其中强制幂等 ✚ 51 个、特殊形态 S1×1 / S2×1 / S3×5 / S4×8；
- 本规范相对 HLD 各 4.x.5 节**补充**的必需接口（6 个）：`GET /auth/me`、`GET/PUT /users/me/settings`（M01）、`POST /org/users/import`（M03）、`POST /uploads/stash`、`GET /tasks/{task_id}`（M16）、`POST /notifications/{id}/read` 与 `/read-all`、`GET /notifications/unread-count`（M06）——补充依据：门户初始化、偏好持久化、兜底导入、暂存闭环、任务轮询闭环、通知已读闭环为前端可用性最小集；
- 18 项需求（R01~R20）接口覆盖对照见附录 D。

---

# 8. 典型接口字段级设计

> 本章选取 6 个最具代表性/最易出分歧的接口做字段级定义，其余接口以 OpenAPI（自动生成的 Schema）为准。所有请求/响应示例的完整路径均省略基础路径 `/api/ext/v1`。

## 8.1 POST /authz（S1 网关判定，M02-13）

**请求**（nginx auth_request 子请求，携带 `X-Ext-Internal-Token`）：

```json
{
  "user_id": "u_9f27c3a1",
  "method": "GET",
  "path": "/api/v1/datasets/6a1fa3e2b7c94d5e8f0a1b2c3d4e5f67/documents",
  "context": { "source_ip": "10.2.3.4", "user_agent": "Mozilla/5.0 ..." }
}
```

**成功响应**：HTTP **204 No Content**（无响应体，符合 auth_request 协议）。

**拒绝响应**：HTTP 403

```json
{
  "code": 30001,
  "message": "无权访问该资源",
  "data": null,
  "trace_id": "01JB2X8K9G2QF7W3D5R8T6Y0ZC",
  "details": { "resource": "dataset:6a1fa3e2...", "action": "read", "subject": "user:u_9f27c3a1", "rule_id": 10086 }
}
```

| 约束项 | 规范 |
|---|---|
| 路径解析 | 只判定 `/api/v1/*` 白名单内的受控资源（附录 B）；非受控路径直接 204 |
| 判定顺序 | deny 优先 → 密级（30002）→ allow（30001）→ 默认拒绝 |
| 性能 | P99 < 30ms（Redis 缓存判定结果 60s，key=user+method+path 模板） |
| 审计 | 拒绝事件采样落审计（§6.6） |

## 8.2 GET /spaces/tree（M04-1）

**请求**：`GET /spaces/tree?space_type=&status=&keyword=&expand=all`

**响应**（不分页，`data.items` 为递归节点数组）：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "items": [
      {
        "id": "12",
        "parent_id": null,
        "name": "华东大区",
        "space_type": "group",
        "status": "active",
        "sort_no": 1,
        "path": "/12/",
        "depth": 1,
        "dataset_id": null,
        "description": "华东区知识空间",
        "stats": { "sub_space_count": 5, "doc_count": 328 },
        "children": [
          {
            "id": "13",
            "parent_id": "12",
            "name": "销售制度库",
            "space_type": "kb",
            "status": "active",
            "sort_no": 1,
            "path": "/12/13/",
            "depth": 2,
            "dataset_id": "6a1fa3e2b7c94d5e8f0a1b2c3d4e5f67",
            "stats": { "sub_space_count": 0, "doc_count": 128 },
            "children": []
          }
        ]
      }
    ]
  },
  "trace_id": "01JB2X..."
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| space_type | enum | `group` / `kb` / `space_group`（附录 C，与 DDL 逐字一致） |
| status | enum | `active` / `archived` |
| path | string | 物化路径（`/父id/本id/`），前端面包屑与拖拽成环校验（40002）依据 |
| stats.doc_count | int | 聚合自原生 API，Redis 缓存 5min，允许轻微延迟 |
| children | array | 递归；无子节点返回 `[]`（§2.6） |

## 8.3 POST /approvals/instances（M05-6，强制幂等 ✚）

**请求头**：`X-Idempotency-Key: 7c9e6679-7425-40de-944b-e07fc1f90ae7`（缺失 → 10008）

**请求**：

```json
{
  "flow_code": "default_doc_upload",
  "action": "upload",
  "biz_ref_type": "dataset",
  "biz_ref_id": "6a1fa3e2b7c94d5e8f0a1b2c3d4e5f67",
  "stash_key": "stash_9d8c1e2f",
  "metadata": {
    "file_name": "2026年Q3销售制度.pdf",
    "dataset_name": "销售制度库",
    "doc_meta": { "department": "0001/0012", "security_level": 3 }
  },
  "remark": "首次上传，请审核"
}
```

| 字段 | 必填 | 校验 |
|---|---|---|
| flow_code | 是 | 存在且 status=active（附录 C 枚举），否则 40006/40007 |
| action | 是 | ∈ {upload, update, delete, share, config}（DDL `ext_approval_instance.action`） |
| biz_ref_type | 是 | ∈ {document, dataset, space} |
| stash_key | 是 | 有效期内暂存键（§5.7），过期 → 40009 前置条件不满足 |
| metadata.doc_meta | 是 | 部门编码 + 密级（1~5），发布时经 `doc_meta` API 写入原生（R09 联动） |

**成功响应**：HTTP 201

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "instance_id": "9001",
    "status": "reviewing",
    "current_node": "空间管理员审核",
    "tasks": [
      { "task_id": "50001", "assignee": "u_88f2", "due_at": "2026-09-19T17:00:00+08:00" }
    ],
    "created_at": "2026-09-17T10:30:00.128+08:00"
  },
  "trace_id": "01JB2X..."
}
```

> `status` 直接进入 `reviewing`（自动提交流程）；若流程配置为草稿模式则返回 `draft` 并由 `/submit` 推进。下游全部任务由通知模块推送（M06）。

---

## 8.4 POST /search/proxy（M07-1，S2 SSE 流）

**请求**：

```json
{
  "query": "差旅报销标准",
  "dataset_ids": [],
  "space_ids": ["12"],
  "department_scope": "dept_and_children",
  "top_n": 6,
  "stream": true,
  "similarity_threshold": 0.2
}
```

| 字段 | 说明 |
|---|---|
| dataset_ids | 显式指定时与"用户可见集"取**交集**（不放大）；空则用可见全集（visible-datasets） |
| department_scope | `self` / `dept` / `dept_and_children` / `all`（受角色上限约束，越权 → 30001） |
| stream | true=SSE；false=聚合 JSON（同信封结构，供导出/无流式场景） |

**响应**：`Content-Type: text/event-stream`

```
data: {"code":0,"data":{"stage":"accepted","seq":0}}

data: {"code":0,"data":{"stage":"retrieval","seq":1,"chunks":[{"chunk_id":"c_11","content":"《差旅报销管理办法》第八条...","dataset_id":"6a1f...","document_id":"d_77","similarity":0.87}]}}

data: {"code":0,"data":{"stage":"answer","seq":2,"content":"根据《差旅报销管理办法》"}}

: ping

data: {"code":0,"data":{"stage":"answer","seq":3,"content":"第八条，出差住宿标准为..."}}

data: {"code":0,"data":{"stage":"done","seq":4,"answer_id":"a_1001","references":[{"document_id":"d_77","dataset_id":"6a1f...","positions":[[0,18]]}],"elapsed_ms":2340}}

```

**错误帧**（下游故障时）：

```
event: error
data: {"code":50001,"message":"RAGFlow API 调用失败","data":null,"trace_id":"01JB2X...","details":{"upstream_code":102,"endpoint":"/api/v1/retrieval","elapsed_ms":3004}}

```

| 约束项 | 规范 |
|---|---|
| 过滤时机 | 可见集/部门/密级过滤在**透传前**完成（E2 执行点，§6.3），chunks 帧不出现越权内容 |
| 帧语义 | stage ∈ {accepted, retrieval, answer, done}；seq 单调递增（§5.8） |
| 历史记录 | 查询完成后写 `ext_search_history`（entry=proxy；无痕模式跳过，R12） |
| 中断 | 网关/客户端断开即取消下游请求（透传断连），不产生孤儿任务 |

## 8.5 POST /parser-route/decide（M09-5）

**请求**：

```json
{
  "stash_key": "stash_9d8c1e2f",
  "file_name": "2026年Q3销售台账.xlsx",
  "hint": { "user_selected": "naive", "size_bytes": 8341504 }
}
```

**响应**：HTTP 200

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "parser_id": "book",
    "confidence": 0.87,
    "reasons": [
      { "factor": "extension", "value": ".xlsx", "weight": 0.40 },
      { "factor": "multi_sheet", "value": true, "weight": 0.30 },
      { "factor": "merged_cells", "value": true, "weight": 0.17 }
    ],
    "rule_id": 33,
    "alternatives": [ { "parser_id": "naive", "confidence": 0.41 } ]
  },
  "trace_id": "01JB2X..."
}
```

| 约束项 | 规范 |
|---|---|
| parser_id | 取值域与 RAGFlow 原生 parser 名册一致（book/naive/paper/laws/…），透传不改写 |
| 置信度 | [0,1]；< 0.6 时前端必须展示 alternatives 供用户选择（人机协同，R06 设计） |
| rule_id | 命中的路由规则；无命中（走默认）返回 null 并记决策日志（R07 统计口径） |
| 缓存 | 同 checksum 结果缓存 10min |

## 8.6 典型错误响应集（信封实例）

**10001 参数校验失败**（HTTP 400）：

```json
{
  "code": 10001, "message": "参数校验失败", "data": null, "trace_id": "01JB2X...",
  "details": {
    "fields": [
      { "field": "metadata.doc_meta.security_level", "rule": "range", "message": "取值需在 1~5 之间" }
    ]
  }
}
```

**40001 审批状态机非法迁移**（HTTP 422）：

```json
{
  "code": 40001, "message": "当前状态不允许该操作", "data": null, "trace_id": "01JB2X...",
  "details": { "from": "published", "to": "reviewing", "allowed": ["failed"] }
}
```

**50001 RAGFlow API 调用失败**（HTTP 502）：

```json
{
  "code": 50001, "message": "知识库服务暂时不可用，请稍后重试", "data": null, "trace_id": "01JB2X...",
  "details": { "upstream_code": 109, "upstream_message": "Authentication error", "endpoint": "/api/v1/datasets", "elapsed_ms": 128 }
}
```

**10009 幂等键冲突**（HTTP 409）：

```json
{
  "code": 10009, "message": "重复请求与首次请求内容不一致", "data": null, "trace_id": "01JB2X...",
  "details": { "idempotency_key": "7c9e6679-7425-40de-944b-e07fc1f90ae7" }
}
```

## 8.7 details 结构字典（按错误码）

| 错误码 | details 结构 |
|---|---|
| 10001 / 40072 | `{fields: [{field, rule, message}]}` |
| 10003 | `{expected, actual}` |
| 10004 | `{limit}` |
| 10006 / 10007 | `{field, value, allowed[]}` / `{field, value, allowed[]}` |
| 10009 | `{idempotency_key}` |
| 20004 | `{reason}` |
| 30001 | `{resource, action, subject, rule_id?}` |
| 30002 | `{required_level, user_level}` |
| 30003 | `{required_flow}` |
| 30005 | `{resource}` |
| 30006 | `{permission}` |
| 40001 | `{from, to, allowed[]}` |
| 40008 | `{expected_version, current_version}` |
| 40012 | `{processed_by, processed_at}` |
| 40023 | `{doc_count, sub_space_count}` |
| 40033 | `{remaining_attempts}` |
| 40061 | `{expression, reason}` |
| 40071 | `{invalid_placeholders[]}` |
| 40084 | `{word}` |
| 50001 | `{upstream_code, upstream_message, endpoint, elapsed_ms}` |
| 50002 | `{last_success_at, consecutive_failures}` |
| 50005 | `{provider, model}` |
| 90002 | `{hint}` |
| 其余 | 无 details（null 省略） |

> 原则：details 字段为**白名单契约**（进入 OpenAPI Schema），新增字段属兼容变更，删除/改名属破坏性变更（§10.3）。

---

# 9. 前端接入规范

## 9.1 TypeScript 类型契约（ext-web 公共类型）

```typescript
/** 统一信封（§3.1） */
export interface ApiResponse<T> {
  code: number;            // 0 成功
  message: string;         // 用户可读
  data: T | null;
  trace_id: string;
  details?: Record<string, unknown>; // 仅失败出现
}

/** 分页数据体（§3.2） */
export interface PagedData<T> {
  items: T[];
  total: number;
  page: number;
  page_size: number;
}

/** 字段级校验错误（10001/40072） */
export interface FieldError {
  field: string;
  rule: string;
  message: string;
}

/** 异步任务（S4，§5.5） */
export type TaskStatus = 'queued' | 'running' | 'succeeded' | 'failed' | 'cancelled';
export interface AsyncTask {
  task_id: string;
  type: string;
  status: TaskStatus;
  progress: { done: number; total: number } | null;
  result: unknown;
  error: { code: number; message: string } | null;
  created_at: string;
  finished_at: string | null;
}

/** SSE 帧级信封（§5.8） */
export interface SseFrame<T = unknown> {
  code: number;
  message?: string;
  data: T;
  seq?: number;
}
```

## 9.2 请求层行为矩阵

ext-web 基于 axios 建立独立实例 `extRequest`（与原生 `request.ts` 并行，不混用拦截器栈），行为与 §4.6 矩阵一一对应：

| 环节 | 行为 |
|---|---|
| 请求前 | camelCase→snake_case 键转换；自动附 `Authorization`；写操作自动生成 `X-Idempotency-Key` 并缓存（重试复用同一键）；注入 `X-Trace-Id`（uuid） |
| 响应后 | `code===0` → 返回 `data`（snake_case→camelCase）；否则抛 `ApiError{code, message, details, trace_id, httpStatus}` |
| 401 处理 | 20002 → 静默 refresh 队列（并发 401 合并为一次 refresh）→ 重放原请求；20001/20003 → 登出重定向 |
| 重试 | GET 与 429/502/503/504 指数退避 ≤3 次（SSE/上传不自动重试）；写重试仅限幂等键接口且复用原键 |
| 错误呈现 | 10001/40072 → 表单字段；其余按 §4.6（message.error / notification.error + trace_id 复制按钮） |
| 上传 | 统一走 `useStashUpload()`：先 `/uploads/stash` 再业务提交，进度条基于 XHR onuploadprogress |

## 9.3 SSE 客户端封装要求

- 统一 `createExtSse(url, body)` 封装：基于 `fetch` + `ReadableStream`（EventSource 不支持 POST/头），自动携带 Bearer、自动续期（连接存活超 25min 重建）、`Last-Event-ID` 断线重连（指数退避 ≤5 次）；
- 对外暴露 `onFrame(stage, data)` / `onError(err)` / `onDone(final)` / `abort()`；
- 页面卸载（路由切换）必须 `abort()`，防止孤儿流。

## 9.4 类型生成与联调

- `pnpm gen:api`：以 ext 服务导出的 `openapi.json` 经 `openapi-typescript` 生成 `src/types/api.d.ts`，业务代码**禁止**手写接口类型；
- Mock 联调：开发期以 `openapi.json` 驱动 `@stoplight/prism` 起 Mock（含错误码示例响应），前后端并行开发；
- 契约版本：`openapi.json` 快照入库 `contracts/` 目录，PR 中 diff 可见（§10.2）。

## 9.5 环境变量

| 变量 | 示例 | 说明 |
|---|---|---|
| `VITE_EXT_API_BASE` | `/api/ext/v1` | A 面基础路径（经网关） |
| `VITE_EXT_SSE_BASE` | `/api/ext/v1` | SSE 基础路径（同域，网关关闭缓冲） |
| `VITE_EXT_PUBLIC_BASE` | `/s` | B 面外链页路径（独立轻量构建） |

> B 面外链页为独立 entry（轻量包，无管理端依赖），仅依赖 §7.14 公开面 3 个接口与 `/watermark/policy` 行为约定。

---

# 10. 契约管理与变更控制

## 10.1 OpenAPI 即契约（Single Source of Truth）

- 本文档（规范语义）+ `openapi.json`（机器契约）双轨：**冲突时以 OpenAPI 为准并回改本文档**；
- FastAPI 路由以 `response_model` 强制约束信封结构，禁止裸 dict 返回；错误码经 `app/common/error_codes.py` 常量引用，禁止字面量。

## 10.2 快照与评审流

1. 后端 PR 必须包含 `/api/ext/v1/openapi.json` 重新导出的快照 diff；
2. diff 为空的功能 PR 视为"无接口变更"（内部实现）；
3. 有 diff 的 PR 需在描述中标注变更级别（§10.3）并 @前端负责人评审通过方可合并。

## 10.3 变更分级

| 级别 | 变更内容 | 流程 |
|---|---|---|
| COMPATIBLE（补丁） | 新增可选请求字段/响应字段、新增接口、新增枚举值*、放宽校验 | 常规 PR + 前端知会 |
| MINOR（次版本） | 新增资源/模块、新增错误码 | PR + 双方评审，本文档同步更新 |
| MAJOR（主版本） | 删除/改名字段、改类型/语义、收窄枚举、变更 HTTP 语义 | 升 `/api/ext/v2` + 迁移方案 + ≥6 个月双轨 |

> *新增枚举值对"前端穷举 switch"是破坏性的：前端必须对未知枚举值做 default 兜底（规范强制），该兜底前提下新增枚举值才视为 COMPATIBLE。

## 10.4 版本与兼容承诺

- v1 存续期内禁止 MAJOR 变更直接上线；
- 错误码废弃：标注 `deprecated` ≥ 6 个月，期间新码旧码并存（旧码仅透传不新产生）。

## 10.5 CI 校验（契约门禁）

| 校验项 | 规则 |
|---|---|
| 唯一性 | 错误码常量无重复、码-HTTP 绑定与附录 A 一致 |
| 信封 | 所有 2xx JSON 响应 Schema 含 code/message/data/trace_id；失败 Schema 含 details |
| 路径 lint | kebab-case、复数名词、层级 ≤2、动作白名单 |
| 枚举一致性 | OpenAPI enum 与 `ddl/ragflow_ext_schema.sql` ENUM 逐字 diff（附录 C 生成脚本） |
| RetCode 覆盖 | `error_mapper.py` 映射表覆盖 `common/constants.py: RetCode` 全量成员 |
| 快照 diff | 无 openapi.json 快照变更的"接口变更 PR"直接阻断 |

## 10.6 本版待评审确认项（遗留）

1. 密级 1~5 的具体口径与部门-密级联动规则（HLD 附录 D 遗留项 #5），影响 30002 details 字段与 `/watermark/policy` 返回结构；
2. 统一认证（SSO）落地后 `/auth/login` 是否降级为"换取接口"（保留代办校验作为降级路径）；
3. 通知是否引入 WebSocket 推送替代 30s 轮询（当前按轮询设计，接口形态预留升级空间）；
4. SSE 断线续传的游标语义在检索代理场景的必要性（答案无法幂等重放时降级为"重开新流"）。

---

# 附录 A：错误码 ↔ HTTP 状态码速查

按 HTTP 状态分组（错误码语义明细见 §4.3，映射总则见 §4.4）：

| HTTP | 错误码 | 说明 |
|---|---|---|
| 200 | 0 | 常规成功（含 DELETE 成功） |
| 201 | 0 | 创建成功（POST 集合资源） |
| 202 | 0 | 异步任务受理（S4） |
| 204 | —（无响应体） | 仅 `POST /authz` 放行（S1） |
| 400 | 10001 ~ 10008 | 参数与请求错误 |
| 401 | 20001、20002、20003、20005、20006、20007 | 认证与会话（含外链票据失效） |
| 403 | 20004、20008、30001~30007、40035 | 账号禁用/票据指纹不匹配/权限段/高密级禁外链 |
| 404 | 40003、40006、40031、40032、40051 | 资源不存在（含外链失效统一表现防探测） |
| 409 | 10009、40005、40007、40008、40010、40012、40014 | 冲突类（幂等键/唯一约束/状态/并发/被引用/已处理/委托重叠） |
| 422 | 其余全部 4xxxx | 业务规则默认状态 |
| 429 | 40034、50006、90006 | 限流（密码爆破/下游限流/本服务限流），均附 `Retry-After` |
| 500 | 90001 ~ 90004 | 系统内部错误 |
| 502 | 50001、50003、50005 | 下游故障（RAGFlow / MinIO / LLM） |
| 503 | 50002、50004、90007 | 降级中/缓存故障/熔断维护 |
| 504 | 50007 | 下游超时 |

> **与 HLD 的一致性说明**：HLD §6.2 失败示例使用码 `40301`（"无权访问该知识空间"），与 HLD §6.3 分段规则（3xxxx＝权限）冲突。本规范按 §6.3 分段规则收口为 **30004（知识空间权限不足）**，`40301` 保留号段但不启用，特此记录评审决策。

---

# 附录 B：原生 RetCode 映射表与 C 面调用白名单

## B.1 RetCode → ext 错误码映射表（`app/ragflow_client/error_mapper.py` 实现基线）

RetCode 定义源：`common/constants.py:45`（0.26.4，16 个成员，全量覆盖无遗漏）：

| 原生 code | 常量名 | 语义 | ext 映射 | HTTP | 告警 | 备注 |
|---|---|---|---|---|---|---|
| 0 | SUCCESS | 成功 | 0 | 200 | — | — |
| 10 | NOT_EFFECTIVE | 对象未生效（如租户） | 50001 | 502 | P1 | 服务账号租户状态异常 |
| 100 | EXCEPTION_ERROR | 上游内部异常 | 50001 | 502 | P1 | — |
| 101 | ARGUMENT_ERROR | 上游参数校验失败 | 50001 | 502 | P1 | 多为 ext 请求构造缺陷，研发介入 |
| 102 | DATA_ERROR | 上游数据错误 | 50001 | 502 | P1 | — |
| 103 | OPERATING_ERROR | 上游操作失败 | 50001 | 502 | P1 | — |
| 105 | CONNECTION_ERROR | 上游连接错误（LLM/存储） | 50001 / 50005 | 502 | P1 | LLM 场景细分映射 50005 |
| 106 | RUNNING | 任务运行中 | **0（非错误）** | 200 | — | run 状态透传 data，前端继续轮询 |
| 108 | PERMISSION_ERROR | 上游权限拒绝 | 50001 | 502 | **P0** | 服务账号被拒；令牌热加载 + 重试一次 |
| 109 | AUTHENTICATION_ERROR | 上游认证失败 | 50001 | 502 | **P0** | 服务账号令牌失效；同上 |
| 400 | BAD_REQUEST | 请求非法 | 50001 | 502 | P1 | — |
| 401 | UNAUTHORIZED | 未认证 | 50001 | 502 | **P0** | 同 109 |
| 403 | FORBIDDEN | 禁止访问 | 50001 | 502 | **P0** | 同 108 |
| 404 | NOT_FOUND | 资源不存在 | 50001 或 **40006** | 502 / 404 | P1 | 经本地资源映射特判后决定（§4.5-3） |
| 409 | CONFLICT | 冲突 | 50001 | 502 | P1 | — |
| 500 | SERVER_ERROR | 上游服务器错误 | 50001 | 502 | P1 | — |

**核心不变式**：① 服务账号身份的失败（108/109/401/403）一律 50001 + P0 告警，**绝不映射为用户侧 2xxxx/3xxxx**（终端用户已在 ext 完成鉴权，此类失败属系统级故障）；② 上游 404 必须先过本地资源映射特判；③ 上游 HTTP 429 → 50006（429）；超时/连接失败 → 50007/50001。

## B.2 C 面调用白名单（继承 HLD 附录 A，逐字一致）

ext 服务仅允许调用以下官方 REST API（`/api/v1`，服务账号 Bearer Token，响应结构 `{code, message, data}`）；**白名单外接口（如 `/v1` 前端内部接口）禁止在 ext 代码中出现（CI 静态检查）**：

| 能力域 | 接口 | 用途（本方案） |
|---|---|---|
| 数据集 | `POST /datasets`、`GET /datasets`、`GET /datasets/{id}`、`PUT /datasets/{id}`、`DELETE /datasets/{id}` | 空间管理（R03）、配置调优（R10） |
| 文档上传 | `POST /datasets/{id}/documents`（支持 `parser_id`）、`POST /documents/upload`（预检信息） | 审批发布（R08）、解析路由（R06） |
| 文档管理 | `GET /datasets/{id}/documents`（列表/过滤）、`PATCH /datasets/{id}/documents/{doc_id}`（含 parser 修正）、`DELETE /datasets/{id}/documents`、`POST /datasets/{id}/documents/status` | 发布/版本/回滚（R08/R09） |
| 元数据 | `GET /datasets/{id}/metadata/summary`、`POST /datasets/{id}/metadata/update`、`PATCH /datasets/{id}/documents/metadatas`、`PUT /datasets/{id}/documents/{doc}/metadata/config` | 部门字段（R04）、打标（R16） |
| 解析 | `POST /datasets/{id}/chunks`（触发解析）、`GET /datasets/{id}/documents/{doc}/chunks`、`PUT /datasets/{id}/documents/{doc}/chunks/{chunk_id}` | 解析触发/快照/人工修正（R06/R07/R09） |
| 对话 | `POST /chats`、`PUT /chats/{id}`（prompt/检索参数）、`POST /chats/{id}/completions`（SSE，支持 `doc_ids`/`metadata_condition`） | 调优（R10）、检索代理（R04/R12） |
| 检索应用 | `POST /searches`、`GET/PUT/DELETE /searches/{id}`、`POST /searches/{id}/completions`（SSE，`kb_ids` 范围） | 检索代理（R04） |
| OpenAI 兼容 | `POST /api/v1/openai/chat_completions`（`extra_body.metadata_condition`） | 部门/密级过滤问答（R04/R02） |
| 文件工作区 | `POST/GET /workspaces/{id}/commits`、`GET .../commits/{id}`、`GET .../commits/diff`、`GET /workspace-files/{fid}/versions` 等 | 版本管理（R09，workspace 场景复用原生） |
| 用户与租户 | `GET /users`（信息）、`GET /tenants`（团队） | 用户映射构建 |
| 模型 | `GET/POST /llms`（模型配置） | 调优环境准备（R10） |

> 完整参数以 `api/apps/restful_apis/` 源码与官方 SDK（`sdk/python/ragflow_sdk/`）为准；后续里程碑随需求逐接口字段化补充。

---

# 附录 C：通用枚举清单（与 DDL 逐字对齐）

## C.1 事实源与对齐机制

1. **唯一事实源**：`ddl/ragflow_ext_schema.sql`（评审版：610 行 / 22 张表 / 34 个 ENUM 列）。本附录由 DDL 生成（脚本纳入 CI，对应 §10.5"枚举一致性"门禁），人工不得脱离 DDL 单独修改本附录；
2. OpenAPI（`openapi.json`）中任何 `enum` 取值必须与本附录一致；DDL 变更 PR 未同步本附录 → CI 阻断；
3. 前端 TS 类型（§9.1）由 OpenAPI 生成，禁止手写字面量枚举与散落魔法字符串。

## C.2 DDL 已落地枚举（34 项，按域分组，取值逐字摘自 DDL）

| 域 | 表.列 | 取值 | 主要暴露接口 |
|---|---|---|---|
| 组织 | `ext_department.status` | `active` / `merged` / `revoked` | `/org/departments/tree` |
| 组织 | `ext_org_sync_log.source` | `hr_api` / `excel` / `manual` | `/org/sync/status` |
| 组织 | `ext_org_sync_log.trigger_type` | `schedule` / `manual` | `/org/sync/status` |
| 组织 | `ext_org_sync_log.result` | `success` / `partial` / `failed` | `/org/sync/status` |
| 空间 | `ext_space.space_type` | `group` / `kb` / `space_group` | `/spaces`、`/spaces/tree`（§8.2） |
| 空间 | `ext_space.status` | `active` / `archived` | `/spaces/tree` |
| 空间 | `ext_space_dataset_map.event` | `mount` / `unmount` / `move` / `migrate` / `adopt` | 迁移任务 result、审计 |
| 权限 | `ext_role.scope_type` | `global` / `space` | `/roles` |
| 权限 | `ext_permission_entry.subject_type` | `user` / `role` / `dept` / `position` | `/permissions`、`/authz` |
| 权限 | `ext_permission_entry.resource_type` | `global` / `space` / `dataset` / `document` / `field` | `/permissions`、`/authz` |
| 权限 | `ext_permission_entry.action` | `read` / `edit` / `manage` / `share` / `approve` | `/permissions`、`/authz` |
| 权限 | `ext_permission_entry.effect` | `allow` / `deny` | `/permissions`、`/authz/batch-check` |
| 审计 | `ext_audit_log.source` | `gateway` / `ext_web` / `ext_api` / `sync_job` | `/audit/logs` |
| 审计 | `ext_audit_log.result` | `success` / `denied` / `error` | `/audit/logs` |
| 审批 | `ext_approval_flow.biz_action` | `upload` / `update` / `delete` / `share` / `config` | `/approvals/flows` |
| 审批 | `ext_approval_flow.scope_type` | `global` / `space` / `dataset` | `/approvals/flows` |
| 审批 | `ext_approval_flow.status` | `draft` / `active` / `disabled` | `/approvals/flows?status=` |
| 审批 | `ext_approval_node.node_type` | `submit` / `review` / `publish` | `/approvals/flows/{code}` |
| 审批 | `ext_approval_node.countersign` | `any` / `all` | `/approvals/flows/{code}` |
| 审批 | `ext_approval_node.timeout_action` | `remind` / `escalate` / `auto_approve` | `/approvals/flows/{code}` |
| 审批 | `ext_approval_instance.action` | `upload` / `update` / `delete` / `share` / `config` | `/approvals/instances` |
| 审批 | `ext_approval_instance.biz_ref_type` | `document` / `dataset` / `space` | `/approvals/instances` |
| 审批 | `ext_approval_instance.status` | `draft` / `reviewing` / `publishing` / `published` / `failed` / `cancelled` | 状态机（§7.5 注） |
| 审批 | `ext_approval_task.task_type` | `review` / `countersign` / `add_sign` | `/approvals/instances/{id}` |
| 审批 | `ext_approval_task.status` | `pending` / `approved` / `rejected` / `returned` / `transferred` / `timeout` / `cancelled` | `/approvals/instances?box=` |
| 审批 | `ext_approval_delegate.scope_type` | `all` / `flow` | `/approvals/delegates` |
| 审批 | `ext_approval_delegate.status` | `active` / `expired` / `revoked` | `/approvals/delegates` |
| 通知 | `ext_notification.type` | `todo` / `timeout` / `result` / `failure` / `system` | `/notifications?type=` |
| 通知 | `ext_notification.channel` | `inbox` / `im` | `/notifications` |
| 搜索 | `ext_search_history.entry` | `proxy` / `suggest` / `direct` | `/search/history` |
| 热词 | `ext_hotword.source` | `stats` / `corpus` / `manual` | `/hotwords/manage` |
| 版本 | `ext_document_version.source` | `approval` / `manual` / `migration` / `rollback` | `/documents/{id}/versions` |
| 版本 | `ext_version_chunk_diff.change_type` | `added` / `removed` / `modified` | `/documents/{id}/versions/diff` |

## C.3 接口层枚举（非 DDL 来源；事实源 = 本规范正文 + OpenAPI）

| 枚举 | 取值 | 使用位置 |
|---|---|---|
| `task.status` | `queued` / `running` / `succeeded` / `failed` / `cancelled` | `GET /tasks/{task_id}`（§5.5）；§9.3 `TaskStatus` |
| `security_level`（密级） | `1` ~ `5`（int；具体口径待评审，§10.6-1） | `/auth/me` 密级上限、`/watermark/policy?level=`、`/shares` 高密级校验（30003/40035） |
| 通用排序方向 | `asc` / `desc` | §2.4 `sort` 参数 |

## C.4 软枚举与受控词表（DDL 为 VARCHAR，取值由应用层约束）

| 字段 | 约束 | 说明 |
|---|---|---|
| `ext_space_type_config.type_code` | 约定 `group` / `kb` / `space_group` 起步，允许自定义业务类型（`uk_type_code` 唯一） | `/spaces/types` 返回；新增类型 ≠ 新增 `space_type` 枚举 |
| `ext_space_type_config.default_parser_id` | 必须 ∈ 原生 ParserType 白名单（`common/constants.py:123`，15 值） | 创建 kb 节点时透传原生 Dataset 创建 |
| `ext_space.type_config`（JSON） | 节点级覆盖 B2 默认值；JSON schema 随 OpenAPI 登记 | `PATCH /spaces/{id}` |

## C.5 待落地表枚举登记规则（后续里程碑）

以下表**不在本版 DDL 22 张内**，随对应里程碑 Alembic 迁移落地；落地时必须同步登记进 C.2（门禁同 C.1）。过渡期（接口先行、表未落地）相关枚举以 OpenAPI `enum` 为准，DDL 落地后回溯对齐：

- **R06/R07/R14**：`ext_parser_route_rule`、`ext_parser_route_log`、`ext_parser_eval`、`ext_parser_eval_sample`；
- **R10**：`ext_eval_dataset`、`ext_eval_case`、`ext_eval_run`、`ext_eval_result`、`ext_prompt_template`；
- **R15**：`ext_template`、`ext_template_version`、`ext_template_instance`、`ext_attachment`；
- **R16**：`ext_tag`、`ext_tagging_task`、`ext_tag_dict`；
- **R18**：`ext_share_link`、`ext_share_access`；
- **R20**：`ext_watermark_policy`（行为审计复用已落地的 `ext_audit_log`）。

---

# 附录 D：接口—需求—数据表对照矩阵

18 项需求（R01~R20）的接口覆盖对照。**"✓" = 已在 `ddl/ragflow_ext_schema.sql`（22 张）落地；"待落地" = 随对应里程碑补齐（附录 C.5）**：

| 需求 | 模块 | 主要接口前缀（§7） | 扩展数据表 | DDL | 原生依赖（C 面，附录 B.2） |
|---|---|---|---|---|---|
| R01/R02 权限模型与审计 | M02 | `/roles`、`/permissions`、`/authz`、`/audit` | `ext_role`、`ext_role_user`、`ext_permission_entry`、`ext_audit_log` | ✓（4/4） | `GET /users` |
| R03 多层级知识空间 | M04 | `/spaces`、`/spaces/types` | `ext_space`、`ext_space_type_config`、`ext_space_dataset_map` | ✓（3/3） | `POST/GET/PUT/DELETE /datasets` |
| R04 部门过滤与组织同步 | M03 + M07 | `/org`、`/search/proxy` | `ext_department`、`ext_org_sync_log`、`ext_user`、`ext_search_history` | ✓（4/4） | `GET /users`；`POST /searches` + `completions`（`metadata_condition`） |
| R05 标签筛选修复 | M07 | `/search/proxy`（复用） | 无新表（L1 修复链路，见 HLD 4.4） | — | `PATCH .../documents/metadatas`（标签写入侧） |
| R06 解析路由 | M09 | `/parser-route/rules`、`/parser-route/decide` | `ext_parser_route_rule`、`ext_parser_route_log` | 待落地 | `POST /datasets/{id}/documents`（`parser_id`） |
| R07/R14 解析质量评测 | M09 | `/parser-route/evaluate`、`/parser-route/eval` | `ext_parser_eval`、`ext_parser_eval_sample` | 待落地 | `POST /datasets/{id}/chunks`、`GET .../chunks` |
| R08/R19 审批流与高级审批 | M05 + M06 | `/approvals`、`/notifications` | `ext_approval_flow`、`ext_approval_node`、`ext_approval_instance`、`ext_approval_task`、`ext_approval_delegate`、`ext_notification` | ✓（6/6） | `POST /datasets/{id}/documents`、`POST .../documents/status` |
| R09 版本管理 | M08 | `/documents/{doc_id}/versions` | `ext_document_version`、`ext_version_chunk_diff` | ✓（2/2） | `GET .../chunks`、workspace commits |
| R10 检索与回答调优 | M10 | `/eval`、`/prompts` | `ext_eval_dataset`、`ext_eval_case`、`ext_eval_run`、`ext_eval_result`、`ext_prompt_template` | 待落地 | `POST /chats`、`PUT /chats/{id}`、`completions`、`GET/POST /llms` |
| R11 存储与传输加密 | —（L0 网关/存储改造） | — | — | — | — |
| R12 检索体验 | M07 + M01 | `/search`、`/users/me/settings` | `ext_search_history`（复用）、`ext_user_setting` | ✓ | `POST /searches/{id}/completions` |
| R13 热词 | M07 | `/hotwords` | `ext_hotword`（Redis 主存 + 快照留档） | ✓ | — |
| R15 模板与附件 | M11 | `/templates`、`/attachments` | `ext_template`、`ext_template_version`、`ext_template_instance`、`ext_attachment` | 待落地 | — |
| R16 智能打标 | M12 | `/tagging`、`/tags` | `ext_tag`、`ext_tagging_task`、`ext_tag_dict` | 待落地 | `PATCH .../metadatas`、`GET .../metadata/summary` |
| R17 门户与路由权限 | M13 | `/routes` | `ext_route_permission` | ✓ | — |
| R18 分享外链 | M14 | `/shares`（A 面）、`/s/{token}`（B 面） | `ext_share_link`、`ext_share_access` | 待落地 | `GET /datasets/{id}/documents` |
| R20 水印与内容安全 | M15 + M02 | `/watermark/policy`、`/content-proxy`、`/audit/events` | `ext_watermark_policy`（行为审计复用 `ext_audit_log` ✓） | 待落地 | — |
| （横切）系统支撑 | M16 | `/tasks`、`/uploads/stash`、`/healthz` | —（任务注册表：Redis） | — | — |

**汇总**：DDL 评审版已落地 22 张表（覆盖 M1/M2 里程碑全部需求：R01~R05、R08/R09、R12/R13、R17）；其余 7 项需求的表随对应里程碑 DDL 演进落地并回溯登记（附录 C.5）。矩阵中"复用"表示该表已在其他需求行计入 ✓，不重复计数。

---

**（全文完）**

> 本文档为 RAGFLOW-EXT-API-001 V1.0（评审版）全文。契约变更遵循 §10 分级与 CI 门禁；§10.6 所列 4 项待评审确认项应在详设评审会一并决议后升版 V1.1。
