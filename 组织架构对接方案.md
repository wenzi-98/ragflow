# RAGFlow 二开组织架构对接方案

| 项目 | 内容 |
|---|---|
| 文档编号 | RAGFLOW-EXT-ORG-001 |
| 文档版本 | V1.1（评审版） |
| 编制依据 | 《20260819-RAGFlow知识库二开工作计划》任务 1.4；数据源《港深公司組織架構圖2026（6月不含主任级）签名档.pdf》（签署 2026-06-01） |
| 编写人 | 李燕文 |
| 编制日期 | 2026-09-21 |
| 评审状态 | 待评审 |
| 密级 | 内部 |

**修订记录**

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| V1.0 | 2026-09-21 | 李燕文 | 初稿：架构图层级树还原（36 节点）、编码/导入/幂等规范、同步频率矩阵、对账指标、三级降级（40009/50002）、Mock 联调 6 场景 17/17 PASS |
| V1.1 | 2026-09-21 | 李燕文 | 落实确认结论：① Q4「何俊」同一人 → 合并 EMP0022 一号两岗（主岗质检科 0020、兼职 0018），人员种子 35→34；② Q5 后勤物资科/基础设施科确认属行政部（编码不变），关闭；③ Q1 列出 HR 可编程接口 8 类形态清单与 OrgSourceProvider 适配层设计；④ 人名修正：徐明章/张颜艳/邓绍谦/姜员泉/张改换；⑤ 新增附录 E《人员维护操作手册》；⑥ 增补「无 HR 系统运营模式」（§4.1.0 补注 + 附录 E.6）；⑦ 技术科 0024 改隶技术研发部 0021（编码不变）、研发科经理正名周树标、培训中心经理补录邓小军（EMP0036，人员 34→35）；⑧ 人名修正：财务部副经理夏云→夏军 |

---

## 目录

1. 文档说明　2. 组织架构现状调研　3. 数据规范　4. 接口规范　5. 同步机制
6. 对账与数据质量　7. 降级与监控　8. 联调测试记录　9. 风险与遗留问题　附录 A~E

---

# 1. 文档说明

## 1.1 编制背景与依据

本文档是《工作计划》**任务 1.4「组织架构接口对接调研」**的核心交付物，是 R04（部门过滤检索）与 R01（权限体系）的开发前置。工作目标：

1. 摸清组织架构数据源与数据质量（基于现有唯一数据源——组织架构签名档图）；
2. 设计接口与数据规范（初始化导入格式、幂等 upsert 键、部门编码全路径规则、主/兼职部门）；
3. 设计同步频率、对账指标与三级降级机制（错误码 40009/50002）；
4. 以 Mock 数据完成同步引擎核心逻辑联调并形成测试记录。

## 1.2 与其他文档的关系

| 文档 | 关系 |
|---|---|
| 《二开技术方案设计文档》RAGFLOW-EXT-HLD-001 | §4.3.3 为本方案基线：同步源、数据模型、同步/降级机制、联动策略；冲突处以 HLD 评审结论为准 |
| 《前后端接口规范文档》RAGFLOW-EXT-API-001 | §7.3 模块 M03（5 接口）为 A 面接口基线；§3 统一返回格式、§4 错误码（40009/50002）直接引用，本文档不重复定义 |
| 《扩展数据库设计文档》RAGFLOW-EXT-DDD-001 与 `ddl/ragflow_ext_schema.sql` | 域A A1 `ext_user` / A2 `ext_department` / A3 `ext_org_sync_log` 为落地表结构，本文档字段语义与 DDL 逐字对齐 |
| 《联调用例/》（本方案配套目录） | `mock_departments.json`（36 部门节点）、`mock_employees.json`（35 人）、`mock_org_sync_test.py`（同步引擎模拟，17/17 PASS） |

## 1.3 调研范围与方法

**数据源现状**：当前唯一可获得的组织数据源为《港深公司組織架構圖2026（6月不含主任级）签名档.pdf》——含部门层级、部门定编/在编人数、部门负责人姓名与职级、公司备注（定编 1000 / 在编 1033）、总经理签署与日期。企业 HR 系统是否存在可编程接口（REST/DB 视图/定时导出）**待甲方确认**（疑点 Q1，见 §9）。

**识图方法**（图像工程备忘，详见附录 D）：

1. 原始 PDF 200dpi 转 PPM（`pdftoppm -r 200` 输出 `org200-1.ppm`）；
2. 六块直切切片存在切断方框、父子连线丢失问题，不满足层级还原要求；
3. 自研 `band.py`（纯 Python PPM 解析 + PNG 编码 + 90° 顺时针旋转）将正置图切为 **6 条横向条带（30% 重叠）**，重叠区保留跨块边界连线，配合整幅正置图逐块识读、交叉拼接，完成 36 节点层级树与全部人员/职级还原；
4. 结构化结果以 JSON 固化（`联调用例/mock_departments.json`、`mock_employees.json`），作为后续同步初始化的种子数据。

> 经验记录：复合 shell 命令与 heredoc 写文件存在竞态，长任务必须单条串行执行 + `ast.parse`/`py_compile` 预校验 + `nohup` 后台 + `sleep` 后查验产物。

---

# 2. 组织架构现状调研

## 2.1 层级树还原结果（36 节点）

> 原图为繁体，本文档统一转写简体；编码为本文档新分配（§3.1），非 HR 既有编码。

```
0001 港深公司【徐明章 总经理】（定编1000/在编1033，图面备注）
├─ 0002 海外营业部（96/93）营销总监 邓绍谦 —— 何定山 副总经理 分管
│   ├─ 0003 业务科（图面无科名，Q2）高级经理 陈洁玲 / 经理 江雪灵 / 助理经理 杨丰亘 /
│   │      杜绍铭 / 袁仕辉 / 陈佩璋 / 洪建宗
│   └─ 0004 船务科 助理经理 程红权
├─ 0005 中国营业部（22/22）销售总监 林文清 / 助理经理 刘小庆
├─ 0006 厂务部（711/743）生产总监 姜员泉
│   └─ 0007 厂部（图面中间框仅“廠務部”三字，Q3）
│       ├─ 0008 生产计划科（无负责人标注）
│       ├─ 0009 外发管理中心（无负责人标注）
│       ├─ 0010 工艺车间 助理经理 王兵伟（兼 0011/0012/0017）
│       ├─ 0011 印后车间 助理经理 王兵伟
│       ├─ 0012 装订中心 助理经理 王兵伟
│       ├─ 0013 设备管理科 经理 吴迎春
│       ├─ 0014 仓库后勤运输科 助理经理 冯二（兼 0015）
│       ├─ 0015 成品包装科 助理经理 冯二
│       ├─ 0016 工程科（无负责人标注）
│       ├─ 0017 印刷中心（印前中心/印刷车间）印前中心副经理 林子健 / 助理经理 王兵伟
│       └─ 0018 数码科创中心 经理 何俊（Q4 已确认：与质检科何俊同一人，主岗 0020、兼职 0018）
├─ 0019 品质管理部（60/60）
│   └─ 0020 质检科 经理 何俊 / 助理经理 田志
├─ 0021 技术研发部（17/17）—— 杜科 助理总经理 分管
│   ├─ 0022 研发科 经理 周树标
│   └─ 0024 技术科 副经理 杨波（自培训中心改隶，编码不变）
├─ 0023 培训中心（6/6）经理 邓小军（图面缺失，2026-09-21 补录）
├─ 0025 人力资源部（12/12）人力资源经理 容正思
│   └─ 0026 环保体系科 助理经理 陈安
├─ 0027 安全保卫部（10/10）安全总监 康彦涛 —— 张颜艳 助理总经理 分管
│   ├─ 0028 安全保卫科（无负责人标注）
│   └─ 0029 工业安全科（无负责人标注）
├─ 0030 行政部（32/31）
│   ├─ 0031 报关科 副经理 张改换
│   ├─ 0032 行政科 副经理 苏金玲
│   ├─ 0033 后勤物资科（图面与 0034 同框，无负责人标注，Q5）
│   └─ 0034 基础设施科（同上）
├─ 0035 采购部（10/10）经理 赖伟峰
└─ 0036 财务部（24/23）经理 邱旭力 / 副经理 夏军 / 助理经理 张海宏 —— 萧永健 财务总监 分管
```

**节点统计**：部门节点 36（公司 1 + 一级部门 11 + 二级科室 24）；高管 5 人（总经理 1 / 副总经理 1 / 助理总经理 2 / 财务总监 1）不占部门编制、以 ext_user 岗位表达（§3.5）。

## 2.2 图面口径对账（本次调研实证）

| 口径 | 图面分项合计 | 图面备注 | 结论 |
|---|---|---|---|
| 定编 | 11 个一级部门加总 = 96+22+711+60+17+6+12+10+32+10+24 = **1000** | 1000 | **精确吻合** ✓ |
| 在编 | 11 个一级部门加总 = 93+22+743+60+17+6+12+10+31+10+23 = **1027**，加高管 5 人 = **1032** | 1033 | **差 1 人**（Q6）|

在编差 1 人属图面级差异（识读误差或统计口径），不影响结构还原，已列入对账机制的实证案例（§6.2）：同步对账报告将此类差异输出为「人工确认清单」，而非静默吞掉或阻断同步。

## 2.3 数据特征与同步挑战

| 特征 | 图面实例 | 同步设计应对 |
|---|---|---|
| 一人多点兼职 | 王兵伟：主岗工艺车间（0010），兼印后车间（0011）/装订中心（0012）/印刷中心（0017）；冯二：主仓库后勤运输科（0014）兼成品包装科（0015） | `ext_user.dept_code`（主）+ `secondary_depts` JSON（兼，多值）；R04 文档元数据 department 字段多值数组 |
| 一人两岗 | 何俊：质检科经理（主岗 0020）兼数码科创中心经理（0018）——Q4 已确认同一人 | 以 employee_no 为唯一幂等键，**禁止**以姓名为键；secondary_depts 多值承载兼职部门（EMP0022 主 0020 兼 ["0018"]） |
| 科室无负责人标注 | 生产计划科、外发管理中心、工程科、安全保卫科、工业安全科、后勤物资科、基础设施科 | dept_leader 推导规则（§3.5）允许无负责人：审批流 assignee_rule 走 fallback（role:super_admin） |
| 科名缺失/框名待确认 | 海外营业部人员大框（Q2）、厂务部中间框（Q3） | 初始化导入用占位名+待确认标记，确认后增量更新（dept_code 不变，仅改 dept_name） |
| 不含主任级 | 图题注明「不含主任级」 | 人员全量以 HR 在职花名册为准，架构图仅作结构基准；初始化导入后以对账补齐 |


---

# 3. 数据规范

## 3.1 部门编码与全路径规则

| 项 | 规则 | 示例 |
|---|---|---|
| dept_code | 4 位顺序码（`%04d`），全局唯一，首次初始化时分配后**永不复用、永不改号**；HR 若有既有部门主键，以其为 dept_code（str 化），本文档编码仅作无既有编码时的种子 | `0006` 厂务部 |
| parent_code | 父部门编码，根为 NULL | 厂务部→港深公司 |
| full_path | 编码全路径 `父full_path/dept_code`，物化存储（对齐 DDL A2 注释双重用途：① RAGFlow 文档元数据 department 字段取值；② 检索代理 metadata_condition 前缀匹配「本部门+下级」） | `0001/0006/0010` 工艺车间 |
| name_path | 名称全路径（展示用） | `港深公司/厂务部/工艺车间` |
| level | 层级深度，根=1 | 厂务部=2，工艺车间=3 |
| sort_no | 同级排序（按图面从左至右） | — |
| status | `active`/`merged`/`revoked`（枚举与 DDL 一致）；merged 必须携带 `merged_to` | — |

**full_path 维护规则**：仅由同步任务写入（应用层自根递归重算，见联调脚本 `rebuild_path()`）；禁止手工改 path。部门移动/合并时受影响子树批量重算（小规模月度级变更，性能无虞；大规模重组走 §5.3 补刷联动）。

**编码分配总表**：见附录 A（0001~0036 全量 + 预留段说明：新增部门顺延取号，merged/revoked 号段不回收）。

## 3.2 幂等 upsert 键（同步不重复、不脏写的根基）

| 表 | 幂等键 | 冲突处置 |
|---|---|---|
| ext_department | `dept_code`（UNIQUE） | `INSERT ... ON DUPLICATE KEY UPDATE`（应用层先查后写亦可，联调脚本即此实现）；同键重复导入 inserted=0 |
| ext_user | `employee_no`（UNIQUE，HR 工号主键）+ `ragflow_user_id`（UNIQUE，对主库映射） | 工号重复导入仅更新变动字段并刷 last_synced_at；姓名/部门同名不同人不冲突 |
| ext_org_sync_log | `batch_no`（UNIQUE，`YYYYMMDDHHmmss+序号`） | 批次重放天然隔离 |

**孤儿节点拒绝**：parent_code 指向不存在部门时**拒绝落库并计入批次错误**（联调 S1 验证 `rebuild_path` 抛错路径），避免「先插子后插父」中断造成脏树；HR 全量包必须保证父先于子（本文档种子数据已按此排序）。

## 3.3 初始化导入格式

**① JSON 全量包**（本方案种子数据即此格式，`联调用例/mock_departments.json`）：

```json
{
  "snapshot_date": "2026-06-01",
  "company": "港深公司",
  "departments": [
    {"dept_code":"0006","parent_code":"0001","dept_name":"厂务部","sort_no":3,
     "planned":711,"actual":743,
     "leaders":[{"name":"姜员泉","title":"生产总监"}]}
  ],
  "employees": [
    {"employee_no":"EMP0018","display_name":"王兵伟","primary_dept":"0010",
     "secondary_depts":["0011","0012","0017"],"position_code":"P06",
     "position_name":"助理经理","status":1}
  ]
}
```

> `planned/actual/leaders` 为**图面还原辅助字段**：仅用于初始化对账与负责人推导的种子输入，不落 ext_department（DDL 无此列）；定编/在编不入库的理由：口径归 HR，ext 库只维护「同步时刻在职人员集合」，避免双口径漂移。

**② Excel 兜底导入模板**（HR 接口不可用时的人工通道，接口=`POST /api/ext/v1/org/users/import`，S4 异步任务）：

| 列 | 必填 | 说明 |
|---|---|---|
| employee_no | ✓ | 工号（幂等键） |
| display_name | ✓ | 姓名 |
| dept_path | ✓ | 名称全路径（`港深公司/厂务部/工艺车间`），导入器按 name_path 反查 dept_code，无匹配则该行报错不落库 |
| secondary_dept_paths |  | 兼职部门（`;` 分隔多值） |
| position_name |  | 岗位名（按 §3.5 分级映射 position_code） |
| status |  | 在职/离职，缺省在职 |

部门级 Excel（初始化建树用）列：dept_code / parent_code / dept_name / sort_no。格式错误行**不阻断**整体导入，错误明细在任务 result 中返回（对齐 API 规范 M03-5）。

## 3.4 主/兼职部门

| 规则 | 说明 |
|---|---|
| 主部门唯一 | `ext_user.dept_code` 单值；权限判定、首页组织定位均以主部门为第一优先 |
| 兼职多值 | `secondary_depts` JSON 数组（如 `["0011","0012","0017"]`）；ACL 判定时兼职部门与主部门**同权**（R01 部门维度） |
| 文档元数据多值 | R04 部门字段写入主部门 full_path + 兼职 full_path 数组（HLD 4.3.3「主部门+兼职部门多值支持」）；检索过滤按数组任一命中 |
| 变更语义 | 主部门变更=调动（触发旧部门可见性回收）；兼职增减=权限增量（即时生效，下次判定生效） |

## 3.5 岗位分级与负责人推导

**岗位分级码**（position_code，建议口径，评审后冻结）：

| 码 | 级别 | 图面实例 |
|---|---|---|
| P01 | 总经理 | 徐明章 |
| P02 | 副总经理 | 何定山 |
| P03 | 助理总经理/各总监 | 杜科、张颜艳、萧永健（财务总监）、邓绍谦（营销总监）、林文清（销售总监）、姜员泉（生产总监）、康彦涛（安全总监） |
| P04 | 经理/高级经理 | 陈洁玲（高级经理）、吴迎春、何俊、周树标、赖伟峰、邱旭力、邓小军、容正思（人力资源经理） |
| P05 | 副经理 | 林子健（印前中心副经理）、杨波、张改换、苏金玲、夏军 |
| P06 | 助理经理 | 程红权、刘小庆、王兵伟、冯二、田志、陈安、张海宏 |
| P09 | 职员 | 杜绍铭、袁仕辉、陈佩璋、洪建宗 |

**部门负责人（dept_leader）推导规则**（供审批流 `assignee_rule: {"type":"dept_leader"}` 使用，不新增 ext_department 列）：

1. 取 dept_code=该部门 且 position_code ∈ {P03,P04,P05,P06} 的在职人员，按级别降序、工号升序取第一人；
2. 部门无负责人（如生产计划科）→ **上溯父部门**重取（最多至公司级）；仍无 → assignee_rule 的 `fallback: role:super_admin`（宁升级不卡死，对齐 HLD 4.8.3③）；
3. 高管（P01~P02）不参与部门负责人推导；分管关系（何定山→海外营业部等）为管理层信息，仅记录于本文档与附录 A 备注列，不入库。

## 3.6 人员状态生命周期

| status | 语义 | 触发 |
|---|---|---|
| 1 | 生效 | 在职同步 |
| 0 | 离职停用 | HR 在职集合消失；同步置 0（不物理删除，保留审计/历史审批链）；已发布外链、在途审批按域C 各自策略处理 |

离职人员的历史数据（搜索历史、审批记录、版本操作人）以 ragflow_user_id 关联保留，展示层标注「已离职」。

---

# 4. 接口规范

## 4.1 HR → ragflow-ext 拉取接口约定（上游）

> HR 系统可编程接口形态**待甲方选型**（Q1；V1.1 已列 8 类形态清单见下，适配层设计使选型不阻塞开发）。若全部不可用，恒以 Excel 兜底（§7.4）。以下为**拉取侧最小接口集**，作为与 HR 方对接谈判基线：

**候选形态清单（§4.1.0，按对接优先级排序）**：

| # | 形态 | 说明 | 实时性 | 对接成本 |
|---|---|---|---|---|
| 1 | 原生 REST/OpenAPI | HR 自带开放接口（token/签名认证）；SaaS/自研网关最理想 | 高（分钟级增量） | 低 |
| 2 | 数据库只读视图/从库 | HR 方按 §4.1 字段约定建只读视图，ext 直查 | 高 | 中（视图隔离表结构） |
| 3 | 中间交接表 | HR 定时写入交接表，ext 轮询增量 | 中 | 中 |
| 4 | 文件接口（SFTP/邮件） | HR 定时导出 CSV/Excel，ext 定时拉取解析 | 中低 | 低 |
| 5 | SOAP WebService/ESB | 遗留企业总线调用 | 中 | 高 |
| 6 | 事件推送 Webhook | 入/调/离事件实时推送，与 1/3 配合 | 最高 | 中 |
| 7 | IM 平台通讯录 API | 组织源实为钉钉/企微时直接拉通讯录 | 高 | 低 |
| 8 | 人工 Excel 导入 | §7.4 兜底通道（恒备） | 人工 | 零开发 |

**适配层设计**：ragflow-ext 侧定义 `OrgSourceProvider` 抽象（`list_departments()` / `list_employee_changes(since)`），形态 1~7 各实现一个 Adapter、按配置切换——Q1 选型结果**不阻塞开发**，甲方确认哪种形态即启用对应 Adapter；降档路径 1→2/3→4→8。

> **无 HR 系统时**：直接选型形态 8，Q1 无需答复；事实源转为人工台账 Excel（§3.3② 冻结模板，指定唯一组织数据责任人）+ 管理端 `POST /org/users/import`（每周固定/异动即导），编码规范、幂等与对账机制全部不变——运营模式详见附录 E.6。

| # | 假定接口 | 说明 | 频率 |
|---|---|---|---|
| H1 | `GET {HR_BASE}/departments?snapshot=` | 部门全量（含 parent、sort、状态、定编/在编） | 每日 1 次 + 手动 |
| H2 | `GET {HR_BASE}/departments/changes?since={ts}` | 部门增量（新增/更名/移动/合并/撤销） | 15 min |
| H3 | `GET {HR_BASE}/employees?snapshot=` | 在职人员全量（工号/姓名/部门/岗位/职级/入离职日期） | 每日 1 次 |
| H4 | `GET {HR_BASE}/employees/changes?since={ts}` | 人员增量（入/调/兼/离） | 15 min |

**对接约定**：认证 API Key + 源 IP 白名单（具体以 HR 侧为准，ragflow-ext 侧仅出网拉取、不对公网暴露）；幂等由 since 时间戳 + 本地 batch_no 保障；**拉取失败不计入 HR 侧责任，由 §7 降级机制兜底**。

**字段映射表**（HR 字段 → ext 字段，DDL 逐字对齐）：

| HR 侧字段 | ext 表.字段 | 转换规则 |
|---|---|---|
| dept_id | ext_department.dept_code | str 化；HR 无主键时按 §3.1 分配 |
| dept_parent_id | parent_code | NULL=根 |
| dept_name | dept_name | 繁转简（图面为繁体） |
| dept_sort | sort_no | 缺省 0 |
| dept_status | status | active/merged/revoked 枚举映射，merged 附 dept_merged_to → merged_to |
| dept_updated_at | hr_updated_at | 对账依据 |
| emp_no | ext_user.employee_no | 幂等键，必填 |
| emp_name | display_name | — |
| emp_email | email | 主库 user.email 对账映射 |
| emp_dept_id | dept_code | 必须存在于 ext_department，否则该行报错 |
| emp_dept_ids_extra | secondary_depts | JSON 数组化 |
| emp_position / emp_title | position_name / position_code | 按 §3.5 分级 |
| emp_status | status | 在职=1 / 离职=0 |
| — | ragflow_user_id | 不由 HR 提供：按 email 匹配主库 user 回填（§5.2 步骤④） |

## 4.2 A 面管理接口（引用 API 规范 §7.3 M03，本文档不重复定义）

| # | 接口 | 与本方案的联动 |
|---|---|---|
| 1 | `GET /org/departments/tree` | 数据源=ext_department（status=active）；Redis 缓存 10min；降级时 50002 + 前端本地快照（§7.2） |
| 2 | `POST /org/sync` | 手动触发全量对账；运行中重复触发 → 40009（并发锁，§7.2） |
| 3 | `GET /org/sync/status` | 读 ext_org_sync_log 最新批次 + STATE（consecutive_failures/degraded/frozen），与 50002 联动 |
| 4 | `GET /org/users` | 人员映射查询（分页：工号/部门/RAGFlow 账号/密级） |
| 5 | `POST /org/users/import` | Excel 兜底（§3.3②）；source=excel，成功后自动解除降级（§7.1 恢复规则） |

# 5. 同步机制

## 5.1 同步频率矩阵

| 数据类型 | 方式 | 频率 | 窗口/锁 | 幂等键 |
|---|---|---|---|---|
| 部门增量 | H2 拉取 | 每 15 min | Redis 分布式锁 `org_sync`；运行中重复触发 → 40009 | dept_code |
| 人员增量 | H4 拉取 | 每 15 min（与部门增量同批） | 同上 | employee_no |
| 部门全量对账 | H1 拉取 | 每日 02:00 | 独立锁 `org_full_sync` | dept_code |
| 人员全量对账 | H3 拉取 | 每日 02:30 | 同上 | employee_no |
| 手动触发 | `POST /org/sync` | 按需（管理端） | 与定时共用锁 | — |
| Excel 兜底 | `POST /org/users/import` | 按需（HR 故障期） | 异步任务队列 | employee_no |

> 频率与 HLD 4.3.3③ 一致（每日凌晨全量对账 + 15 分钟增量 + 手动带锁）。15 分钟增量若 HR 不支持 since 参数（Q1），退化为整点全量对账 + 每日全量，降级路径不变。

## 5.2 同步任务流程（全量对账，增量同理）

```
① 取锁（失败→40009 记日志退出）                ② 拉取 H1/H3（超时 30s，失败→降级计数++，§7.1）
③ 逐行校验：parent 存在性/枚举合法性/工号唯一   ④ upsert ext_department / ext_user（幂等键）
⑤ 重算受影响子树 full_path/name_path/level      ⑥ 回填映射：email 匹配 rag_flow.user → ragflow_user_id
⑦ 统计 dept_stats/user_stats → 写 ext_org_sync_log（batch_no/source/result/…）
⑧ 成功：刷新 last_success_at、清降级计数、失效部门树缓存；失败：保留旧数据不动
```

**人员-部门映射回填**（⑥）：优先 email 精确匹配主库 `rag_flow.user.email`；次选工号（RAGFlow 账号备注字段，若启用）；均未命中 → 标记待绑定，管理端 `GET /org/users` 呈现缺口清单人工绑定。**映射率是对账指标之一**（§6.1 M5）。

## 5.3 变更联动（对 R04 检索的影响收口）

| 组织变更 | ext 侧动作 | 联动动作 |
|---|---|---|
| 部门更名/移动 | full_path 重算 | 受影响 full_path 的文档元数据 department 补刷（队列化、限速 20 doc/s、夜间窗口，HLD 4.3.7） |
| 部门合并 | status=merged + merged_to；子树人员 dept_code 划转（联调 S3 验证） | 旧 full_path 元数据补刷为 merged_to 的 full_path |
| 部门撤销 | status=revoked | 同上；无人员时无补刷 |
| 人员调动 | dept_code 更新 + secondary_depts 重建 | 次次检索判定即时生效（无需补刷文档——过滤条件是「人的部门→允许的 full_path 集合」） |
| 人员离职 | status=0 | ACL 主体失效；在途审批走域C 委托/超时升级 |

> 设计取舍：文档元数据存部门 full_path 而非工号，使**人员流动零补刷**；仅组织结构变化触发补刷（月度级小量），成本可控。

## 5.4 批次日志与可观测性

`ext_org_sync_log` 每批次一行（DDL A3）：batch_no / source(hr_api|excel|manual) / trigger_type(schedule|manual) / dept_stats / user_stats（`{inserted,updated,deactivated}` JSON）/ result(success|partial|failed) / degraded_events（快照沿用/冻结/告警明细）/ error_msg / started_at / finished_at。管理端 `GET /org/sync/status` 与告警均以此表为唯一事实源。

---

# 6. 对账与数据质量

## 6.1 对账指标矩阵

| # | 指标 | 计算口径（每日全量对账产出） | 阈值 | 超阈处置 |
|---|---|---|---|---|
| M1 | 部门数一致率 | HR active 部门数 vs ext_department active 数 | 100% | 差异清单入报告，result=partial |
| M2 | 在编一致率 | HR 在职人数 vs ext_user status=1 数 | 100% | 同上 |
| M3 | 归属有效率 | ext_user.dept_code 与 secondary_depts 全部命中 ext_department(active) | 100%（孤儿=缺陷） | 同步侧拒绝写（§3.2），存量告警 |
| M4 | 定编覆盖 | 一级部门定编合计 vs 公司总定编（基准 1000） | 差异 ≤0（信息项） | 报告呈现，不入阻断 |
| M5 | 主库映射率 | ext_user 中 ragflow_user_id 已回填占比 | ≥98% | 缺口清单 → 管理端人工绑定 |
| M6 | 工号唯一性 | employee_no 重复数 | 0 | 同步拒绝（UNIQUE 兜底）+ 告警 |
| M7 | 树完整性 | full_path/level 与 parent 链重算一致性抽查 | 100% | 重算修复 + 告警 |

对账报告 JSON（挂 ext_org_sync_log.dept_stats/user_stats 与管理端呈现），形如联调 S4 输出：

```json
{"reconcile_at":"…","planned_total":1000,"actual_total":1032,
 "baseline":{"planned":1000,"actual":1033},
 "gaps":[{"item":"在编","diff":1,"suspect":"图面识读误差或高管口径（差1人），转人工核对"}]}
```

## 6.2 本次图面对账实证

初始化种子数据（36 节点）对账结果：**M4 定编 1000 vs 1000 精确吻合**；在编 1027+5=1032 vs 备注 1033 差 1（Q6）。结论：图面定编口径可信，可直接作为初始化基准；在编以 HR 花名册为准、图面值仅作初版参考。该差异将由 M2（HR 全量对账）自然收敛——图面对账的价值在于**证明对账机制能捕获此类差异**（联调 S4 已验证输出）。

## 6.3 人工确认机制

差异清单（M1~M7 超阈项）→ 管理端「对账差异」页呈现（字段：类型/差异明细/建议处置）→ 管理员逐项「确认同步/标记图面误差/挂起」→ 处置结果回写批次日志。**差异不自动改库**（除 M3 孤儿节点同步侧硬拒绝外），避免 HR 脏数据直冲权限体系。

---

# 7. 降级与监控

## 7.1 三级降级机制（对齐 HLD 4.3.3「降级机制」与 API 规范 50002）

| 级别 | 触发条件 | 系统行为 | 查询侧体验 |
|---|---|---|---|
| L1 沿用快照 | 单批次失败（result=failed） | 记日志、降级计数 +1、告警（信息级）；**ext 库旧数据一律不动** | 无感（照常返回上次成功数据） |
| L2 降级告示 | 连续 **2** 个周期失败 | `GET /org/departments/tree` 返回 **50002（HTTP 503）**，`details={last_success_at, consecutive_failures}`；前端切换本地缓存快照 | 顶部提示「组织架构数据维护中，展示最近快照」 |
| L3 冻结保护 | 连续 **3** 个周期失败 | **冻结部门变更类操作**（部门权限调整、空间-部门映射变更、元数据补刷启动）+ 管理员强通知；ext_org_sync_log 记 degraded_events | 同 L2，且管理操作入口置灰 |

**恢复规则**：任一来源同步成功（hr_api 或 excel 兜底）→ 清零计数、解除 50002 与冻结、失效树缓存、补发恢复通知（联调 S5 已验证全程）。

**为什么冻结阈值=3**：L1 单次失败可能是网络抖动；连续 3 周期（约 45 分钟）失败说明 HR 侧持续异常，此时若放任管理员基于「陈旧部门树」做权限调整，恢复后极易产生权限漂移——冻结是把风险锁在窗口期内。

## 7.2 错误码联动（引用 API 规范 §4，此处仅列组织架构相关）

| 码 | HTTP | 场景 | details |
|---|---|---|---|
| 40009 | 422 | 同步运行中重复触发（并发锁）/ 冻结期内提交部门变更 | `{lock:"org_sync"}` / `{frozen:true, since}` |
| 50002 | 503 | 组织架构同步降级中（L2/L3） | `{last_success_at, consecutive_failures}` |
| 30006 | 403 | manage:org 权限点缺失 | `{permission:"manage:org"}` |
| 10005/10001 | 400 | 分页越界 / 参数校验失败（如导入行 dept_path 无法解析） | 按 API 规范通用结构 |

## 7.3 监控告警矩阵

| 指标 | 阈值 | 级别 | 渠道 |
|---|---|---|---|
| 同步批次 result=failed | 单次 | WARN | 管理端消息 + 群机器人 |
| consecutive_failures ≥2（L2） | 2 | ERROR | 同上 + 值班电话（工作时段） |
| L3 冻结生效 | 即时 | CRITICAL | 同上 + 邮件管理员 |
| M5 主库映射率 <98% | 每日对账 | WARN | 管理端对账差异页 |
| 批次耗时 >5min / 单批 inserted+updated >500 | 每批 | INFO | 供容量评估，不入告警 |
| Excel 兜底导入发生 | 即时 | WARN | 隐含 HR 通道异常，需复盘根因 |

## 7.4 Excel 兜底操作规程（HR 长时间不可用时）

1. HR 方提供在职花名册导出（§3.3② 模板）；
2. 管理员经 `POST /org/users/import` 上传 → S4 异步任务；
3. 任务完成：错误行清单在 result 返回 → 修正重传（幂等，已导入行不受重复影响）；
4. 导入成功 → 自动解除 L2/L3 降级（source=excel 亦计入 last_success_at）；
5. HR 接口恢复后，次日全量对账与 Excel 数据自动收敛（同幂等键 upsert）；差异经 §6.3 人工确认。

---

# 8. 联调测试记录

## 8.1 测试环境与数据

| 项 | 内容 |
|---|---|
| 环境 | 本机 Python 3（标准库 sqlite3 内存库），模拟 ragflow-ext 同步引擎与 ext 三表 |
| 数据 | `联调用例/mock_departments.json`（36 部门节点，源自架构图识读）+ `mock_employees.json`（35 名管理层人员，含 2026-09-21 补录邓小军） |
| 脚本 | `联调用例/mock_org_sync_test.py`（表结构对齐 `ddl/ragflow_ext_schema.sql` A1/A2/A3 关键列） |
| 执行 | `python3 mock_org_sync_test.py`；2026-09-21 实测 **17/17 PASS**（见 8.2） |

> 说明：本联调验证的是**同步引擎核心逻辑**（幂等/full_path/对账/降级/锁）与数据规范的正确性；M03 五接口的 HTTP 层联调待 ragflow-ext 服务开发完成后，以 HR Mock Server（按 §4.1 约定）重放本批用例。

## 8.2 用例与结果

| # | 场景 | 验证点 | 结果 |
|---|---|---|---|
| S1 | 初始化全量导入 | 36 部门/35 人落库；full_path 生成正确（0001/0006，level=2） | PASS ×3 |
| S2 | 幂等重放 | 同批数据重复导入 inserted=0、行数不变 | PASS |
| S3 | 增量变更包 | 新增 0037→路径 0001/0030/0037 level=3；装订中心 merged_to=印后车间；工程科 revoked；田志兼职 ["0026"]；洪建宗离职停用；合并部门人员划转 | PASS ×4 |
| S4 | 图面口径对账 | 定编 1000 vs 1000 ✓；在编 1032 vs 1033 差 1 → 人工确认；active 部门 37-2=35；在编 35-1=34 | PASS ×4 |
| S5 | 三级降级演练 | 失败×1 仅计数；×2 → 50002/HTTP 503（details 含 last_success_at/consecutive_failures）；×3 → 冻结+告警；Excel 兜底成功 → 自动解冻清零 | PASS ×4 |
| S6 | 并发锁 | 同步运行中重复触发 → 40009/HTTP 422 | PASS |

**关键输出摘录**（50002 信封，S5 实测）：

```json
HTTP 503
{"code":50002,"message":"组织架构同步降级中，展示最近快照",
 "details":{"last_success_at":"2026-09-21 12:52:13","consecutive_failures":3}}
```

**批次日志一览**（S1~S6 共 8 批，ext_org_sync_log 实测）：hr_api/schedule/success ×3 → hr_api failed ×4（第 4 批含 degraded_events=freeze_dept_changes）→ excel/manual/success（解冻）。与 §5.4/§7.1 设计一致。

## 8.3 结论

1. 数据规范（编码/full_path/幂等键/主兼职）与降级机制（50002/40009）在模拟环境全部验证通过，可进入 ragflow-ext 开发（任务 2.3，2026-11-30~12-17）；
2. 初始化种子数据（36 节点 JSON）可直接作为开发与测试环境建库输入；
3. HTTP 层与 HR 真实接口联调的前置条件：Q1（HR 接口形态）与 ragflow-ext 骨架就绪。

---

# 9. 风险与遗留问题

| # | 事项 | 影响 | 建议/下一步 | 责任 |
|---|---|---|---|---|
| Q1 | HR 系统可编程接口形态待选型（8 类形态清单见 §4.1.0；OrgSourceProvider 适配层已设计，选型不阻塞开发） | 决定启用哪个 Adapter 与同步频率落点 | 甲方按清单选型；未定期间以 Excel 兜底先行（§7.4） | 甲方/项目经理 |
| Q2 | 海外营业部人员大框无科名（暂记「业务科」） | 部门树 0003 名称待定 | 业务确认后增量更名（dept_code 不变） | 业务 |
| Q3 | 厂务部中间框仅「廠務部」三字（暂记「厂部」） | 0007 语义（管理办公室 or 生产中心） | 业务确认 | 业务 |
| Q4 | 质检科与数码科创中心「何俊」同名 | ~~两工号 EMP0022/0023 是否同人~~ | **已确认（2026-09-21）：同一人** → 合并 EMP0022 一号两岗（主岗质检科 0020、兼职 0018），EMP0023 废弃不分配；人员种子 35→34 | 已关闭 |
| Q5 | 后勤物资科/基础设施科同框无负责人 | ~~0033/0034 是否确为两科~~ | **已确认（2026-09-21）：两科均属行政部**，编码 0033/0034（parent=0030）维持不变，关闭；负责人仍缺 → 按 §3.5 上溯行政部/公司级 | 已关闭 |
| Q6 | 图面在编 1032 vs 备注 1033 差 1 | 初始化基准口径 | 以 HR 花名册为准；图面值仅参考（§6.2） | HR |
| R1 | 架构图不含主任级（图题注明） | 人员全量缺失 P06 以下明细 | 初始化以 HR 花名册补齐，M5 映射率跟踪 | HR/开发 |
| R2 | 部门编码若与 HR 既有主键冲突 | upsert 键切换 | Q1 明确后以 HR 主键替换种子编码（映射表随附） | 开发 |

---

# 附录 A 部门编码分配总表（0001~0036）

| code | parent | 名称 | 定编/在编 | 负责人（职级） | 备注 |
|---|---|---|---|---|---|
| 0001 | — | 港深公司 | 1000/1033 | 徐明章（总经理） | 根；高管 5 人挂此 |
| 0002 | 0001 | 海外营业部 | 96/93 | 邓绍谦（营销总监） | 何定山分管 |
| 0003 | 0002 | 业务科（待确认 Q2） | — | 陈洁玲（高级经理）等 7 人 | 图面无科名 |
| 0004 | 0002 | 船务科 | — | 程红权（助理经理） | |
| 0005 | 0001 | 中国营业部 | 22/22 | 林文清（销售总监）、刘小庆（助理经理） | |
| 0006 | 0001 | 厂务部 | 711/743 | 姜员泉（生产总监） | |
| 0007 | 0006 | 厂部（待确认 Q3） | — | — | 图面中间框 |
| 0008~0009 | 0007 | 生产计划科/外发管理中心 | — | — | 无负责人标注 |
| 0010~0012 | 0007 | 工艺车间/印后车间/装订中心 | — | 王兵伟（助理经理，兼） | 一人三岗+印刷中心四岗 |
| 0013 | 0007 | 设备管理科 | — | 吴迎春（经理） | |
| 0014~0015 | 0007 | 仓库后勤运输科/成品包装科 | — | 冯二（助理经理，兼） | |
| 0016 | 0007 | 工程科 | — | — | 无负责人标注 |
| 0017 | 0007 | 印刷中心（印前中心/印刷车间） | — | 林子健（副经理）、王兵伟（助理经理） | |
| 0018 | 0007 | 数码科创中心 | — | 何俊（经理，兼） | Q4 已确认：与质检科同一人 |
| 0019~0020 | 0001 | 品质管理部/质检科 | 60/60 | 何俊（经理，主岗）、田志（助理经理） | Q4 已确认 |
| 0021~0022 | 0001 | 技术研发部/研发科 | 17/17 | 周树标（经理） | 杜科分管；技术科 0024 改隶此部 |
| 0023 | 0001 | 培训中心 | 6/6 | 邓小军（经理） | 负责人图面缺失，2026-09-21 补录 |
| 0024 | 0021 | 技术科 | — | 杨波（副经理） | 原挂培训中心（图面），2026-09-21 改隶技术研发部；编码不变 |
| 0025~0026 | 0001 | 人力资源部/环保体系科 | 12/12 | 容正思（人力资源经理）、陈安（助理经理） | |
| 0027~0029 | 0001 | 安全保卫部/安全保卫科/工业安全科 | 10/10 | 康彦涛（安全总监） | 张颜艳分管 |
| 0030 | 0001 | 行政部 | 32/31 | — | |
| 0031~0034 | 0030 | 报关科/行政科/后勤物资科/基础设施科 | — | 张改换（副经理）、苏金玲（副经理） | Q5 已确认：两科属行政部，关闭 |
| 0035 | 0001 | 采购部 | 10/10 | 赖伟峰（经理） | |
| 0036 | 0001 | 财务部 | 24/23 | 邱旭力（经理）、夏军（副经理）、张海宏（助理经理） | 萧永健分管 |

> 新增部门自 0037 顺延取号（联调 S3 已演练 0037 新媒体运营科）；merged/revoked 号段不回收。

# 附录 B 人员映射样例（节选，全量见 联调用例/mock_employees.json）

| employee_no | 姓名 | 主部门 | 兼职 | 岗位 |
|---|---|---|---|---|
| EMP0001 | 徐明章 | 0001 | — | 总经理（P01） |
| EMP0002 | 何定山 | 0001 | — | 副总经理（P02） |
| EMP0003/0004 | 杜科/张颜艳 | 0001 | — | 助理总经理（P03） |
| EMP0005 | 萧永健 | 0001 | — | 财务总监（P03） |
| EMP0018 | 王兵伟 | 0010 | 0011,0012,0017 | 助理经理（P06） |
| EMP0020 | 冯二 | 0014 | 0015 | 助理经理（P06） |
| EMP0022 | 何俊 | 0020 | 0018 | 经理（P04），一人两岗（Q4 已确认同一人，EMP0023 废弃） |
| EMP0036 | 邓小军 | 0023 | — | 经理（P04），2026-09-21 补录（图面缺失） |
| 其余 27 人 | 见 JSON | — | — | P03~P09 |

# 附录 C 架构图识读工具链备忘

1. `pdftoppm -r 200` 出 `org200-1.ppm`（注意多页输出带页号后缀 `-1`）；
2. 六块直切切片（org_t1..t6）可辨单框内容，但切断方框与父子连线，不满足层级还原；
3. `band.py`（纯 Python PPM→PNG + 90°顺时针旋转）输出整幅正置图 org_cw.png + 6 条横向条带 band1..6.png（**30% 重叠**），重叠区保住跨块连线；
4. 条带逐块识读 + 整图对照拼接 → 36 节点树；识读结果固化为 mock_departments.json（种子数据）；
5. 运维教训：复合命令与 heredoc 有竞态——单条串行 + py_compile/ast 预校验 + nohup 后台 + sleep 后查验产物。

# 附录 D 交付物清单

| 交付物 | 位置 |
|---|---|
| 本方案 | `/home/admin/ragflow/组织架构对接方案.md` |
| 部门种子数据（36 节点） | `/home/admin/ragflow/联调用例/mock_departments.json` |
| 人员种子数据（35 人） | `/home/admin/ragflow/联调用例/mock_employees.json` |
| 同步引擎模拟脚本（17/17 PASS） | `/home/admin/ragflow/联调用例/mock_org_sync_test.py` |
| 架构识读中间产物 | `/tmp/org_t*.png`、`/tmp/org_cw.png`、`/tmp/band1..6.png`、`/tmp/band.py`（临时区，不随仓库分发） |

# 附录 E 人员维护操作手册（管理员/HR 日常用）

## E.1 核心原则

**人事数据以 HR 系统为唯一事实源，RAGFlow 侧不手工建人、不手改库**。所有人员变更从 HR 侧发起、由同步任务自动落库——防止「双头维护」造成对账漂移；直接 SQL 修改 `ext_user` 会在下个同步批次被 HR 数据覆盖，且破坏 `hr_updated_at/synced_at` 对账链。

## E.2 场景速查表

| 变更场景 | HR 侧操作 | RAGFlow 侧自动行为 | 生效时间 |
|---|---|---|---|
| 新员工入职 | 录入工号/姓名/部门/岗位 | 增量批次 upsert 插入（inserted） | ≤15 分钟 |
| 调岗/换部门 | 变更所属部门 | 更新 `dept_code`（即时影响检索权限，无需文档补刷） | ≤15 分钟 |
| 兼职部门增减 | 变更兼职部门字段 | 重建 `secondary_depts` 数组 | ≤15 分钟 |
| 员工离职 | 置离职状态 | `status=0` 停用（记录保留） | ≤15 分钟 |
| 姓名/岗位修正 | 修正字段 | 按 `employee_no` 幂等覆盖 | ≤15 分钟 |
| 急用不等批次 | — | 管理端 `POST /org/sync` 手动触发 | 立即 |

## E.3 三条操作路径

**路径 A：HR 接口同步（日常主通道，无需人工）**——HR 侧完成人事操作后，15 分钟增量（H4）或每日 02:30 全量对账（H3）自动落库；等不及就管理端手动触发 `POST /org/sync`（运行中重复触发返回 40009 属正常并发锁）。

**路径 B：Excel 兜底导入（HR 接口不可用/降级期）**：
1. HR 方导出花名册（§3.3② 模板：employee_no / display_name / dept_path / secondary_dept_paths / position_name / status）；
2. 管理端 `POST /org/users/import` 上传 → S4 异步任务；
3. 错误行清单在任务 result 返回（如 dept_path 无法解析），修正重传即可（幂等）；
4. 导入成功自动解除 50002 降级/冻结（§7.1）。

**路径 C：管理端查询核验**：`GET /org/users`（按工号/部门查映射）、`GET /org/sync/status`（批次统计与降级状态）、`GET /org/departments/tree`（确认部门在树中）。

## E.4 前置条件与红线

| # | 规则 | 说明 |
|---|---|---|
| 1 | **部门必须先存在** | 新人员 `dept_code` 必须已在 `ext_department`，否则该行拒绝（孤儿节点防护 §3.2）；新部门编码自 0037 顺延（附录 A） |
| 2 | **RAGFlow 账号映射回填** | 新员工先落 `ext_user`（映射待回填），有账号后按 email 自动匹配；未匹配项出现在对账 M5 缺口清单，管理端人工绑定 |
| 3 | **以工号为唯一键** | 禁止按姓名操作人员（Q4 教训：同名按一号两岗处理，如 EMP0022 主质检科兼数码科创中心） |
| 4 | **禁止手工改库** | `ext_user` 只允许同步任务/Excel 导入两条写入通道 |
| 5 | **离职不物理删除** | `status=0` 停用保留审计与历史审批链 |

## E.5 异常处置

| 现象 | 排查顺序 |
|---|---|
| 人员变更未生效 | ① `GET /org/sync/status` 看最近批次 result 与 user_stats；② 确认 HR 侧数据已提交且部门已存在；③ 手动触发 `POST /org/sync` |
| 批次出现 failed | 看 error_msg：HR 接口超时 → 等 §7.1 降级计数自然处理；单行校验错 → 该行不入库，修正后下批次重放 |
| 50002 降级中 | 展示旧快照属预期；紧急维护走路径 B Excel 导入（成功即自动恢复） |
| 映射率下降（M5） | 管理端对账差异页查看未绑定清单 → 补 email 或人工绑定 |

## E.6 无 HR 系统时的运作模式

甲方未部署 HR 系统（或无可编程接口）时，组织数据事实源为**人工维护的台账 Excel**（§3.3② 冻结模板），同步触发从「定时拉取」降档为「人工导入」，其余骨架（编码规范/幂等/对账/权限）全部不变：

| 项 | 有 HR 系统 | 无 HR 系统（形态 8） |
|---|---|---|
| 事实源 | HR 系统 | 台账 Excel（冻结模板，版本受控） |
| 责任人 | HR 专员（系统操作留痕） | 指定唯一「组织数据责任人」，禁止多头修改 |
| 同步触发 | 15 分钟增量 + 每日 02:30 全量自动 | 每周固定 / 异动即导，人工 `POST /org/users/import` |
| 对账基准 | HR 接口 vs 系统数据 | 台账 vs 系统数据（M1~M6 照常跑） |
| 降级链路 | 50002 三级降级（§7.1） | 不触发（无接口超时）；仅剩导入行级校验错误 |
| 升级路线 | — | ① 钉钉/企微通讯录（形态 7，自动化）；② ext 升格组织主数据源（二期候选，需客户确认） |

红线不变：仍以工号为唯一键（禁止按姓名操作）、部门先行（孤儿拒绝）、禁止手工改库——此模式下写入通道仅剩 Excel 导入一条，误操作风险更低。
